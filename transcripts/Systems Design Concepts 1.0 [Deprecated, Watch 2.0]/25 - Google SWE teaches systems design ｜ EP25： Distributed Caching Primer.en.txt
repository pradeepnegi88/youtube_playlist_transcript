all right it's time for another video i all right it's time for another video i want to thank everyone else who's new want to thank everyone else who's new again for subscribing because it seems again for subscribing because it seems like there's a lot of you today and that like there's a lot of you today and that is sweet um just to give a quick little is sweet um just to give a quick little rant before i start today's video rant before i start today's video um i hope that all this stuff has been um i hope that all this stuff has been useful just as kind of like a primer for useful just as kind of like a primer for those of you who are first starting to those of you who are first starting to look at this channel look at this channel i see a ton of systems design channels i see a ton of systems design channels out there and that's great and i'm out there and that's great and i'm trying to make myself unique for most of trying to make myself unique for most of those by you know starting to do deeper those by you know starting to do deeper dives and actually providing something dives and actually providing something of value to you guys because the reason of value to you guys because the reason i started this channel was i read i started this channel was i read designing data intensive applications designing data intensive applications like probably a lot of you have or will like probably a lot of you have or will do at some point if you're watching this do at some point if you're watching this and then you're going to wonder well and then you're going to wonder well what do i do from here how does this what do i do from here how does this actually translate to a systems design actually translate to a systems design interview and how do i actually hone my interview and how do i actually hone my skills in a way that you know reading skills in a way that you know reading that book was worthwhile and that's that book was worthwhile and that's where i hope this channel comes in i where i hope this channel comes in i mean i literally see a million computer mean i literally see a million computer science videos on youtube and they're science videos on youtube and they're all so useless like it's you know tips all so useless like it's you know tips and tricks to put on your resume and and tricks to put on your resume and this is how i got my internship and like this is how i got my internship and like those channels are literally all more those channels are literally all more redundant than a replicated database and redundant than a replicated database and at the end of the day like i'm just at the end of the day like i'm just trying to provide something of value to trying to provide something of value to you guys and you know hopefully help out you guys and you know hopefully help out the community as a whole so without the community as a whole so without further ado i'm going to do a pretty further ado i'm going to do a pretty introductory video on caching which you introductory video on caching which you could probably get from other channels could probably get from other channels but for the sake of completeness on my but for the sake of completeness on my channels i want to do it and then i am channels i want to do it and then i am going to do a deep dive into some of going to do a deep dive into some of those in-memory services that you can those in-memory services that you can use in order to kind of implement use in order to kind of implement caching on a distributed scale all right caching on a distributed scale all right okay so introduction to caching well okay so introduction to caching well what is caching caching is a super what is caching caching is a super useful thing that pretty much any super useful thing that pretty much any super large at scale system is going to need large at scale system is going to need where you are basically going to provide where you are basically going to provide lower latency reads and oftentimes even lower latency reads and oftentimes even rights to the end user rights to the end user just this is generally done either by just this is generally done either by improving data locality which means you improving data locality which means you hold the data closer to the user or hold the data closer to the user or you're using some faster form of data you're using some faster form of data storage and in distributed systems what storage and in distributed systems what that generally means is you're storing that generally means is you're storing data in ram as opposed to disk data in ram as opposed to disk caching accomplishes three main things caching accomplishes three main things the first one is there are going to be the first one is there are going to be fewer network calls to the database fewer network calls to the database which is probably going to be further which is probably going to be further away than where your cache away than where your cache systems are located systems are located additionally if you ever need to do huge additionally if you ever need to do huge computations or you know if you're computations or you know if you're making a duplicated request to a server making a duplicated request to a server you can store a bunch of pre-computed you can store a bunch of pre-computed values we've spoken about this in values we've spoken about this in analytics processing with something like analytics processing with something like data cubes and then finally data cubes and then finally it reduces the read load on the database it reduces the read load on the database heavily and possibly even the right load heavily and possibly even the right load by virtue of intercepting a bunch of by virtue of intercepting a bunch of these operations and handling them on these operations and handling them on their own their own okay so what are the main tradeoffs of okay so what are the main tradeoffs of caching well there are a ton of pros caching well there are a ton of pros here and you know obviously they're cons here and you know obviously they're cons but if there weren't so many pros not but if there weren't so many pros not everyone would do it but here are the everyone would do it but here are the pros the pros are hugely faster reads a pros the pros are hugely faster reads a read from a cache can be well over in read from a cache can be well over in order of magnitude which means 10 times order of magnitude which means 10 times more than a read from a database more than a read from a database however if we attempt to find our data however if we attempt to find our data in the cache and it's not there in the cache and it's not there which is called a cache miss then having which is called a cache miss then having that cache is going to slow things down that cache is going to slow things down so there are kind of so there are kind of a couple of scenarios where this can a couple of scenarios where this can happen happen happen one of them is called thrashing one of them is called thrashing thrashing is when you know let's say we thrashing is when you know let's say we have a small cache and me and my friend have a small cache and me and my friend are both trying to are both trying to load our facebook profiles but when i load our facebook profiles but when i load my profile and it's originally not load my profile and it's originally not in the cache now we have jordan's in the cache now we have jordan's profile on the cache then he loads his profile on the cache then he loads his so now his is in the cache it's actually so now his is in the cache it's actually replaced mine because it was such a replaced mine because it was such a small piece of cache and then i try and small piece of cache and then i try and load mine again now mine is no longer in load mine again now mine is no longer in the cache we have a bunch of cache the cache we have a bunch of cache misses and we've actually slowed things misses and we've actually slowed things down quite a bit down quite a bit this can also happen because of poor this can also happen because of poor cash eviction policies i guess thrashing cash eviction policies i guess thrashing is technically an example of a poor cash is technically an example of a poor cash eviction policy but you know generally eviction policy but you know generally there are other poor cash eviction there are other poor cash eviction policies as well that aren't just you policies as well that aren't just you know because the size of the cache was know because the size of the cache was too small too small additionally caches can potentially additionally caches can potentially improve the speed of rights and i'll improve the speed of rights and i'll touch upon that in a bit touch upon that in a bit the biggest con is the increased the biggest con is the increased complexity of each right or generally complexity of each right or generally speaking just you know kind of the speaking just you know kind of the schema that you're going to use to keep schema that you're going to use to keep the data and the cache valid and up to the data and the cache valid and up to date date date this is kind of a design by design type this is kind of a design by design type of thing and obviously there are of thing and obviously there are trade-offs for each design but you trade-offs for each design but you really have to think about what users really have to think about what users need for your application and then make need for your application and then make the right decision here there's not one the right decision here there's not one objectively correct answer for any objectively correct answer for any approach approach approach so what are some cash eviction policies so what are some cash eviction policies well first of all let's start by well first of all let's start by discussing what a cash eviction even is discussing what a cash eviction even is like i said generally speaking we're like i said generally speaking we're storing our cache data in ram storing our cache data in ram ram is more expensive and as a result of ram is more expensive and as a result of that most computers don't have a lot of that most computers don't have a lot of ram and so generally speaking when you ram and so generally speaking when you get a bunch of data in your cache get a bunch of data in your cache eventually you're going to have to evict eventually you're going to have to evict some of it which means that there's some of it which means that there's newer newer data which would be better newer newer data which would be better off going in the cache and you're going off going in the cache and you're going to have to get rid of some older data to have to get rid of some older data so what i've done here is i've listed a so what i've done here is i've listed a bunch of cache eviction policies but the bunch of cache eviction policies but the truth of the matter is most of these truth of the matter is most of these suck and the only one that you should suck and the only one that you should really be using probably out of these is really be using probably out of these is least recently used you'll see i wrote least recently used you'll see i wrote sliding window at the bottom there which sliding window at the bottom there which actually can work very well but it's actually can work very well but it's kind of still a matter of research and kind of still a matter of research and it's not as widely implemented so for it's not as widely implemented so for now let's ignore that and just say that now let's ignore that and just say that least recently used is the most valid least recently used is the most valid one and so you know if it comes up in a one and so you know if it comes up in a systems design interview or something systems design interview or something and they're like how might you implement and they're like how might you implement this this is a lead code problem and the this this is a lead code problem and the answer is a doubly linked list and a answer is a doubly linked list and a hash map every single time you access hash map every single time you access something that means it's been recently something that means it's been recently used so you put it at the head of the used so you put it at the head of the linked list and if you're going to linked list and if you're going to delete something from there you have to delete something from there you have to delete it from the doubly linked list delete it from the doubly linked list and delete it from the hashmap and delete it from the hashmap okay so now we have a couple types of okay so now we have a couple types of cache that we can actually think about cache that we can actually think about because the question is you know wow all because the question is you know wow all of our application servers are accessing of our application servers are accessing the database and all of the results from the database and all of the results from the database are going through the the database are going through the application server so wouldn't it make application server so wouldn't it make sense for us to just use the application sense for us to just use the application server's ram as a cache server's ram as a cache and the answer to that is well kind of and the answer to that is well kind of but not really so even though the but not really so even though the application server cache helps a lot application server cache helps a lot because it means that you don't have to because it means that you don't have to make any extra network calls hitting make any extra network calls hitting some dedicated cache server some dedicated cache server the fact that each server has its own the fact that each server has its own cache means a couple of things means cache means a couple of things means that one that one all of those cache results are kind of all of those cache results are kind of limited to that server another server limited to that server another server can't really access them can't really access them two is that if that application server two is that if that application server dies the cache goes with it and three is dies the cache goes with it and three is that if you want you know to add a bit that if you want you know to add a bit more cash or you know add more servers more cash or you know add more servers without adding more cash without adding more cash application servers are going to scale application servers are going to scale linearly with the application server linearly with the application server cache so generally speaking what most cache so generally speaking what most people choose to do is use a global people choose to do is use a global cache and you know this might be cache and you know this might be something like redis or memcache but something like redis or memcache but some service that actually allows you to some service that actually allows you to use nodes as an independent cache use nodes as an independent cache and you know obviously we'll talk about and you know obviously we'll talk about those services in a future video but those services in a future video but generally speaking global caches are generally speaking global caches are good because they scale independently of good because they scale independently of the application server cache and they the application server cache and they also don't die when an application also don't die when an application server does server does and additionally they can be accessed by and additionally they can be accessed by any application server and then any application server and then furthermore because we have all of these furthermore because we have all of these nodes they can be replicated and nodes they can be replicated and partitioned in any schema that we want partitioned in any schema that we want and we'll see this in play eventually and we'll see this in play eventually okay so in terms of keeping cache okay so in terms of keeping cache updated because we know that obviously updated because we know that obviously we want to be able to get fast reads we want to be able to get fast reads from the cache however if we're writing from the cache however if we're writing data which most applications are data which most applications are generally speaking the data in the generally speaking the data in the database is changing over time you don't database is changing over time you don't want a ton of stale data in your cache want a ton of stale data in your cache so obviously you can just have so obviously you can just have you know cache data eventually expire you know cache data eventually expire after say a couple hours and then when after say a couple hours and then when you re-access it it'll be repopulated you re-access it it'll be repopulated with the newest stuff but generally with the newest stuff but generally speaking you don't want to just wait on speaking you don't want to just wait on some time for it to expire and you want some time for it to expire and you want to kind of take a more active approach to kind of take a more active approach in keeping the data up to date so i'm in keeping the data up to date so i'm going to look at three approaches of going to look at three approaches of writing in a system that implements writing in a system that implements cache and we'll see kind of the cache and we'll see kind of the trade-offs of each of those trade-offs of each of those so the first is actually going to be so the first is actually going to be right through cache so just to quickly right through cache so just to quickly explain this diagram the computer is explain this diagram the computer is basically going to be like a client and basically going to be like a client and you know let's assume there's just some you know let's assume there's just some application server that is attached to application server that is attached to that that that the red thing is the diagram for redis the red thing is the diagram for redis but for right now let's just call that a but for right now let's just call that a cache server and then that silver thing cache server and then that silver thing is the database is the database so so so the right through cache is basically the right through cache is basically doing the following we're going to write doing the following we're going to write both the cache and the database at the both the cache and the database at the same time in parallel and obviously a same time in parallel and obviously a write is going to be successful once you write is going to be successful once you get successes from both of those get successes from both of those if you have you know thought about this if you have you know thought about this a little bit you can see that this is a little bit you can see that this is going to be pretty problematic in the going to be pretty problematic in the sense that anytime you're writing two sense that anytime you're writing two different things over the network it is different things over the network it is probably going to require some sort of probably going to require some sort of distributed or atomic transaction unless distributed or atomic transaction unless you really don't care about the you really don't care about the consistency that much consistency that much you know if we have to do an atomic you know if we have to do an atomic transaction what that means is that we transaction what that means is that we probably need two-phase commit probably need two-phase commit because you know these aren't identical because you know these aren't identical replicas of one another so we can't just replicas of one another so we can't just use raft here what we do probably need use raft here what we do probably need is two-face commit with a coordination is two-face commit with a coordination manager and as a result that's gonna manager and as a result that's gonna really slow down the rights really slow down the rights another option is right around cash so another option is right around cash so what you do here and this is probably what you do here and this is probably the most simple implementation of the most simple implementation of writing with a cash system is you just writing with a cash system is you just write to the database as expected and write to the database as expected and then the other thing you have to do is then the other thing you have to do is also invalidate the current cache data also invalidate the current cache data for that key so you basically just go to for that key so you basically just go to the cache and say delete the data for the cache and say delete the data for this key this key the client is then going to eventually the client is then going to eventually request to read from the cache for request to read from the cache for whatever data it is that you updated whatever data it is that you updated and then eventually what's going to and then eventually what's going to happen is the cache is going to say wait happen is the cache is going to say wait i don't have that database give me the i don't have that database give me the data and then report it back to the data and then report it back to the client client client so that's obviously decently simple but so that's obviously decently simple but the the issue with that is it's going to the the issue with that is it's going to result in a cache miss the first time so result in a cache miss the first time so now moving on to the third scheme we now moving on to the third scheme we have right back cache so this one's have right back cache so this one's pretty interesting we actually write pretty interesting we actually write data only to the cache at first data only to the cache at first and then eventually the data is going to and then eventually the data is going to go from the cache to the database go from the cache to the database possibly even in bulk and that way we possibly even in bulk and that way we make fewer network calls to the database make fewer network calls to the database and make sure that and make sure that everything is actually going to get everything is actually going to get there in one piece there in one piece so even though this approach has a fast so even though this approach has a fast write speed it's definitely got its write speed it's definitely got its downsides downsides downsides in the case where we can't deal with in the case where we can't deal with eventual consistency such as something eventual consistency such as something like changing a password because like changing a password because obviously you need the most updated obviously you need the most updated thing to be in the database which is our thing to be in the database which is our source of truth source of truth right back is not going to work right back is not going to work additionally since all of our data in additionally since all of our data in the cache is going to be in memory the cache is going to be in memory if we were to write some data to the if we were to write some data to the cache and then that cache went ahead and cache and then that cache went ahead and failed failed failed then we just lost our right so maybe then we just lost our right so maybe that's not the hugest deal to have lost that's not the hugest deal to have lost rights but in some systems it certainly rights but in some systems it certainly is so in that case write back cash is is so in that case write back cash is unacceptable unacceptable unacceptable what if we wanted to be able to have a what if we wanted to be able to have a slight modification of write-back cash slight modification of write-back cash where we actually achieved some sort of where we actually achieved some sort of cash consistency so not only would we cash consistency so not only would we use write-backs that clients see fast use write-backs that clients see fast rights on their end but additionally rights on their end but additionally when another server say wants to read when another server say wants to read that data that may have been modified we that data that may have been modified we make sure that they're actually reading make sure that they're actually reading the stuff that could still potentially the stuff that could still potentially be in that cache be in that cache well the way we could do this is by well the way we could do this is by using something like a distributed lock using something like a distributed lock service so i've mentioned zookeeper and service so i've mentioned zookeeper and at cd in the past and you know those are at cd in the past and you know those are based on coordination services and based on coordination services and consensus algorithms to work and consensus algorithms to work and basically what's going to happen here basically what's going to happen here is every single time one server wants to is every single time one server wants to read a piece of data that may have been read a piece of data that may have been modified by a cache modified by a cache we know that any cache server that has we know that any cache server that has modified a piece of data in order to do modified a piece of data in order to do so must grab a lock for that object from so must grab a lock for that object from the lock service the lock service and so when another server wants to read and so when another server wants to read it it'll basically say to the lock it it'll basically say to the lock server okay i'm trying to read this data server okay i'm trying to read this data the lock server will say okay i'm going the lock server will say okay i'm going to have to kick the cache server off the to have to kick the cache server off the lock and the cache server will then say lock and the cache server will then say okay but before i get off the lock i'm okay but before i get off the lock i'm going to go ahead and send my going to go ahead and send my modifications back to the database so modifications back to the database so that other people can go ahead and read that other people can go ahead and read this this this so that's kind of one way of dealing so that's kind of one way of dealing with that if you've ever heard of the with that if you've ever heard of the frangipani file system frangipani file system this is kind of the approach they took this is kind of the approach they took where where where you could make cash rights on your local you could make cash rights on your local machine but if anyone else wanted to go machine but if anyone else wanted to go ahead and read your rights from a remote ahead and read your rights from a remote machine machine machine the local machine would have to give up the local machine would have to give up its locks and send those changes back to its locks and send those changes back to the central server store the central server store obviously the the downside of this is obviously the the downside of this is that since only one server can hold the that since only one server can hold the lock at a time it's going to greatly lock at a time it's going to greatly reduce both the write and read reduce both the write and read throughput of whatever object it is that throughput of whatever object it is that you're trying to modify you're trying to modify okay so in practice i've kind of okay so in practice i've kind of talked about cache right now as this talked about cache right now as this abstraction where it's just like a abstraction where it's just like a single memory server single memory server and the truth of the matter is that's and the truth of the matter is that's obviously not acceptable obviously not acceptable if the cache server goes down if the cache server goes down we want it to be available or at least we want it to be available or at least the data that it held to still be the data that it held to still be available available available if a cache server isn't big enough to if a cache server isn't big enough to hold all the data that should be getting hold all the data that should be getting cached cached cached it probably needs to be partitioned it probably needs to be partitioned cache servers need to be able to cache servers need to be able to coordinate with one another or coordinate with one another or communicate with one another to say communicate with one another to say who's up and who's not and additionally who's up and who's not and additionally sometimes it's not even sufficient to sometimes it's not even sufficient to just search by keys and values sometimes just search by keys and values sometimes we want something like secondary indexes we want something like secondary indexes so in practice we have all of these you so in practice we have all of these you know third-party services that have been know third-party services that have been built on top of you know the idea of built on top of you know the idea of caching but they add all of these caching but they add all of these concepts that we talked about in the concepts that we talked about in the past in this channel and so we're past in this channel and so we're actually going to take a look at those actually going to take a look at those next and that's things like redis and next and that's things like redis and and memcached which i think is a little and memcached which i think is a little bit more simple than redis but we're bit more simple than redis but we're still going to end up talking about them still going to end up talking about them both both both so in conclusion caching is a super so in conclusion caching is a super important part of any distributed system important part of any distributed system with a ton of users and users that kind with a ton of users and users that kind of need low latency of need low latency it greatly improves read latency and it greatly improves read latency and depending on the design of your actual depending on the design of your actual system possibly even the right latency system possibly even the right latency if you're using something like a right if you're using something like a right back cache back cache generally speaking you want to be generally speaking you want to be decoupling your cache services from your decoupling your cache services from your application servers so that they can application servers so that they can grow independently and scale on their grow independently and scale on their own and also not fail if a server does own and also not fail if a server does but of course this comes at the cost of but of course this comes at the cost of having to make additional rpc or network having to make additional rpc or network calls every single time you're updating calls every single time you're updating or changing the cash or changing the cash and then in terms of rights in a system and then in terms of rights in a system with cash being implemented with cash being implemented we want to use right-back cash for we want to use right-back cash for minimal right latency but of course minimal right latency but of course that's at the cost of eventual that's at the cost of eventual consistency of you know the cash and the consistency of you know the cash and the the database which you know may not be the database which you know may not be feasible for your use case feasible for your use case um if you want maximum consistency um if you want maximum consistency between the cash and the database then between the cash and the database then right through is your move however it right through is your move however it requires two phase commit or some sort requires two phase commit or some sort of other atomic commit algorithm which of other atomic commit algorithm which is going to slow things down quite a bit is going to slow things down quite a bit and then right around cache while being and then right around cache while being the most simple comes at the cost of the most simple comes at the cost of having an initial cache miss every having an initial cache miss every single time you go ahead and make a single time you go ahead and make a write to the database so that the cache write to the database so that the cache can actually repopulate itself with the can actually repopulate itself with the correct data correct data okay guys i hope this serves as a decent okay guys i hope this serves as a decent introduction or primer to caching i'm introduction or primer to caching i'm going to go much more in depth going to go much more in depth in terms of actual services that you can in terms of actual services that you can use for caching use for caching and possibly even what existing and possibly even what existing companies have done to make caching work companies have done to make caching work for them and keep everything consistent for them and keep everything consistent
okay we are back for another episode i okay we are back for another episode i just got some pre-workout in me and i've just got some pre-workout in me and i've got as much energy as a silverback got as much energy as a silverback gorilla gorilla gorilla um today we're going to be talking about um today we're going to be talking about angela merkel trees aka merkle trees angela merkel trees aka merkle trees they're not actually named after her they're not actually named after her so let's go ahead and get into that so let's go ahead and get into that because i want to get myself to the gym because i want to get myself to the gym okay so merkle trees what are they okay so merkle trees what are they merkle trees are really just an merkle trees are really just an algorithm that easily allows you to algorithm that easily allows you to compute uh basically a set of compute uh basically a set of differences over two similar sets of differences over two similar sets of records they do this really quickly and records they do this really quickly and efficiently using basically something efficiently using basically something known as a hash tree and what this goes known as a hash tree and what this goes ahead and is really useful for are ahead and is really useful for are things like a git version control system things like a git version control system or an or an anti-entropy process in some sort of anti-entropy process in some sort of leaderless database like cassandra or leaderless database like cassandra or dynamodb and also in blockchain or dynamodb and also in blockchain or decentralized currencies basically decentralized currencies basically anytime you need to calculate the anytime you need to calculate the difference between two things and difference between two things and ultimately attempt to rectify them by ultimately attempt to rectify them by sending some changes over a network a sending some changes over a network a merkle tree is extremely useful for merkle tree is extremely useful for allowing you to calculate the smallest allowing you to calculate the smallest unit of difference between two unit of difference between two two sets of records two sets of records so let's look at the basic algorithm for so let's look at the basic algorithm for a merkle tree so we have these four a merkle tree so we have these four files here a b c and d and the first files here a b c and d and the first thing we're going to do is go ahead and thing we're going to do is go ahead and calculate some hashes for them and these calculate some hashes for them and these are going to be the leaf nodes in our are going to be the leaf nodes in our tree tree tree so i'm going to put these leaf nodes so i'm going to put these leaf nodes down here and now every single parent down here and now every single parent node of any node is going to basically node of any node is going to basically incorporate the two hashes into its own incorporate the two hashes into its own hash and go ahead and use that so hash and go ahead and use that so basically to get the hashes of the basically to get the hashes of the parent node i would say concatenate parent node i would say concatenate those two strings of each child node and those two strings of each child node and then go ahead and take the hash of that then go ahead and take the hash of that we would do the same thing all the way we would do the same thing all the way up to the root now imagine i wanted to up to the root now imagine i wanted to make a change to one of the files so i'm make a change to one of the files so i'm going to change c dot txt so you can see going to change c dot txt so you can see that the hash of c dot txt has changed that the hash of c dot txt has changed thus changing that leaf node thus thus changing that leaf node thus changing the parent node and ultimately changing the parent node and ultimately changing the root changing the root so now after i make that change if i so now after i make that change if i want to go ahead and compare the want to go ahead and compare the previous merkle tree with the current previous merkle tree with the current merkle tree to kind of see what file merkle tree to kind of see what file actually changed i would do the actually changed i would do the following following following i would go ahead and look at these two i i would go ahead and look at these two i would say okay well the roots different would say okay well the roots different so i know that i have a difference so i know that i have a difference between them somewhere i'm going to have between them somewhere i'm going to have to traverse that then i would say okay to traverse that then i would say okay the right node or the right child of the the right node or the right child of the root is different so i'm going to have root is different so i'm going to have to traverse the right path and then to traverse the right path and then ultimately that child of 93 pq versus ultimately that child of 93 pq versus 4w12 is different and so that's where 4w12 is different and so that's where the differences lie between the two and the differences lie between the two and so you know if i wanted to kind of so you know if i wanted to kind of propagate that change over the network propagate that change over the network now as opposed to having to propagate now as opposed to having to propagate every single file and go ahead and do every single file and go ahead and do this kind of naive implementation where this kind of naive implementation where we waste a ton of network bandwidth i we waste a ton of network bandwidth i can only go ahead and can only go ahead and propagate the change file and even still propagate the change file and even still there are optimizations to be made even there are optimizations to be made even beyond that where you basically only beyond that where you basically only propagate the differences of the file propagate the differences of the file and that's kind of what git does where and that's kind of what git does where you know you have the original file but you know you have the original file but then git calculates the diffs and then then git calculates the diffs and then the diffs are propagated over the the diffs are propagated over the network but you use the merkle tree to network but you use the merkle tree to actually go ahead and calculate where actually go ahead and calculate where the diffs are in the first place the diffs are in the first place okay okay okay so how do merkle trees actually work in so how do merkle trees actually work in git well it's a little bit different git well it's a little bit different than what i just showed in the sense than what i just showed in the sense that git is a versioning system which that git is a versioning system which means that we want to be able to look at means that we want to be able to look at old commits as well as opposed to just old commits as well as opposed to just seeing the updated merkle tree of any seeing the updated merkle tree of any state because it's important to be able state because it's important to be able to kind of go back in time to kind of go back in time so in order to do this what we actually so in order to do this what we actually do is every single file that gets do is every single file that gets changed almost gets treated like its own changed almost gets treated like its own file and we go ahead and take the hash file and we go ahead and take the hash of it so if the hash is in the merkle of it so if the hash is in the merkle tree even if it's under a different name tree even if it's under a different name like it has some different metadata we like it has some different metadata we just go ahead and don't change anything just go ahead and don't change anything and you know that's efficient but if and you know that's efficient but if there is a new hash that we go ahead and there is a new hash that we go ahead and do is instead of modifying the old hash do is instead of modifying the old hash that it corresponds to we go ahead and that it corresponds to we go ahead and add it as a leaf to the merkle tree and add it as a leaf to the merkle tree and then create this entirely new branch of then create this entirely new branch of that merkle tree and ultimately go up that merkle tree and ultimately go up and propagate those changes all the way and propagate those changes all the way to the root however the new root that to the root however the new root that we're creating and all those new we're creating and all those new intermediate nodes that we're creating intermediate nodes that we're creating are all going to be copies we're not are all going to be copies we're not actually modifying the old merkle tree actually modifying the old merkle tree and what that means is that if we're to and what that means is that if we're to use an old pointer to a root uh use an old pointer to a root uh representing an old commit we can still representing an old commit we can still go ahead and access those old hashes to go ahead and access those old hashes to the files and go ahead and retrieve the the files and go ahead and retrieve the prior versions of those files prior versions of those files okay okay okay in cassandra we have anti-entropy so in cassandra we have anti-entropy so merkle trees are really useful for merkle trees are really useful for basically going ahead and determining basically going ahead and determining the differences between two replicas as the differences between two replicas as a result a result determining you know the basically the determining you know the basically the smallest unit of change that can be sent smallest unit of change that can be sent over the network and then doing so in over the network and then doing so in order to ensure that they are eventually order to ensure that they are eventually consistent with one another however if consistent with one another however if you think about this for a second you think about this for a second there's a little bit of an issue there's a little bit of an issue isn't the data actually always going to isn't the data actually always going to be changing so as a result you might go be changing so as a result you might go ahead and perform anti-entropy and by ahead and perform anti-entropy and by the time the data gets there it's the time the data gets there it's already stale already stale so so so that could be the case however cassandra that could be the case however cassandra actually does this actually does this anti-entropy process not on the raw data anti-entropy process not on the raw data itself not on the keys and values but itself not on the keys and values but instead on those immutable ss table instead on those immutable ss table files that are part of cassandra's lsm files that are part of cassandra's lsm tree based architecture tree based architecture so this is something known as so this is something known as incremental read repair basically incremental read repair basically cassandra uses immutable ss table files cassandra uses immutable ss table files and as a result and as a result if two replicas have kind of these if two replicas have kind of these similar ss table files and they have yet similar ss table files and they have yet to be repaired we can basically just go to be repaired we can basically just go ahead and repair an ss table file once ahead and repair an ss table file once because we know that once all of those because we know that once all of those replicas have agreed on the ss table replicas have agreed on the ss table file file file the fact that it's immutable means there the fact that it's immutable means there will be no further changes to it and as will be no further changes to it and as a result we're not going to have to a result we're not going to have to perform anti-entropy multiple times per perform anti-entropy multiple times per the ss table so as a result we can start the ss table so as a result we can start marking off all of these access tables marking off all of these access tables as repaired and then once they've been as repaired and then once they've been repaired we can go ahead and compact repaired we can go ahead and compact them in the background because we're them in the background because we're gonna get you know consistency between gonna get you know consistency between all of these replicas so instead what we all of these replicas so instead what we actually do is instead of sending an actually do is instead of sending an entire merkle tree that represents the entire merkle tree that represents the contents of every single ss table we do contents of every single ss table we do this incremental read repair where we're this incremental read repair where we're sending these smaller merkle trees over sending these smaller merkle trees over the network only representing the the network only representing the contents of these unrepaired ss tables contents of these unrepaired ss tables and as a result of that we've lowered and as a result of that we've lowered the overhead and the the network the overhead and the the network bandwidth and i o that needs to be used bandwidth and i o that needs to be used so this makes everything more efficient so this makes everything more efficient another thing that has to happen here as another thing that has to happen here as a result of having these two types of ss a result of having these two types of ss tables both unrepaired and repaired is tables both unrepaired and repaired is the compaction process between them kind the compaction process between them kind of needs to be separated of needs to be separated for example if we have a bunch of for example if we have a bunch of repaired ss tables and then we were to repaired ss tables and then we were to go ahead and kind of perform log go ahead and kind of perform log compaction and merge those in with a compaction and merge those in with a bunch of unre unrepaired ss tables well bunch of unre unrepaired ss tables well then we would have all these keys uh then we would have all these keys uh mixed into our previously repaired ss mixed into our previously repaired ss table that now perhaps may not be valid table that now perhaps may not be valid so now we're going to have to perform so now we're going to have to perform sorry not compaction but rather sorry not compaction but rather anti-entropy all over again on something anti-entropy all over again on something that we've already performed that we've already performed anti-entropy on so the way the cassandra anti-entropy on so the way the cassandra tries to mitigate this problem which is tries to mitigate this problem which is pretty smart is that it has two types of pretty smart is that it has two types of compaction basically you can compact compaction basically you can compact unrepaired esses tables with one another unrepaired esses tables with one another and you can compact repaired ss tables and you can compact repaired ss tables with one another and by doing this we with one another and by doing this we make sure that we're never performing make sure that we're never performing duplicate anti-entropy by making sure duplicate anti-entropy by making sure that basically once an access table has that basically once an access table has been repaired that data is never going been repaired that data is never going to be sent in a merkle tree over the to be sent in a merkle tree over the network again network again okay so in conclusion merkle trees are a okay so in conclusion merkle trees are a really cool way of detecting the really cool way of detecting the differences between two sets of records differences between two sets of records using a hash tree so obviously we can using a hash tree so obviously we can kind of detect those differences in kind of detect those differences in logarithmic time by basically just going logarithmic time by basically just going ahead and traversing down the tree once ahead and traversing down the tree once we've figured out those differences it we've figured out those differences it allows us to kind of send that minimal allows us to kind of send that minimal unit of you know file diffs over the unit of you know file diffs over the network and it greatly makes things a network and it greatly makes things a lot more efficient lot more efficient we can see these come up in a decent we can see these come up in a decent amount of system design problems such as amount of system design problems such as you know git revision control systems you know git revision control systems and in addition anti-entropy or and in addition anti-entropy or blockchain even though i'm not going to blockchain even though i'm not going to talk about the applications of merkle talk about the applications of merkle trees in blockchain at the moment i am trees in blockchain at the moment i am planning on doing some sort of planning on doing some sort of blockchain or d5 video in the future blockchain or d5 video in the future and hopefully we'll be able to kind of and hopefully we'll be able to kind of see how merkle trees come into play then see how merkle trees come into play then all right guys i hope this one was all right guys i hope this one was useful and i'm going to get myself to useful and i'm going to get myself to
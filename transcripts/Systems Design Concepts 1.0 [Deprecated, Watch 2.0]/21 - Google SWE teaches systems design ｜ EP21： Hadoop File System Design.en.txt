all right i'm back again this time in all right i'm back again this time in the morning because my roommate's not the morning because my roommate's not here if you guys can tell my voice is a here if you guys can tell my voice is a little messed up little messed up i guess i was playing with the boys a i guess i was playing with the boys a little too late last night and for some little too late last night and for some reason my knees are a little scratched reason my knees are a little scratched up too i don't really get that one but up too i don't really get that one but who knows so anyways today we're going who knows so anyways today we're going to talk about the architecture of hdfs to talk about the architecture of hdfs and figure out why that works the way it and figure out why that works the way it does and how they're able to achieve does and how they're able to achieve high throughput both on reads and writes high throughput both on reads and writes and then that'll allow us to segue and then that'll allow us to segue hopefully pretty easily into hbase and hopefully pretty easily into hbase and see kind of the good reasons to use see kind of the good reasons to use okay so hdfs and the design of it okay so hdfs and the design of it just to give a background i've mentioned just to give a background i've mentioned distributed file systems in the past distributed file systems in the past but basically they're a really important but basically they're a really important component of a ton of large-scale component of a ton of large-scale distributed systems distributed systems even though hdfs is probably the most even though hdfs is probably the most popular one it itself is based off the popular one it itself is based off the google file system which is a paper that google file system which is a paper that came out well over a decade ago now came out well over a decade ago now and because of the fact that hdfs can be and because of the fact that hdfs can be run on just normal desktop computers run on just normal desktop computers it's really popular obviously there's a it's really popular obviously there's a big wave to be able to just like spin up big wave to be able to just like spin up instances of things using things like instances of things using things like ect ec2 clusters or just amazon web ect ec2 clusters or just amazon web services in general services in general so even though hdfs is really useful for so even though hdfs is really useful for things like batch processing we've things like batch processing we've talked about this with mapreduce spark talked about this with mapreduce spark and tes and tes hdfs is really really good for a hdfs is really really good for a database building block so that you can database building block so that you can provide an extra layer to kind of provide an extra layer to kind of interact with the data on hdfs and then interact with the data on hdfs and then ultimately run a ton of computations on ultimately run a ton of computations on it it it okay so just to give an overview as you okay so just to give an overview as you can see on the right can see on the right you're going gonna see a ton of terms you're going gonna see a ton of terms that you don't know yet but by the end that you don't know yet but by the end of this video you will so generally of this video you will so generally speaking hadoop is designed to basically speaking hadoop is designed to basically be able to write a file once and then be able to write a file once and then you know from then on you can append or you know from then on you can append or truncate it but generally speaking just truncate it but generally speaking just read it many times over the way this is read it many times over the way this is done is by storing them in chunks across done is by storing them in chunks across a bunch of different data nodes and a bunch of different data nodes and typically these are around 64 128 typically these are around 64 128 megabyte chunks and the reason you do megabyte chunks and the reason you do that is to improve the parallelism of that is to improve the parallelism of both reading and writing big files both reading and writing big files oftentimes these files are gigabytes or oftentimes these files are gigabytes or maybe even terabytes in size and as a maybe even terabytes in size and as a result having to write them all result having to write them all sequentially would be terrible sequentially would be terrible and then finally in order to ensure and then finally in order to ensure availability and no loss data chunks are availability and no loss data chunks are obviously going to be replicated obviously going to be replicated okay so the first component of hdfs that okay so the first component of hdfs that we have to talk about is probably the we have to talk about is probably the most important one and that's going to most important one and that's going to be called the name node so the name node be called the name node so the name node generally speaking is where all of the generally speaking is where all of the metadata regarding files is stored so metadata regarding files is stored so that includes things like names but not that includes things like names but not only does it just hold the names of the only does it just hold the names of the files and perhaps even their files and perhaps even their subdirectories if it is a directory subdirectories if it is a directory but more importantly it has to keep but more importantly it has to keep track of all the chunks so basically all track of all the chunks so basically all of the data nodes where those chunks are of the data nodes where those chunks are located as well as their corresponding located as well as their corresponding version numbers like i said you can version numbers like i said you can append or truncate files and doing so append or truncate files and doing so would increment the version number of it would increment the version number of it okay so how does it do this well it okay so how does it do this well it keeps all of that metadata in memory all keeps all of that metadata in memory all of the changes to file system memory go of the changes to file system memory go to something to something sorry all of the changes to file system sorry all of the changes to file system metadata go to something called the edit metadata go to something called the edit log so the edit log is effectively just log so the edit log is effectively just a write ahead log for the name node a write ahead log for the name node because obviously if we had to you know because obviously if we had to you know like go ahead and change kind of disk like go ahead and change kind of disk state every single time or some state every single time or some persistent state of the entire state of persistent state of the entire state of the file system those writes would not the file system those writes would not be sequential and they would take longer be sequential and they would take longer so we do is we put them to a write-ahead so we do is we put them to a write-ahead log log log change the state in memory and then change the state in memory and then occasionally checkpoint that state on occasionally checkpoint that state on disk to something called an fs image disk to something called an fs image file file file and then if the name node ever crashes and then if the name node ever crashes and has to reboot the fs image and has to reboot the fs image checkpoint file in conjunction with checkpoint file in conjunction with whatever edit log rights come after that whatever edit log rights come after that checkpoint can be combined to create a checkpoint can be combined to create a new state for the name node new state for the name node okay in terms of continuing to talk okay in terms of continuing to talk about the name node about the name node it actually only keeps the location of it actually only keeps the location of all the chunks all the chunks only in memory so when it first boots only in memory so when it first boots what happens is the name node goes into what happens is the name node goes into safe mode and it's going to receive safe mode and it's going to receive something called a block report from something called a block report from each data node where the data node is each data node where the data node is going to tell it which chunks are held going to tell it which chunks are held on that data node the name node is then on that data node the name node is then going to compile all of this information going to compile all of this information construct that local state and say construct that local state and say here's where the chunks are located and here's where the chunks are located and say now say now it sees that only one replica is holding it sees that only one replica is holding a given chunk and you know the user is a given chunk and you know the user is say specified replication factor of say specified replication factor of three for that chunk it's going to say three for that chunk it's going to say okay we don't have enough replicas for okay we don't have enough replicas for this this this chunk in particular let's go ahead and chunk in particular let's go ahead and add some additional replicas for it so add some additional replicas for it so go ahead and replicate that chunk to two go ahead and replicate that chunk to two other nodes and that way we can reach other nodes and that way we can reach the replication threshold the same thing the replication threshold the same thing will occur if a name node assumes that a will occur if a name node assumes that a given data node is dead because it given data node is dead because it hasn't received any heartbeats from it hasn't received any heartbeats from it for a while for a while okay now let's talk about replication so okay now let's talk about replication so replication in hadoop is something replication in hadoop is something called rack aware and this is really called rack aware and this is really important because it allows for both important because it allows for both maximizing availability and throughput maximizing availability and throughput so we'll talk about that in a second so we'll talk about that in a second chunks are going to be replicated in a chunks are going to be replicated in a way that not only reduces latency for way that not only reduces latency for clients but also reduces the possibility clients but also reduces the possibility of all the replica nodes going down of all the replica nodes going down because of the fact that they're put in because of the fact that they're put in a different rack or data center a different rack or data center so for example for the default so for example for the default replication factor of three hadoop is replication factor of three hadoop is going to put one replica in the same going to put one replica in the same rack as the writer and then two replicas rack as the writer and then two replicas on the same remote random rack on the same remote random rack the reason they put them in the same the reason they put them in the same random rack is just to minimize network random rack is just to minimize network bandwidth you don't have to go to two bandwidth you don't have to go to two different racks and since we have kind different racks and since we have kind of a synchronous replication here where of a synchronous replication here where we wait for all of these rights to we wait for all of these rights to complete it's actually pretty important complete it's actually pretty important that all of these replicas complete that all of these replicas complete their right as fast as possible it's not their right as fast as possible it's not eventually consistent so i'll touch upon eventually consistent so i'll touch upon that in a second that in a second so so so how does replication actually work well how does replication actually work well there's something called pipelining there's something called pipelining basically the replicas are arranged into basically the replicas are arranged into some order and the data is pipelined some order and the data is pipelined from one replica in the next on a right from one replica in the next on a right or an append or a truncate rights are or an append or a truncate rights are only considered successful from the only considered successful from the client's point of view if all of the client's point of view if all of the replicas in this pipeline actually replicas in this pipeline actually acknowledge them so even though in acknowledge them so even though in theory this should lead to strong theory this should lead to strong consistency consistency consistency the issue is that say the first replica the issue is that say the first replica receives a right it's going to go ahead receives a right it's going to go ahead and commit that to itself so it's going and commit that to itself so it's going to perform the right and then the second to perform the right and then the second and third replicas don't ever actually and third replicas don't ever actually acknowledge the right meaning that you acknowledge the right meaning that you know they didn't perform them themselves know they didn't perform them themselves well the client is going to receive a well the client is going to receive a failure for its right however failure for its right however it's going to be the case that the right it's going to be the case that the right is still in one of the replicas so is still in one of the replicas so generally speaking when a client generally speaking when a client receives a failure on a right receives a failure on a right it needs to just keep retrying until it it needs to just keep retrying until it receives a success message receives a success message so as you can see the first replica is so as you can see the first replica is going to send that right to the second going to send that right to the second one which sends the right to the third one which sends the right to the third one which then sends the acknowledgement one which then sends the acknowledgement back and back again so once this whole back and back again so once this whole process is complete the client sees its process is complete the client sees its right as successful right as successful okay okay okay in terms of reading in hadoop basically in terms of reading in hadoop basically all that happens here is the client is all that happens here is the client is going to query the master or when i say going to query the master or when i say the master in this case i mean the name the master in this case i mean the name node to get a list of data nodes node to get a list of data nodes carrying the chunk that it wants it's carrying the chunk that it wants it's going to figure out which data node is going to figure out which data node is closest to it because like i said hadoop closest to it because like i said hadoop is aware of the rack that the nodes are is aware of the rack that the nodes are in and as a result of that it can say in and as a result of that it can say for a given client which one is probably for a given client which one is probably going to have minimal network latency going to have minimal network latency when communicating with it so you choose when communicating with it so you choose the best data node to read from the best data node to read from you're going to cache this result on the you're going to cache this result on the client in the case that you want to read client in the case that you want to read that file again because like i said that file again because like i said write once read many times and then the write once read many times and then the client's going to just go ahead and client's going to just go ahead and perform that read perform that read okay in terms of doing rights this is a okay in terms of doing rights this is a little bit more complex but i'll have a little bit more complex but i'll have a visualization after i walk through this visualization after i walk through this process process process so to append to a file so to append to a file go ahead and reach out to the name node go ahead and reach out to the name node see the data nodes where the chunk is see the data nodes where the chunk is located and then you have to pick located and then you have to pick something called a primary replica this something called a primary replica this is going to be the first replica in that is going to be the first replica in that replication pipeline replication pipeline if there's already a primary and the if there's already a primary and the lease for the primary is still valid lease for the primary is still valid because you know the lease basically because you know the lease basically says how long until there's no longer a says how long until there's no longer a primary perform the right to the primary primary perform the right to the primary replica let it go through the chain of replica let it go through the chain of replication otherwise we need to pick a replication otherwise we need to pick a primary replica how can we do this well primary replica how can we do this well we look at the data nodes that the chunk we look at the data nodes that the chunk is located on is located on and pick one of them with the most and pick one of them with the most up-to-date version of that chunk if it up-to-date version of that chunk if it doesn't exist we have a data loss doesn't exist we have a data loss problem and hopefully this never comes problem and hopefully this never comes up up up once the primary replica is determined once the primary replica is determined all the other replicas are considered all the other replicas are considered secondary we kind of establish that path secondary we kind of establish that path for the replication and then the client for the replication and then the client is going to make the right to the is going to make the right to the primary replica and you know hope to get primary replica and you know hope to get a success result a success result okay so to actually visualize this let's okay so to actually visualize this let's say i'm trying to write jordan's say i'm trying to write jordan's nudes.png we've got three replicas nudes.png we've got three replicas replica one two and three replica one two and three and the first thing we're going to do is and the first thing we're going to do is i'm going to i'm going to go ahead and ask the name node for what go ahead and ask the name node for what chunks are holding it and try and find chunks are holding it and try and find out what the leader was so i find out out what the leader was so i find out that the leader was replica 3 because as that the leader was replica 3 because as you can see it has version 23 of the you can see it has version 23 of the file there but that's since expired file there but that's since expired so what are we going to do we're going so what are we going to do we're going to randomly pick a replica with an to randomly pick a replica with an up-to-date version number as the leader up-to-date version number as the leader so now the leader is going to be r1 and so now the leader is going to be r1 and let's say we have a lease that expires let's say we have a lease that expires in an hour for it because replica 1 also in an hour for it because replica 1 also has version 23 it could have been one or has version 23 it could have been one or three here three here now all that's going to happen is we're now all that's going to happen is we're going to go ahead and contact replica going to go ahead and contact replica one which is the leftmost one in the one which is the leftmost one in the bottom there and send the right through bottom there and send the right through it and let it propagate propagate it and let it propagate propagate through the through the pipeline okay so what are some issues pipeline okay so what are some issues with hadoop well if you've been paying with hadoop well if you've been paying attention so far you may have noticed attention so far you may have noticed that i've only mentioned one name node that i've only mentioned one name node which is obviously a problem what which is obviously a problem what happens if the name node goes down well happens if the name node goes down well everything crashes so in the original everything crashes so in the original hadoop implementation hadoop implementation there was kind of like this hacky way of there was kind of like this hacky way of solving things called a secondary name solving things called a secondary name node which was basically just like a node which was basically just like a standby name node that went ahead and standby name node that went ahead and tried to take in all those changes but tried to take in all those changes but there's actually a better way of solving there's actually a better way of solving this and it uses coordination services this and it uses coordination services like i talked about in the last video so like i talked about in the last video so this is known as high availability hdfs this is known as high availability hdfs what do we actually do well keep in mind what do we actually do well keep in mind that the name node basically the the that the name node basically the the main persistence point of the name node main persistence point of the name node that you can use to derive the state of that you can use to derive the state of the name node is the edit log so the the name node is the edit log so the edit log is basically just going to keep edit log is basically just going to keep track of all that file metadata changes track of all that file metadata changes such as renaming files creating a new such as renaming files creating a new directory anything along those lines and directory anything along those lines and so instead of just keeping all of those so instead of just keeping all of those changes locally to the name node what changes locally to the name node what we're going to do is go ahead and use we're going to do is go ahead and use something like a few zookeeper nodes to something like a few zookeeper nodes to create a replicated log which represents create a replicated log which represents the edit log the edit log this in hadoop turns is known as the this in hadoop turns is known as the quorum journal manager so anyways after quorum journal manager so anyways after we have this replicated log we can now we have this replicated log we can now have a second instance of a name node have a second instance of a name node which you know we'll just call it the which you know we'll just call it the backup for now and all it's going to do backup for now and all it's going to do is read that replicated log and keep its is read that replicated log and keep its state up to date in the same way the state up to date in the same way the name node does name node does so by using uh this coordination service so by using uh this coordination service here we're actually able to keep a here we're actually able to keep a secondary name node relatively up to secondary name node relatively up to date so in the event that the first one date so in the event that the first one goes down goes down you know say we have you know say we have the coordination service also has a the coordination service also has a distributed lock distributed lock the first one goes down it will no the first one goes down it will no longer be holding that distributed lock longer be holding that distributed lock and then the second one can grab the and then the second one can grab the distributed lock to basically say i'm distributed lock to basically say i'm the leader now i am going to be the name the leader now i am going to be the name node node node okay so in conclusion um hdfs can okay so in conclusion um hdfs can provide really high read and write provide really high read and write throughput by using a rack-aware throughput by using a rack-aware replication schema replication schema and reading as well and reading as well this is super useful and in addition to this is super useful and in addition to that while the original hdfs kind of that while the original hdfs kind of design wasn't very fault tolerant the design wasn't very fault tolerant the fact that they've now added a fact that they've now added a coordination service to it is great for coordination service to it is great for kind of that leader failover of the name kind of that leader failover of the name node and allows high availability node and allows high availability obviously hdfs isn't perfect like i said obviously hdfs isn't perfect like i said it kind of aims for strong consistency it kind of aims for strong consistency but in reality you might have data but in reality you might have data inconsistencies and have to handle this inconsistencies and have to handle this in your application code in your application code but on the whole hdfs is really good for but on the whole hdfs is really good for storing data in conjunction with large storing data in conjunction with large scale compute we're going to see that scale compute we're going to see that plenty of databases are built on top of plenty of databases are built on top of hdfs in order to kind of provide a hdfs in order to kind of provide a better programming interface and allow better programming interface and allow for more complicated querying of it and for more complicated querying of it and that's what i'm going to be talking that's what i'm going to be talking about in my next video about in my next video so i hope this was useful guys and so i hope this was useful guys and welcome to all the new subscribers again welcome to all the new subscribers again
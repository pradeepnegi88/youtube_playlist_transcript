all right um i'm back again uh welcome all right um i'm back again uh welcome everyone everyone everyone i should just say yesterday was the one i should just say yesterday was the one month anniversary of this channel so i'm month anniversary of this channel so i'm pretty you know overwhelmed and excited pretty you know overwhelmed and excited that we already have 260 people that we already have 260 people subscribed so let's keep those numbers subscribed so let's keep those numbers up but uh today we're going to talk up but uh today we're going to talk about hbase it's kind of wild actually about hbase it's kind of wild actually because i was doing research for this because i was doing research for this topic in order to make a video about it topic in order to make a video about it and there's literally like probably 10 and there's literally like probably 10 blog posts which are just blatant blog posts which are just blatant plagiarisms of one another and just copy plagiarisms of one another and just copy the same like bullcrap information that the same like bullcrap information that go into absolutely zero depth so i go into absolutely zero depth so i actually had to dive into a little bit actually had to dive into a little bit here and then right as all that happens here and then right as all that happens it occurs to me that hbase is modeled it occurs to me that hbase is modeled after bigtable which there's literally a after bigtable which there's literally a ton of information on so i'm an idiot ton of information on so i'm an idiot but anyways uh let's get into this video but anyways uh let's get into this video all right so hbase what is it um well all right so hbase what is it um well the reason i'm talking about hbase in the reason i'm talking about hbase in the order that i'm talking about it in the order that i'm talking about it in is a couple things first of all it is a couple things first of all it allowed me to touch on hdfs in the prior allowed me to touch on hdfs in the prior video but more importantly hbase is video but more importantly hbase is another wide column nosql storage another wide column nosql storage database and the reason i want to talk database and the reason i want to talk about this now is the last database i about this now is the last database i talked about cassandra was also a wide talked about cassandra was also a wide column nosql database however there are column nosql database however there are some pretty serious differences between some pretty serious differences between the two even though on the surface level the two even though on the surface level they look like they serve similar they look like they serve similar functionalities functionalities functionalities so basically we're going to compare so basically we're going to compare those two but just to give an overview those two but just to give an overview even though hadoop or the the file even though hadoop or the the file system provides pretty high latency system provides pretty high latency rights and reads since they're all rights and reads since they're all straight from disk straight from disk and only allows basically sequential and only allows basically sequential appends and truncates we'll see how appends and truncates we'll see how hbase uses lsm trees and ss tables in hbase uses lsm trees and ss tables in order to achieve order to achieve better random read and write performance better random read and write performance in addition to lower latency reads and in addition to lower latency reads and writes performances writes performances okay so in terms of the data model hbase okay so in terms of the data model hbase is a wide column database so it's not is a wide column database so it's not identical to cassandra but it looks identical to cassandra but it looks pretty damn similar where each row has a pretty damn similar where each row has a single row key and then basically any single row key and then basically any column values that they want and there's column values that they want and there's this concept of like a column family as this concept of like a column family as well which is kind of like you know well which is kind of like you know combining uh you know two values to combining uh you know two values to create a column create a column the row key also can and probably should the row key also can and probably should be comprised of multiple parts so as be comprised of multiple parts so as opposed to having those clustering keys opposed to having those clustering keys in cassandra where that allows like an in cassandra where that allows like an internal sort order instead row keys internal sort order instead row keys just have you know maybe say multiple uh just have you know maybe say multiple uh delineations inside of them and you have delineations inside of them and you have to be pretty clever when developing this to be pretty clever when developing this row key to make sure that your data is row key to make sure that your data is sorted the way that you want it sorted the way that you want it okay so just as an image to kind of give okay so just as an image to kind of give you a sense of the architectural you a sense of the architectural overview overview overview we have this master server a region we have this master server a region server and then you know kind of the server and then you know kind of the replication that's going on in the replication that's going on in the background there's also a zookeeper background there's also a zookeeper instance which i'll mention a little bit instance which i'll mention a little bit as well but keep in mind that um you as well but keep in mind that um you know if you're ever talking about hbase know if you're ever talking about hbase it's very much modeled after google's it's very much modeled after google's bigtable which basically does the same bigtable which basically does the same exact thing as hbase however it's built exact thing as hbase however it's built on the google file system or now on the google file system or now actually the updated version of the actually the updated version of the google file system called colossus google file system called colossus so what is the master server well if you so what is the master server well if you remember the hdfs name node it's remember the hdfs name node it's actually very similar to that but it's actually very similar to that but it's kind of it's its own thing in hbase so kind of it's its own thing in hbase so it's going to store all the file it's going to store all the file metadata and you know all the operations metadata and you know all the operations that occur on the metadata that occur on the metadata such as like renaming files or anything such as like renaming files or anything like that and also the corresponding like that and also the corresponding chunks of where all the files are chunks of where all the files are located located located and so this is basically how hbase does and so this is basically how hbase does its partitioning its partitioning it's a range based partitioning on that it's a range based partitioning on that original row key original row key and if a given partition gets too big or and if a given partition gets too big or it has too much load it has too much load you can go ahead and split that up you can go ahead and split that up the one thing to note here is that means the one thing to note here is that means that hbase is not good for usage that hbase is not good for usage patterns patterns patterns say with like time series data where you say with like time series data where you know the time stamp is the row key know the time stamp is the row key because then every single write is going because then every single write is going to be going to one partition at a time to be going to one partition at a time and it's going to create hotspots so in and it's going to create hotspots so in that case you might want to use that case you might want to use something like a hash of a key something like a hash of a key okay okay okay in terms of reads and writes the client in terms of reads and writes the client is going to reach out to that hmaster is going to reach out to that hmaster server in order to figure out the server in order to figure out the location of the file and then you know location of the file and then you know it's going to lead it to a region server it's going to lead it to a region server which i'll touch upon next and then which i'll touch upon next and then presumably in order to ensure high presumably in order to ensure high availability the master server like the availability the master server like the master server and htfs can use that master server and htfs can use that zookeeper instance in order to keep a zookeeper instance in order to keep a eventually consistent write ahead log eventually consistent write ahead log and that way you can have a backup and that way you can have a backup master server reading from zookeeper in master server reading from zookeeper in order to eventually pull that right order to eventually pull that right ahead log and stay consistent with the ahead log and stay consistent with the master server master server okay what is a region server well a okay what is a region server well a region server before you guys kind of region server before you guys kind of get into the wrong mindset here a region get into the wrong mindset here a region server tends to be run on hdfs data node server tends to be run on hdfs data node servers so as opposed to being its own servers so as opposed to being its own dedicated server usually it's just a dedicated server usually it's just a second program that's being run on those second program that's being run on those data nodes in conjunction with whatever data nodes in conjunction with whatever the program is running that actually you the program is running that actually you know makes a computer a data node so know makes a computer a data node so what the region server does is it what the region server does is it occasionally sends heartbeats to occasionally sends heartbeats to zookeeper so basically says okay i'm zookeeper so basically says okay i'm alive you can still send rights to me or alive you can still send rights to me or reads to me reads to me additionally on that actual region additionally on that actual region server we're holding an in-memory lsm server we're holding an in-memory lsm tree style data structure and that's tree style data structure and that's where rights are going to first be set where rights are going to first be set so if you remember from literally my so if you remember from literally my first video on this channel we send first video on this channel we send rights to the lsm tree rights to the lsm tree this is a pretty efficient write this is a pretty efficient write structure because it means that they structure because it means that they first go to memory first go to memory and then reads once you want them first and then reads once you want them first go to the lsm tree and if the lsm tree go to the lsm tree and if the lsm tree gets too big it gets flushed to ss gets too big it gets flushed to ss tables and then if whatever the key it tables and then if whatever the key it is that you want to read is not in the is that you want to read is not in the lsm tree you look towards the ss tables lsm tree you look towards the ss tables and start sorting through those and and start sorting through those and there are a bunch of optimizations on there are a bunch of optimizations on that that you can do in order to kind of that that you can do in order to kind of speed up that read process like bloom speed up that read process like bloom filters filters filters and you know other types of caching and you know other types of caching so ss tables are actually going to be so ss tables are actually going to be stored in column oriented format so that stored in column oriented format so that means that as opposed to just having means that as opposed to just having you know the the key and then the entire you know the the key and then the entire value of the row what you have are these value of the row what you have are these sorted tables where sorted tables where it's all the values of the column in it's all the values of the column in turn so that column oriented storage is turn so that column oriented storage is something that i've mentioned in my data something that i've mentioned in my data data warehousing video but basically data warehousing video but basically what it means that you can achieve what it means that you can achieve really high throughput over an entire really high throughput over an entire table if you say you just want one table if you say you just want one column okay so now let's talk about replication okay so now let's talk about replication because this is kind of the whole point because this is kind of the whole point of being built on hdfs in addition to of being built on hdfs in addition to kind of the analytical processing that kind of the analytical processing that we'll talk about next we'll talk about next so like i said region nodes generally so like i said region nodes generally speaking are going to be putting rights speaking are going to be putting rights in this mem store and then once that mem in this mem store and then once that mem store gets too big they're going to store gets too big they're going to flush them to an h file which is just an flush them to an h file which is just an ss table ss table and since the region node is generally and since the region node is generally speaking on or near an hdfs data node speaking on or near an hdfs data node all it's going to do is once that hdfs all it's going to do is once that hdfs file so the h file is going to be file so the h file is going to be propagated to that data node the data propagated to that data node the data node is going to use the replication node is going to use the replication pipeline of hdfs that we talked about in pipeline of hdfs that we talked about in the previous video on this channel in the previous video on this channel in order to go ahead and reach the required order to go ahead and reach the required replication factor of that ss table so replication factor of that ss table so even though the region server is even though the region server is effectively going to be handling all effectively going to be handling all rights and reads for a given rights and reads for a given like partition of data it may like partition of data it may technically be communicating with one or technically be communicating with one or many of the replicas many of the replicas that is going to be holding that ss that is going to be holding that ss table for example if a given data node table for example if a given data node goes down the region server is probably goes down the region server is probably going to have to be talking to a going to have to be talking to a um oh and furthermore uh we should just um oh and furthermore uh we should just keep in mind the fact that technically keep in mind the fact that technically that is strong consistency because um that is strong consistency because um everything has to kind of succeed in everything has to kind of succeed in hdfs for hdfs for the replication to be considered the replication to be considered successful and the right to be successful and the right to be considered successful in the first place considered successful in the first place but okay in terms of the analytics but okay in terms of the analytics perspective what good does it do us perspective what good does it do us storing all of these ss tables on hdfs storing all of these ss tables on hdfs as opposed to just you know as opposed to just you know kind of having them on any normal kind of having them on any normal database database database well for starters the fact that they are well for starters the fact that they are column oriented storage means that we column oriented storage means that we can have really high read throughput can have really high read throughput when trying to read over all the values when trying to read over all the values on one partition and a column on one partition and a column and additionally built being built on and additionally built being built on hdfs means really good integration with hdfs means really good integration with mapreduce and dataflow engines like mapreduce and dataflow engines like spark and tes spark and tes one thing to note is that since hbase is one thing to note is that since hbase is not a sql database it doesn't have not a sql database it doesn't have structured data you can't actually structured data you can't actually perform native joins what you have to do perform native joins what you have to do is instead use something like a batch is instead use something like a batch process to perform joints and i talked process to perform joints and i talked about those in the batch process video about those in the batch process video basically you are trying to find data basically you are trying to find data associations perhaps even using two sets associations perhaps even using two sets of mappers and then combining those of mappers and then combining those together into one set of producers together into one set of producers okay so in conclusion even though hbase okay so in conclusion even though hbase looks really similar to cassandra on a looks really similar to cassandra on a surface level they're very very surface level they're very very different in terms of what you want to different in terms of what you want to be doing with them cassandra's really be doing with them cassandra's really nice for real-time transactions nice for real-time transactions processing so that means that basically processing so that means that basically anything user facing where you just want anything user facing where you just want like a really quick write so you can get like a really quick write so you can get them a really quick success message and them a really quick success message and then have that in the database to you then have that in the database to you know have that eventual consistency know have that eventual consistency between all of these cassandra instances between all of these cassandra instances that's good that's kind of like this that's good that's kind of like this leaderless replication strategy it's leaderless replication strategy it's very um based off dynamo hbase on the very um based off dynamo hbase on the other hand is a master other hand is a master you know master oriented database in you know master oriented database in terms of the replication strategy and so terms of the replication strategy and so even though they both use lsm trees even though they both use lsm trees hbase isn't going to be able to handle hbase isn't going to be able to handle those rights as fast those rights as fast however in terms of reads and analytics however in terms of reads and analytics processing processing processing you can achieve very high read you can achieve very high read throughput over a column which is super throughput over a column which is super useful if you want to run a huge batch useful if you want to run a huge batch job or a stream job job or a stream job this is great um for something called this is great um for something called being a data lake which is basically being a data lake which is basically saying i'm going to dump a ton of saying i'm going to dump a ton of unstructured data in kind of like a unstructured data in kind of like a database format into one place and then database format into one place and then eventually run analytics later it's just eventually run analytics later it's just that the advantage of doing this with that the advantage of doing this with hbase over hdfs itself is that you can hbase over hdfs itself is that you can get get get nice read and write performance for both nice read and write performance for both random reads and writes in the event random reads and writes in the event that you do have to do some general like that you do have to do some general like you know transaction processing so it's you know transaction processing so it's basically a data lake that allows you to basically a data lake that allows you to do some transaction processing but keep do some transaction processing but keep in mind that data lakes also are not in mind that data lakes also are not exactly the same as data warehouses exactly the same as data warehouses which are specifically for structured which are specifically for structured data with a ton of data associations if data with a ton of data associations if you recall data warehouses use that you recall data warehouses use that whole like stars and snowflake schema whole like stars and snowflake schema where you have a huge fact table which where you have a huge fact table which is very structured and then a ton of is very structured and then a ton of dimension and sub-dimension tables which dimension and sub-dimension tables which reference the fact table or actually the reference the fact table or actually the fact table is going to reference those fact table is going to reference those dimension tables dimension tables but the point is if you just want to be but the point is if you just want to be able to run sql queries here hbase is able to run sql queries here hbase is not the thing but if you want to be able not the thing but if you want to be able to dump in a ton of data and then to dump in a ton of data and then eventually run you know a ton of eventually run you know a ton of expensive batch jobs on it then hbase is expensive batch jobs on it then hbase is really nice for you really nice for you so yeah all right guys i think uh next so yeah all right guys i think uh next i'm going to start moving into key value i'm going to start moving into key value stores so i can talk about things like stores so i can talk about things like ryak redis memcached and just caching in ryak redis memcached and just caching in
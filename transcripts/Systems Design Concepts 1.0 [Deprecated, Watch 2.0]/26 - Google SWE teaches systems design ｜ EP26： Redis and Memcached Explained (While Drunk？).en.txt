hello everyone it is uh i think 1 39 a.m hello everyone it is uh i think 1 39 a.m on a saturday morning if you can't tell on a saturday morning if you can't tell i am actively hammered i am actively hammered why am i doing this right now it's why am i doing this right now it's because my roommate is at his because my roommate is at his girlfriend's house and uh i have the girlfriend's house and uh i have the free house to record so you know what free house to record so you know what screw it let's get this done um screw it let's get this done um i'm gonna talk about redis and memcache i'm gonna talk about redis and memcache today and assuming my brain can support today and assuming my brain can support it because i guess the balmer curve it because i guess the balmer curve tells me that i can have a couple drinks tells me that i can have a couple drinks and and you know talk about code then uh and and you know talk about code then uh hopefully this video will be coherent hopefully this video will be coherent that being said i'm also about eight or that being said i'm also about eight or nine drinks in so i'm a little bit past nine drinks in so i'm a little bit past the balmer curve but the balmer curve but you know what i can still talk about you know what i can still talk about computer science so let's get it done computer science so let's get it done hey yeah so this is me from the next hey yeah so this is me from the next morning uh morning uh i actually did record that video and uh i actually did record that video and uh i looked back on it and i was like damn i looked back on it and i was like damn i am slurring a lot of words right now i am slurring a lot of words right now so so so um we're gonna do a re-recording and uh um we're gonna do a re-recording and uh hopefully i'll explain a little bit more hopefully i'll explain a little bit more concisely this time so let's do memcache concisely this time so let's do memcache and redis and redis so basically in terms of what memcache so basically in terms of what memcache and redis are they're both solutions and redis are they're both solutions that are typically used for caching in that are typically used for caching in large-scale distributed systems the large-scale distributed systems the reason for this is that they generally reason for this is that they generally speaking store their data in ram random speaking store their data in ram random access memory and that allows you to access memory and that allows you to basically build something called a basically build something called a distributed hash map which can access distributed hash map which can access keys and set keys at of one time which keys and set keys at of one time which is great because obviously databases on is great because obviously databases on disk can't do that disk can't do that as we can see though there are pretty as we can see though there are pretty significant differences between the two significant differences between the two services themselves services themselves mainly in the sense that mainly in the sense that memcache is basically like a subset of memcache is basically like a subset of redis but you know sometimes it's good redis but you know sometimes it's good to have a more limited feature set to have a more limited feature set because you can build out more because you can build out more so so so what is memcache like i said memcache what is memcache like i said memcache allows you to build a distributed hash allows you to build a distributed hash map amongst a bunch of nodes however the map amongst a bunch of nodes however the nodes basically don't know about one nodes basically don't know about one another so generally speaking you're another so generally speaking you're actually kind of just using this client actually kind of just using this client library to go ahead and wire all those library to go ahead and wire all those requests to the proper node how do we requests to the proper node how do we wire each request to the right node well wire each request to the right node well generally speaking we're using generally speaking we're using consistent hashing so basically you give consistent hashing so basically you give all of your you know application servers all of your you know application servers that are going to be using memcache that are going to be using memcache instances a list of all the nodes that instances a list of all the nodes that are running memcached and then that will are running memcached and then that will allow them to create a consistent allow them to create a consistent hashing ring hashing ring additionally you have an lru cache so if additionally you have an lru cache so if each memcache instance gets too big or each memcache instance gets too big or basically there's too much data in there basically there's too much data in there and you need to evict something in order and you need to evict something in order to make room for a new element you're to make room for a new element you're using the lru algorithm and then you using the lru algorithm and then you know a couple of other features that are know a couple of other features that are built in are built in are you know basically just like compare and you know basically just like compare and set um generally speaking there's not set um generally speaking there's not really any failure handling for memcache really any failure handling for memcache they don't have any built-in like they don't have any built-in like replication or availability measures so replication or availability measures so um what actually a company like facebook um what actually a company like facebook did and i'll link this lecture did and i'll link this lecture from like mit basically in the video from like mit basically in the video description is that they use something description is that they use something called like a gutter rattus instance called like a gutter rattus instance where every time um a red is not a redis where every time um a red is not a redis instance um a memcache instance failed instance um a memcache instance failed they basically go ahead and throw a new they basically go ahead and throw a new memcache instance in there to just take memcache instance in there to just take its place and then eventually it'll get its place and then eventually it'll get repopulated over time but there's no repopulated over time but there's no like you know copying over of the data like you know copying over of the data from one instance to another which is from one instance to another which is interesting interesting interesting okay so what is redis well at least on a okay so what is redis well at least on a single node single node redis is a modification of memcache that redis is a modification of memcache that basically has the following features so basically has the following features so obviously it's an lru distributed hash obviously it's an lru distributed hash map but it's got some other stuff too so map but it's got some other stuff too so instead of just being a hash map from instead of just being a hash map from strings to strings you can actually have strings to strings you can actually have other data types as values in the hash other data types as values in the hash maps so those can be sets maps so those can be sets you know strings but with atomic you know strings but with atomic operations like appending to the string operations like appending to the string maps and lists and you can also even maps and lists and you can also even have transactions so if you want to make have transactions so if you want to make multiple writes to a single node and multiple writes to a single node and make sure that those are make sure that those are executed both serially and also as an executed both serially and also as an atomic unit you can do that you can also atomic unit you can do that you can also even make range queries on a single even make range queries on a single partition and there's also a way to kind partition and there's also a way to kind of of of go ahead and hash keys using only part go ahead and hash keys using only part of the key of the key doing something called the hashtag which doing something called the hashtag which allows you to kind of have some control allows you to kind of have some control over where each key is going to be sent over where each key is going to be sent to in a partitioning scheme to in a partitioning scheme then finally there's also a concept of then finally there's also a concept of actual disk persistence which makes actual disk persistence which makes redis a little bit more viable as an redis a little bit more viable as an actual database for your application as actual database for your application as well and you can do disk persistence via well and you can do disk persistence via checkpointing which is probably faster checkpointing which is probably faster but obviously comes with the cost of but obviously comes with the cost of losing some rights if you don't losing some rights if you don't checkpoint everything or you can use a checkpoint everything or you can use a write ahead log write ahead log where basically every single write is where basically every single write is going to the disk before it's written in going to the disk before it's written in memory and this obviously comes at the memory and this obviously comes at the cost of slower writes cost of slower writes in terms of redis cluster redis cluster in terms of redis cluster redis cluster is basically what redis calls is basically what redis calls you know running a bunch of radis nodes you know running a bunch of radis nodes in a distributed manner so basically the in a distributed manner so basically the point here is that this provides both point here is that this provides both high availability and consistency so how high availability and consistency so how do they provide high ability well unlike do they provide high ability well unlike memcache they actually support memcache they actually support replication out of the box so that uses replication out of the box so that uses single leader replication with an single leader replication with an automatic failover automatic failover the thing with single leader replication the thing with single leader replication here is that some rights can be lost so here is that some rights can be lost so if the leader has some rights and it's if the leader has some rights and it's replicating them asynchronously just replicating them asynchronously just some of its replicas and then the leader some of its replicas and then the leader goes ahead and fails before all of those goes ahead and fails before all of those rights get properly sent out to all of rights get properly sent out to all of the followers then those rights are the followers then those rights are probably just going to be lost um so how probably just going to be lost um so how do we actually do a failover well do we actually do a failover well there's a gossip protocol between all there's a gossip protocol between all the nodes where you're basically sharing the nodes where you're basically sharing heartbeats that convey the you know kind heartbeats that convey the you know kind of the state of the node as well as the of the state of the node as well as the partitions that it's holding and then uh partitions that it's holding and then uh in order now to basically you know put a in order now to basically you know put a new replica to the master what needs to new replica to the master what needs to happen is that a quorum of masternodes happen is that a quorum of masternodes amongst all partitions need to basically amongst all partitions need to basically go ahead and agree that this new go ahead and agree that this new follower is going to become the leader follower is going to become the leader and they use an epoch number to do that and they use an epoch number to do that and so this basically allows us to and so this basically allows us to prevent split brain because obviously a prevent split brain because obviously a quorum of nodes can't make a quorum of nodes can't make a conflicting decision for a single epoch conflicting decision for a single epoch so that's pretty smart there how they so that's pretty smart there how they make sure to prevent split brain and make sure to prevent split brain and then finally then finally in order to kind of do partitioning in order to kind of do partitioning unlike a bunch of other database unlike a bunch of other database solutions which most of which we've kind solutions which most of which we've kind of just seen using of just seen using consistent hashing so far this actually consistent hashing so far this actually uses the fixed number of partition uses the fixed number of partition solution that i discussed from ddia solution that i discussed from ddia where there are exactly 16 384 fixed where there are exactly 16 384 fixed partitions with fixed ranges and then partitions with fixed ranges and then your job is basically to make sure that your job is basically to make sure that you know you're putting the right number you know you're putting the right number of partitions on each node such that of partitions on each node such that there aren't too many hotspots and there aren't too many hotspots and obviously you know due to the fact that obviously you know due to the fact that there's replication if there are certain there's replication if there are certain hotkeys hopefully the load won't be too hotkeys hopefully the load won't be too great on them because you'll actually be great on them because you'll actually be able to serve a lot of requests from able to serve a lot of requests from those replicas as opposed to just having those replicas as opposed to just having to serve them all from one instance to serve them all from one instance okay so in terms of a comparison between okay so in terms of a comparison between memcached and redis memcached and redis as you can see um redis is basically as you can see um redis is basically just memcached with a bunch of just memcached with a bunch of additional features built in out of the additional features built in out of the box so why would you ever not want to box so why would you ever not want to use it well use it well by virtue of having all these features by virtue of having all these features built in it makes it harder to kind of built in it makes it harder to kind of diverge from that design pattern so diverge from that design pattern so let's say you needed strongly consistent let's say you needed strongly consistent data maybe you'd be better off just data maybe you'd be better off just using memcache and then kind of building using memcache and then kind of building out your own system using something with out your own system using something with like a coordination service in order to like a coordination service in order to ensure strong consistency ensure strong consistency maybe you want alternate replication maybe you want alternate replication patterns like a leaderless replication patterns like a leaderless replication schema which you know kind of resembles schema which you know kind of resembles a dynamo database then perhaps you'd be a dynamo database then perhaps you'd be better off using memcache and then you better off using memcache and then you know kind of implementing that yourself know kind of implementing that yourself or maybe even you want to use just a or maybe even you want to use just a coordination service in general for all coordination service in general for all of that partition management as opposed of that partition management as opposed to just using a gossip protocol to just using a gossip protocol because gossip protocol even though it because gossip protocol even though it does generally work you know it's just a does generally work you know it's just a little bit harder to reason about little bit harder to reason about sometimes sometimes sometimes you know you can do that too you know you can do that too so in conclusion both redis and memcache so in conclusion both redis and memcache are super useful systems for are super useful systems for implementing caching and in an interview implementing caching and in an interview i imagine it probably won't really come i imagine it probably won't really come up if you just said yeah i'm going to up if you just said yeah i'm going to use redis or memcache in order to use redis or memcache in order to implement my cache but i think it's implement my cache but i think it's important to know the subtle differences important to know the subtle differences between the two of them because that's between the two of them because that's kind of what this channel is all about kind of what this channel is all about is being able to you know hear all of is being able to you know hear all of the names of these different technology the names of these different technology services and basically go and say okay services and basically go and say okay well actually now i understand why one well actually now i understand why one is different from the other is different from the other obviously caching is hugely beneficial obviously caching is hugely beneficial for any large scale distributed system for any large scale distributed system and as a result of that you should and as a result of that you should basically be using it whenever you can basically be using it whenever you can until it pretty much prices you out so until it pretty much prices you out so you know as long as you can afford it you know as long as you can afford it you should be using caching you should be using caching that video i mentioned about caching at that video i mentioned about caching at facebook basically says that pretty much facebook basically says that pretty much 99 of their read requests are handled by 99 of their read requests are handled by their cash in order to take load off their cash in order to take load off their databases and allow them to keep their databases and allow them to keep operating so okay i hope this video was operating so okay i hope this video was useful sorry i couldn't post the drunk useful sorry i couldn't post the drunk one but it was probably double as long one but it was probably double as long and very incoherent but uh have a good and very incoherent but uh have a good
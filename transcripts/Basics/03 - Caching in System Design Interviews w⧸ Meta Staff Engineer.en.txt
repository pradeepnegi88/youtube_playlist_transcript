Hey everyone, welcome back to the Hey everyone, welcome back to the channel. For those of you who are new, channel. For those of you who are new, I'm Evan. I'm a former Meta staff I'm Evan. I'm a former Meta staff engineer and the current co-founder of engineer and the current co-founder of hellointerview.com. If you're preparing hellointerview.com. If you're preparing for software interviews, head over to for software interviews, head over to hellointerview. We got everything you hellointerview. We got everything you need, overwhelming majority of which is need, overwhelming majority of which is free. free. free. Uh in this particular video though, Uh in this particular video though, we're going to be covering the basics of we're going to be covering the basics of caching, and specifically in the context caching, and specifically in the context of system design interviews. And so, of system design interviews. And so, we'll look at where caching fits into a we'll look at where caching fits into a system. We'll talk about the most common system. We'll talk about the most common caching architectures, typical eviction caching architectures, typical eviction policies you want to understand. We're policies you want to understand. We're also going to talk about the kinds of also going to talk about the kinds of issues that show up when you introduce issues that show up when you introduce caching. These are things like caching. These are things like consistency, stampedes, hot keys, the consistency, stampedes, hot keys, the things that interviewers really love to things that interviewers really love to dig into and that you'll want to be dig into and that you'll want to be prepared for. And then finally, we'll go prepared for. And then finally, we'll go over how to talk about caching in an over how to talk about caching in an interview, when to bring it up, how much interview, when to bring it up, how much depth you should go you should go into. depth you should go you should go into. And the things that interviewers are are And the things that interviewers are are usually looking for. So, this should be usually looking for. So, this should be fun. Without further ado, let's get fun. Without further ado, let's get after it. Chances are you already have a after it. Chances are you already have a pretty decent idea of what caching is. pretty decent idea of what caching is. Uh but let's go ahead and start from Uh but let's go ahead and start from just the very basics really quickly to just the very basics really quickly to make sure that everybody's on the same make sure that everybody's on the same page. page. page. And so, a cache is quite simply just a And so, a cache is quite simply just a temporary storage that keeps recently temporary storage that keeps recently used data handy and close by so that you used data handy and close by so that you can fetch it faster the next time. can fetch it faster the next time. So, to see why this matters, let's take So, to see why this matters, let's take a look at an example here. And consider a look at an example here. And consider the difference in speed between where the difference in speed between where data usually lives in a database and data usually lives in a database and where it can live in a cache. where it can live in a cache. And so, accessing data from disk, like And so, accessing data from disk, like an SSD in the case of a database, takes an SSD in the case of a database, takes about a millisecond on average. about a millisecond on average. Accessing data from memory or RAM on the Accessing data from memory or RAM on the other hand takes about 100 nanoseconds. other hand takes about 100 nanoseconds. This is roughly 10,000 times faster. This is roughly 10,000 times faster. Now, that gap adds up really quickly Now, that gap adds up really quickly when you're serving thousands of when you're serving thousands of requests per second. And caching takes requests per second. And caching takes advantage of that big difference. It advantage of that big difference. It keeps copies of frequently used data in keeps copies of frequently used data in a faster layer, often times memory, but a faster layer, often times memory, but not always. We'll talk about that later not always. We'll talk about that later on. So, that systems don't have to reach on. So, that systems don't have to reach all the way back into that slower source all the way back into that slower source every single time. every single time. So, you have the basic idea. Caching So, you have the basic idea. Caching trades a bit of storage and complexity trades a bit of storage and complexity for speed. for speed. Now, the next question is, where should Now, the next question is, where should you cache your data? And there's a few you cache your data? And there's a few different layers in your system where different layers in your system where caching can live, each of which have caching can live, each of which have their own set of trade-offs, of course. their own set of trade-offs, of course. And so, the first one, and by far the And so, the first one, and by far the most common in system design interviews, most common in system design interviews, is called external caching. is called external caching. This is where you introduce a dedicated This is where you introduce a dedicated caching service like Redis or Memcached. caching service like Redis or Memcached. It runs, importantly, on its own server It runs, importantly, on its own server and manages its own memory, and it's and manages its own memory, and it's totally separate from your application totally separate from your application or your database, right? It's your own or your database, right? It's your own component in the system here. component in the system here. And so, when your application needs And so, when your application needs data, it first checks the cache. If the data, it first checks the cache. If the data is found there, that's a cache hit, data is found there, that's a cache hit, and it returns your data instantly, and it returns your data instantly, super fast. If it's not there, we call super fast. If it's not there, we call that a cache miss, and it has to fall that a cache miss, and it has to fall back to the database, fetch the data, back to the database, fetch the data, and it stores a copy of that data back and it stores a copy of that data back in the cache, and also returns it back in the cache, and also returns it back to the client, right? to the client, right? Uh now, the nice thing here is that in a Uh now, the nice thing here is that in a scaled system, which might have multiple scaled system, which might have multiple application servers like we represented application servers like we represented here, all of these different application here, all of these different application servers can share that same external servers can share that same external cache. cache. cache. This way, once one server has fetched This way, once one server has fetched and cached the data, the others can all and cached the data, the others can all reuse it instantly instead of all reuse it instantly instead of all hitting the database separately, right? hitting the database separately, right? Because this is a global view. It's a Because this is a global view. It's a global cache that's shared by all of the global cache that's shared by all of the Now, the second option Now, the second option is what we call in-process caching. is what we call in-process caching. And this lets you skip the complexity of And this lets you skip the complexity of adding something like Redis entirely. Uh adding something like Redis entirely. Uh it's often overlooked, to be honest, and it's often overlooked, to be honest, and I think in real system design outside of I think in real system design outside of the context of interviews, it's probably the context of interviews, it's probably overlooked more than it should be, but overlooked more than it should be, but it can be incredibly effective. it can be incredibly effective. Now, the key thing to note here is that Now, the key thing to note here is that modern application servers usually run modern application servers usually run on really big machines nowadays that on really big machines nowadays that have plenty of memory. And you can have plenty of memory. And you can actually use some of that memory in actually use some of that memory in order to cache data right inside of the order to cache data right inside of the process. process. process. Uh this is important because it's by far Uh this is important because it's by far the fastest kind of caching, right? You the fastest kind of caching, right? You don't have to go and have an expensive don't have to go and have an expensive network hop here to hit some external network hop here to hit some external cache anymore. The data is already cache anymore. The data is already sitting in the same memory space as your sitting in the same memory space as your application, right? So, you don't have application, right? So, you don't have that expensive network hop, it's already that expensive network hop, it's already right there where you want it. But of right there where you want it. But of course, this this comes with trade-offs. course, this this comes with trade-offs. And the main trade-off is that unlike And the main trade-off is that unlike the external cache, each application the external cache, each application server has its own in-process memory. server has its own in-process memory. And so, this means that if one server And so, this means that if one server caches something, the others won't see caches something, the others won't see it. So, you can end up with these it. So, you can end up with these inconsistencies or even wasted memory if inconsistencies or even wasted memory if you're not too careful. you're not too careful. And so, the chances are, at least in the And so, the chances are, at least in the context of a system design interview, context of a system design interview, that you probably won't need to bring that you probably won't need to bring this up unless you're talking about a this up unless you're talking about a low-level optimization or have a use low-level optimization or have a use case where ultra low latency matters. case where ultra low latency matters. For example, if you need to cache config For example, if you need to cache config data or small lookup tables that every data or small lookup tables that every single request depends upon, then single request depends upon, then caching within the application server caching within the application server makes sense, but your default should makes sense, but your default should remain as external caching. Next up, we remain as external caching. Next up, we have what are called CDNs or content have what are called CDNs or content delivery networks. delivery networks. And so, a CDN is a geographically And so, a CDN is a geographically distributed network of servers that can distributed network of servers that can cache content closer to your users. And cache content closer to your users. And I know that that sounds fancier really I know that that sounds fancier really than it is. It's just putting servers than it is. It's just putting servers around the world so that they're close around the world so that they're close to the people who need them. Here, we're to the people who need them. Here, we're not optimizing for the difference not optimizing for the difference between memory and disk speeds like we between memory and disk speeds like we were before. Instead, we're optimizing were before. Instead, we're optimizing for network latency. for network latency. So, without a CDN, every single request So, without a CDN, every single request has to travel all the way to your origin has to travel all the way to your origin server. server. server. If you had a server in Virginia, like If you had a server in Virginia, like was the case here, think that this is S3 was the case here, think that this is S3 or some blob storage, right? And your or some blob storage, right? And your user is all the way over here in user is all the way over here in Australia, then this round trip could Australia, then this round trip could take 300 to 350 milliseconds. That's take 300 to 350 milliseconds. That's huge, especially when we're talking huge, especially when we're talking about disk access being just 1 about disk access being just 1 millisecond. millisecond. millisecond. With a CDN on the other hand, that same With a CDN on the other hand, that same request might hit an edge server that's request might hit an edge server that's just a few miles away, which may be 20 just a few miles away, which may be 20 to 40 milliseconds round trip, which is to 40 milliseconds round trip, which is a huge difference. a huge difference. Now, the way that it works is that when Now, the way that it works is that when a user requests something like an image, a user requests something like an image, that request goes to the nearest CDN that request goes to the nearest CDN edge server like we just said. And if edge server like we just said. And if that image is already cached there, it's that image is already cached there, it's returned immediately. Perfect, that's returned immediately. Perfect, that's the happy case, that's the cache hit. the happy case, that's the cache hit. If not, if it's a cache miss, then the If not, if it's a cache miss, then the CDN itself goes and fetches that media CDN itself goes and fetches that media or whatever you're looking for from uh or whatever you're looking for from uh your origin server, like S3 or whatever your origin server, like S3 or whatever blob storage you have, and then it's blob storage you have, and then it's going to return that back to the CDN. going to return that back to the CDN. The CDN will then cache it so that it The CDN will then cache it so that it has it for next time and return it back has it for next time and return it back to the client. to the client. Now, modern CDNs, and this is something Now, modern CDNs, and this is something people often times forget, they can do a people often times forget, they can do a lot more than just cache static media, lot more than just cache static media, which is what they're most known for. which is what they're most known for. They can also cache public API They can also cache public API responses, of course HTML pages, run responses, of course HTML pages, run edge logic even to personalize content. edge logic even to personalize content. But as far as a system design interview But as far as a system design interview goes, the most common and the most goes, the most common and the most impactful use case to bring up is on impactful use case to bring up is on media delivery. So, things like images, media delivery. So, things like images, videos, or static assets, files, etc. videos, or static assets, files, etc. that you want to load really quickly that you want to load really quickly around the world. And so, if you have around the world. And so, if you have global users who are accessing media global users who are accessing media regularly, then a CDN is probably a regularly, then a CDN is probably a great fit for you. great fit for you. Lastly, we have what's called Lastly, we have what's called client-side caching. client-side caching. So, this is when data is stored directly So, this is when data is stored directly on the user's device, either in the on the user's device, either in the browser or the app, which avoids browser or the app, which avoids unnecessary network costs. Um unnecessary network costs. Um So, in web apps, that might be something So, in web apps, that might be something like the HTTP cache or local storage like the HTTP cache or local storage within the browser itself. For mobile within the browser itself. For mobile apps, this could be data kept in memory apps, this could be data kept in memory or even written to the local disk on or even written to the local disk on device. device. device. And it's nice cuz it's obviously super And it's nice cuz it's obviously super fast. Data never leaves the device. But fast. Data never leaves the device. But it comes with the downside, of course, it comes with the downside, of course, and that's that you have less control and that's that you have less control over it. Data can go stale, validation, over it. Data can go stale, validation, freshness, all of that is a bit harder. freshness, all of that is a bit harder. And so, when it comes to your interview, And so, when it comes to your interview, you'll see this come up a lot less you'll see this come up a lot less often. Um usually, it's only relevant often. Um usually, it's only relevant when your system involves some offline when your system involves some offline functionality or client-heavy workloads. functionality or client-heavy workloads. For example, if a browser's reusing For example, if a browser's reusing images it already downloaded, or an app images it already downloaded, or an app like Strava caching your run data like Strava caching your run data locally while you're offline and then locally while you're offline and then syncing it once you're reconnected. We syncing it once you're reconnected. We have a problem breakdown where we do have a problem breakdown where we do exactly that. But for all intents and exactly that. But for all intents and purposes, this is the least important purposes, this is the least important for you to know as it pertains to your for you to know as it pertains to your Really quick, before we continue, let me Really quick, before we continue, let me tell you a little bit about the most tell you a little bit about the most popular feature on hellointerview.com, popular feature on hellointerview.com, guided practice. guided practice. It's an interactive tool that lets you It's an interactive tool that lets you practice system design interview practice system design interview step-by-step using that hello interview step-by-step using that hello interview delivery framework. You're going to walk delivery framework. You're going to walk through everything from the through everything from the non-functional requirements to the core non-functional requirements to the core entities, API routes, all the way entities, API routes, all the way through to your high-level design and through to your high-level design and deep dives, drawing on the whiteboard deep dives, drawing on the whiteboard and narrating your response. All while and narrating your response. All while getting real-time feedback on what getting real-time feedback on what you're doing well and where you can you're doing well and where you can improve by a model that Stefan and I improve by a model that Stefan and I have spent hundreds of hours tuning. have spent hundreds of hours tuning. We expanded the library to 25 of the We expanded the library to 25 of the most common system design interview most common system design interview questions now, and we're constantly questions now, and we're constantly adding more. So, candidates absolutely adding more. So, candidates absolutely love this feature. I think you will, love this feature. I think you will, too. Check it out at hellointerview.com. too. Check it out at hellointerview.com. Link will be in the description. Now Link will be in the description. Now that we've covered where you can cache that we've covered where you can cache your data, let's talk a little about your data, let's talk a little about cache architectures. cache architectures. Cache architecture is just defined how Cache architecture is just defined how your application actually interacts with your application actually interacts with the cache. And so, specifically, this is the cache. And so, specifically, this is defining the order in which reads and defining the order in which reads and writes happen between both your cache writes happen between both your cache and your database and your application and your database and your application servers. servers. servers. And so, the most common caching pattern And so, the most common caching pattern by far, and this is the one that you by far, and this is the one that you should default to in your interview, should default to in your interview, uh is cache-aside. uh is cache-aside. And so, cache-aside, what happens is And so, cache-aside, what happens is that the application checks the cache that the application checks the cache first. This is the one we brought up first. This is the one we brought up earlier, right? If the data is there, it earlier, right? If the data is there, it returns it, that's a cache hit, great. returns it, that's a cache hit, great. If not, it goes and fetches it from the If not, it goes and fetches it from the database, and then it stores it in the database, and then it stores it in the cache, and returns it to the user. cache, and returns it to the user. And so, cache-aside is great because it And so, cache-aside is great because it keeps the cache really lean. You only keeps the cache really lean. You only cache data when you actually need it, cache data when you actually need it, right? We're only caching the things right? We're only caching the things that users actually requested. If a user that users actually requested. If a user never requested anything, it never made never requested anything, it never made it into the cache. it into the cache. But the downside is that a cache miss is But the downside is that a cache miss is going to add that latency since the going to add that latency since the request then has to go hit the database, request then has to go hit the database, request the data, the more expensive request the data, the more expensive operation, store it in the cache, and operation, store it in the cache, and return it back to the user, right? return it back to the user, right? So, So, So, if you're only going to remember one if you're only going to remember one caching architecture from this video, caching architecture from this video, make it cache-aside. This is the one make it cache-aside. This is the one you're probably going to use in the you're probably going to use in the interview, uh but it's important for us interview, uh but it's important for us to talk about some of the other ones as to talk about some of the other ones as well. Starting with write-through well. Starting with write-through caching. caching. caching. So, with write-through, the application So, with write-through, the application actually writes directly to the cache actually writes directly to the cache first. And then the cache synchronously first. And then the cache synchronously writes that data to the database before writes that data to the database before it returns to the user. And so, the it returns to the user. And so, the write isn't considered complete until write isn't considered complete until both the cache and the database have both the cache and the database have been updated. been updated. In practice, this means that you need a In practice, this means that you need a caching library or a framework for this caching library or a framework for this part right here that supports this part right here that supports this write-through behavior. Something that write-through behavior. Something that knows to trigger your database write knows to trigger your database write logic automatically. logic automatically. Because tools like Redis or Memcached, Because tools like Redis or Memcached, they don't natively support this. And they don't natively support this. And so, you need either handle this logic so, you need either handle this logic yourself in your application code by yourself in your application code by writing to both at the same time, and writing to both at the same time, and there's all sorts of complications that there's all sorts of complications that come from that, or you'll want to use a come from that, or you'll want to use a library, something like Spring Cache or library, something like Spring Cache or Hazelcast, which can automatically do Hazelcast, which can automatically do that write-through for you. that write-through for you. Um but the trade-off here becomes pretty Um but the trade-off here becomes pretty obvious, and that's that you have slower obvious, and that's that you have slower writes cuz you need to wait for both of writes cuz you need to wait for both of these to happen. these to happen. Um and then also, you can pollute your Um and then also, you can pollute your cache with all this data that may never cache with all this data that may never be read again. If we write everything to be read again. If we write everything to our cache, then it might be data that our cache, then it might be data that nobody ever actually accesses, and we're nobody ever actually accesses, and we're just bloating our cache for no reason, just bloating our cache for no reason, unlike cache-aside that we talked about unlike cache-aside that we talked about a moment ago, right? a moment ago, right? Um and then write-through also suffers Um and then write-through also suffers from what's called the dual write from what's called the dual write problem. problem. problem. This would be that if the cache update This would be that if the cache update succeeds, but the database write fails succeeds, but the database write fails or vice versa, then the two enter an or vice versa, then the two enter an inconsistent state. And so, you would inconsistent state. And so, you would need fancy retry logic, error handling, need fancy retry logic, error handling, all of these things to deal with this. all of these things to deal with this. But in a distributed system, this But in a distributed system, this perfect consistency is incredibly hard perfect consistency is incredibly hard to achieve. So, there's all sorts of to achieve. So, there's all sorts of complications here. complications here. Now, Now, Now, what should you do in a system design what should you do in a system design interview? Well, the reality is that interview? Well, the reality is that write-through is much less common than write-through is much less common than cache-aside because it requires all of cache-aside because it requires all of this specialized infrastructure and has this specialized infrastructure and has all these tricky edge cases around all these tricky edge cases around consistency. consistency. consistency. And so, you'd really only bring it up And so, you'd really only bring it up when reads must always return the fresh when reads must always return the fresh data and your system can tolerate some data and your system can tolerate some slightly slower writes. If that's the slightly slower writes. If that's the case, this could be a good fit. But if case, this could be a good fit. But if your mind is going in this direction, your mind is going in this direction, really be sure that you can convince really be sure that you can convince yourself that cache-aside or some other yourself that cache-aside or some other design system doesn't already satisfy design system doesn't already satisfy your use case. Next is write-behind your use case. Next is write-behind caching, also called write-back caching, also called write-back sometimes. And it's really similar to sometimes. And it's really similar to write-through, which we just discussed, write-through, which we just discussed, but instead of updating the database but instead of updating the database synchronously, the cache writes to the synchronously, the cache writes to the database asynchronously in the database asynchronously in the background. background. background. And so, the application only writes to And so, the application only writes to the cache, just like we did for the cache, just like we did for write-through, but then the cache write-through, but then the cache flushes those updates to the database flushes those updates to the database usually in batches later on. usually in batches later on. And so, this makes writes much faster And so, this makes writes much faster than write-through. We solved that than write-through. We solved that problem, but it introduces new risk. And problem, but it introduces new risk. And that's that if the cache were to crash that's that if the cache were to crash or fail before this flush, then we would or fail before this flush, then we would have data loss. have data loss. You would use this only when high write You would use this only when high write throughput is more important than throughput is more important than immediate consistency or close to immediate consistency or close to immediate consistency. And so, for immediate consistency. And so, for example, something like analytics or example, something like analytics or metric pipelines where some occasional metric pipelines where some occasional data loss might be acceptable. data loss might be acceptable. But here's the reality. While this But here's the reality. While this pattern is useful to know, and if you pattern is useful to know, and if you are an expert in caching and you have a are an expert in caching and you have a use case for it, by all means use it. If use case for it, by all means use it. If you're a novice, you're a novice, I wouldn't suggest using this unless you I wouldn't suggest using this unless you can strongly justify it. There are other can strongly justify it. There are other ways to solve the problems that we just ways to solve the problems that we just discussed, discussed, discussed, um and you're probably introducing um and you're probably introducing or opening an opportunity for more or opening an opportunity for more questions than your interviewer than you questions than your interviewer than you would like. So, would like. So, my honest suggestion to you is probably my honest suggestion to you is probably avoid it. The last one we'll discuss is avoid it. The last one we'll discuss is read-through caching. It's incredibly read-through caching. It's incredibly similar to cache-aside, the very first similar to cache-aside, the very first one we talked about, except that the one we talked about, except that the cache handles the database look-up cache handles the database look-up instead of the application. instead of the application. And so, on a cache miss, whereas before And so, on a cache miss, whereas before the application server went and read the application server went and read from the database and updated the cache from the database and updated the cache and returned the value, now the cache and returned the value, now the cache itself is going to do that. So, we try itself is going to do that. So, we try to read from the cache. If we miss, the to read from the cache. If we miss, the cache goes and requests the data from cache goes and requests the data from the database, stores it in the cache, the database, stores it in the cache, and returns it back to the application and returns it back to the application server. server. server. You can basically think about it like You can basically think about it like cache-aside, but with the cache acting cache-aside, but with the cache acting as a proxy. as a proxy. And this is essentially how CDNs work, And this is essentially how CDNs work, right? As we discussed a moment ago. right? As we discussed a moment ago. When it when you have a CDN miss, it When it when you have a CDN miss, it fetches from the origin server, caches fetches from the origin server, caches the results, and then serves it the next the results, and then serves it the next time. time. time. Um now, as far as system design Um now, as far as system design interviews go again, you'd really only interviews go again, you'd really only bring this up in the context of CDNs or bring this up in the context of CDNs or edge caching. For most application-level edge caching. For most application-level caching, cache-aside is still the best caching, cache-aside is still the best default because it doesn't require a default because it doesn't require a special framework or caching library to special framework or caching library to handle this part right here, right? So, handle this part right here, right? So, you can just use Redis, Memcached, you can just use Redis, Memcached, something simple, um and not have to something simple, um and not have to have some adapter right there. So, have some adapter right there. So, zooming out now, let's see if we can fit zooming out now, let's see if we can fit all four of them in there. There we go. all four of them in there. There we go. We got cache-aside, write-through, We got cache-aside, write-through, write-behind, and read-through. And if write-behind, and read-through. And if you're anything like me, you're probably you're anything like me, you're probably thinking, "These names are super thinking, "These names are super confusing. How am I supposed to remember confusing. How am I supposed to remember all of them?" And I've good news for all of them?" And I've good news for you. You don't have to. Interviewers you. You don't have to. Interviewers don't care if you remember these exact don't care if you remember these exact names or exact terms. What matters is names or exact terms. What matters is just that you can describe the behavior just that you can describe the behavior clearly. And so, if you forget clearly. And so, if you forget cache-aside, no problem. Just say, "I'll cache-aside, no problem. Just say, "I'll check the cache first, and if it's not check the cache first, and if it's not there, I'll go to the database and then there, I'll go to the database and then update the cache." That's really all we update the cache." That's really all we as interviewers are looking for. as interviewers are looking for. And so, it's always better to show that And so, it's always better to show that you understand how caching works than to you understand how caching works than to just be able to memorize any of these just be able to memorize any of these terms. So, don't overwhelm yourself. terms. So, don't overwhelm yourself. Understand how cache-aside works most Understand how cache-aside works most importantly, be able to describe it, importantly, be able to describe it, don't stress yourself on the naming. don't stress yourself on the naming. Next up, let's talk a little bit about Next up, let's talk a little bit about cache eviction policies. cache eviction policies. And so, one trade-off of storing data in And so, one trade-off of storing data in memory instead of on disk is that memory memory instead of on disk is that memory is limited. You can't fit your whole is limited. You can't fit your whole data set in the cache. At least you data set in the cache. At least you usually can't. And so, you need a usually can't. And so, you need a strategy for deciding what to keep and strategy for deciding what to keep and what to remove as new data comes in. And what to remove as new data comes in. And that's exactly what your eviction that's exactly what your eviction policies do. They determine which items policies do. They determine which items stay in the cache and which ones get stay in the cache and which ones get replaced when it fills up. replaced when it fills up. And so, cache eviction policies are And so, cache eviction policies are really straightforward, and so we're not really straightforward, and so we're not going to make them any more complicated going to make them any more complicated than they need to be. There are four than they need to be. There are four that you should know about, three of that you should know about, three of which you might actually use in an which you might actually use in an interview, one is just good to know interview, one is just good to know because of its simplicity. because of its simplicity. And so, first, we have least recently And so, first, we have least recently used or LRU. used or LRU. And just like the name suggests, this And just like the name suggests, this eviction policy evicts items that eviction policy evicts items that haven't been used very recently. haven't been used very recently. In practice, it's often implemented with In practice, it's often implemented with a linked list or maybe a priority queue a linked list or maybe a priority queue that tracks the access order, but you're that tracks the access order, but you're rarely going to need to discuss the rarely going to need to discuss the level of detail that level of detail in level of detail that level of detail in a system design interview. a system design interview. Implementation for eviction policies is Implementation for eviction policies is almost always out of scope, right? almost always out of scope, right? And so, the second one that you have And so, the second one that you have here is least frequently used, LFU. here is least frequently used, LFU. Really similar idea, but instead of Really similar idea, but instead of evicting based on recency, it's based on evicting based on recency, it's based on how often something is accessed. how often something is accessed. And so, the least frequently used items And so, the least frequently used items are evicted first, even if they were are evicted first, even if they were used recently, right? So, even if it was used recently, right? So, even if it was just used once, and that just used once just used once, and that just used once was a second ago, was a second ago, it's going to get evicted cuz it was it's going to get evicted cuz it was only used one time compared to only used one time compared to everything else that that might have everything else that that might have been accessed more frequently. been accessed more frequently. Um third is first in, first out. This is Um third is first in, first out. This is that simple one, right? It's exactly that simple one, right? It's exactly like what it sounds like. Your oldest like what it sounds like. Your oldest item gets removed to make space for your item gets removed to make space for your newest item. It's dead simple, and it's newest item. It's dead simple, and it's rarely the right choice in in a system rarely the right choice in in a system design interview. design interview. Um Um Um and then lastly, you have time to live and then lastly, you have time to live or TTL. or TTL. And so, here each cached item has an And so, here each cached item has an expiration time, and once that time expiration time, and once that time passes, say like 5 minutes, then the passes, say like 5 minutes, then the cache will automatically remove it. This cache will automatically remove it. This is great for data that can go stale, is great for data that can go stale, like user sessions or API responses or like user sessions or API responses or any of that. any of that. And so, in system design interviews, And so, in system design interviews, least recently used is the most common least recently used is the most common and oftentimes the default. Um least and oftentimes the default. Um least frequently used makes sense when your frequently used makes sense when your access pattern is highly skewed, meaning access pattern is highly skewed, meaning there's a few items that are read way there's a few items that are read way more often than others. more often than others. And then TTL is super common as it's And then TTL is super common as it's perfect for when freshness really perfect for when freshness really matters more than recency or frequency. matters more than recency or frequency. So far, caching sounds pretty So far, caching sounds pretty straightforward. We store data in a straightforward. We store data in a faster layer, we read it when we can, faster layer, we read it when we can, and everything just gets quicker. and everything just gets quicker. But in practice, adding cache introduces But in practice, adding cache introduces a whole new set of challenges, and a whole new set of challenges, and they're challenges that interviewers they're challenges that interviewers love to ask about. And so, there's this love to ask about. And so, there's this famous saying in computer science that famous saying in computer science that some of you might have heard, and it's some of you might have heard, and it's that there's only two hard problems in that there's only two hard problems in computer science, naming things and computer science, naming things and cache invalidation. And that second one cache invalidation. And that second one exists for a reason. Once you start exists for a reason. Once you start caching, new problems start to show up, caching, new problems start to show up, things like uneven loads, stale data, or things like uneven loads, stale data, or unexpected spikes in traffic. And so, unexpected spikes in traffic. And so, we're going to go through a few of the we're going to go through a few of the most common issues that you should be most common issues that you should be ready to talk about in an interview, the ready to talk about in an interview, the ones that interviewers will kind of be ones that interviewers will kind of be the most likely to maybe probe into. the most likely to maybe probe into. And so, let's start with what's called a And so, let's start with what's called a cache stampede or oftentimes referred to cache stampede or oftentimes referred to as a thundering herd. as a thundering herd. And so, this happens when a popular And so, this happens when a popular cache entry expires via that TTL that we cache entry expires via that TTL that we discussed a moment ago. And suddenly, a discussed a moment ago. And suddenly, a flood of requests all try to rebuild flood of requests all try to rebuild that cache at the same time. that cache at the same time. And so, even if that window lasts just a And so, even if that window lasts just a second, every single one of those cache second, every single one of those cache misses is going to hit our database, misses is going to hit our database, turning one query into thousands or even turning one query into thousands or even millions, and ultimately overwhelming millions, and ultimately overwhelming the database. And so, let me give a the database. And so, let me give a concrete example. concrete example. Imagine that you have some website for Imagine that you have some website for which you cache the homepage feed with a which you cache the homepage feed with a TTL of 60 seconds, right? You don't want TTL of 60 seconds, right? You don't want to cache it too long cuz you don't want to cache it too long cuz you don't want it to get too stale. Um so, 60 seconds it to get too stale. Um so, 60 seconds is what you choose. is what you choose. And then we get 100,000 requests every And then we get 100,000 requests every second. second. second. And so, all 100,000 requests cuz all And so, all 100,000 requests cuz all users need that home feed, they hit us users need that home feed, they hit us in the cache, and everything works in the cache, and everything works great. But after 60 seconds, it expires. great. But after 60 seconds, it expires. And what happens when it expires in the And what happens when it expires in the case of cache-aside is that we're then case of cache-aside is that we're then going to go ask for it from the database going to go ask for it from the database and update the cache. and update the cache. But in that moment, 100,000 requests But in that moment, 100,000 requests come into the cache, they all fail, and come into the cache, they all fail, and then they all go try to hit the then they all go try to hit the database, and they could take the database, and they could take the database down, overwhelm it, and cause database down, overwhelm it, and cause cascading failures. Now, there's two cascading failures. Now, there's two common ways that you can prevent this. common ways that you can prevent this. The first is what's called request The first is what's called request coalescing or single flight. coalescing or single flight. Both fairly fancy names, but the idea is Both fairly fancy names, but the idea is actually really simple. When a request actually really simple. When a request tries to rebuild the same cache key, or tries to rebuild the same cache key, or when multiple requests try to rebuild when multiple requests try to rebuild the same cache key, then only the first the same cache key, then only the first one should work, and the rest of them one should work, and the rest of them should just wait for the results to come should just wait for the results to come in and then read from the cache. in and then read from the cache. So, that's the first most common way to So, that's the first most common way to handle it. handle it. The second, which is actually maybe The second, which is actually maybe equally as common, I'll contradict equally as common, I'll contradict myself, is called cache warming. myself, is called cache warming. And the way that this works is that And the way that this works is that instead of waiting for popular keys to instead of waiting for popular keys to expire, waiting that full 60 seconds, expire, waiting that full 60 seconds, you can proactively refresh them just you can proactively refresh them just before they do. And so, say at the before they do. And so, say at the 55-second mark, we could come in here 55-second mark, we could come in here and refresh the feed, um thus giving it and refresh the feed, um thus giving it another 60 seconds, and essentially another 60 seconds, and essentially preventing it from ever actually preventing it from ever actually expiring, right? We just keep refreshing expiring, right? We just keep refreshing it every 55 seconds so that it remains it every 55 seconds so that it remains fresh and not stale, um but it never fresh and not stale, um but it never expires and causes this thundering herd. expires and causes this thundering herd. Next up is cache consistency. And this Next up is cache consistency. And this is probably actually the most common is probably actually the most common issues that interviewers like to ask issues that interviewers like to ask about when caching comes up. And this about when caching comes up. And this happens because the cache and the happens because the cache and the database could return different values database could return different values for the same data. for the same data. And it's really easy for this to occur, And it's really easy for this to occur, right? Because most systems read from right? Because most systems read from the cache, but they write to the the cache, but they write to the database. And so, this creates this database. And so, this creates this short window, depending on your eviction short window, depending on your eviction policy, where you can have stale data in policy, where you can have stale data in the cache. All right, let me give you a the cache. All right, let me give you a concrete example to make this click. concrete example to make this click. Imagine that you have some social Imagine that you have some social network, and a user updates their network, and a user updates their profile picture. And so, that new value profile picture. And so, that new value is written to the database, but that old is written to the database, but that old one is still sitting out there in the one is still sitting out there in the cache. cache. cache. And so, now other users who are And so, now other users who are requesting are hitting the cache, and requesting are hitting the cache, and they're getting image two. they're getting image two. No, that's bad copy and paste, actually. No, that's bad copy and paste, actually. Let me update that. Bang, right? All Let me update that. Bang, right? All right. right. right. Image three. Wait, no, I want one image Image three. Wait, no, I want one image one. Silly. Okay. one. Silly. Okay. &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; They're getting the old one, the &gt;&gt; They're getting the old one, the original one, image one, despite the original one, image one, despite the fact that we'd already updated the fact that we'd already updated the database to image two. And they're going database to image two. And they're going to continue to read this value until to continue to read this value until this ends up being evicted for some this ends up being evicted for some reason. Right? reason. Right? Now, there's no perfect fix to this. It Now, there's no perfect fix to this. It very much depends on how fresh your data very much depends on how fresh your data needs to be, and that is a case-by-case needs to be, and that is a case-by-case basis, right? But there's some common basis, right? But there's some common strategies in order to answer the strategies in order to answer the question if your if your interviewer question if your if your interviewer asks about this, or if this ends up asks about this, or if this ends up being an issue. being an issue. And so, the first is to invalidate on And so, the first is to invalidate on write. write. write. And so, if consistency is really And so, if consistency is really important here, then when that profile important here, then when that profile picture came in and we updated in the picture came in and we updated in the database, then we could also go delete database, then we could also go delete that key proactively from the cache. that key proactively from the cache. In this way, the next time a read comes In this way, the next time a read comes in, well, we would see that it's in, well, we would see that it's missing, and we would go grab it from missing, and we would go grab it from the database and update the cache, the database and update the cache, right? That would be the process of right? That would be the process of invalidating on write, and it's going to invalidating on write, and it's going to make sure that you're reading the latest make sure that you're reading the latest data for the most part. data for the most part. You can also use just short TTLs. And You can also use just short TTLs. And so, if some stale-ness is acceptable, so, if some stale-ness is acceptable, well, you can keep the cache entry here, well, you can keep the cache entry here, but have a TTL that's really short. This but have a TTL that's really short. This is like what we had a second ago, right? is like what we had a second ago, right? So, in the case of that newsfeed, we had So, in the case of that newsfeed, we had a 60-second TTL, and maybe we keep this a 60-second TTL, and maybe we keep this around for 60 seconds, then it'll be around for 60 seconds, then it'll be gone, and we'll go grab a new one, cuz gone, and we'll go grab a new one, cuz we know this is something that changes we know this is something that changes often. That's potentially acceptable. often. That's potentially acceptable. Alternatively, Alternatively, Alternatively, you might just accept that eventual you might just accept that eventual consistency is fine, and this is totally consistency is fine, and this is totally valid. Uh this is for things like feeds, valid. Uh this is for things like feeds, analytics, metrics, or brief delays analytics, metrics, or brief delays totally acceptable. And so, if I was totally acceptable. And so, if I was discussing this specific problem that we discussing this specific problem that we have here, maybe I would say, you know, have here, maybe I would say, you know, we have a 5-minute TTL on our profile we have a 5-minute TTL on our profile data cached here, and that's totally data cached here, and that's totally fine. Because what it means is that some fine. Because what it means is that some users will see a stale profile image for users will see a stale profile image for 5 minutes, and I'm okay with that, 5 minutes, and I'm okay with that, because it's not the end of the world, because it's not the end of the world, and it's fine if they see an old image and it's fine if they see an old image for a while. Life is still going to go for a while. Life is still going to go on. Okay, right? Maybe that's something on. Okay, right? Maybe that's something that I could justify based on the that I could justify based on the design, and it would be a totally valid design, and it would be a totally valid justification to make. Another common justification to make. Another common issue, which you've seen come up in many issue, which you've seen come up in many of the videos if you've watched any of of the videos if you've watched any of our other content, is called hot keys. our other content, is called hot keys. And so, hot keys is a cache entry that And so, hot keys is a cache entry that gets way more traffic than everything gets way more traffic than everything else. Even if your overall cache hit else. Even if your overall cache hit rate is great, everything's working as rate is great, everything's working as planned, that single key can still planned, that single key can still become a large bottleneck for your become a large bottleneck for your system. system. system. And so, for example, imagine that you're And so, for example, imagine that you're building Twitter X, and everyone is building Twitter X, and everyone is viewing Taylor Swift's profile. viewing Taylor Swift's profile. That cache key for her user data could That cache key for her user data could be receiving millions of requests per be receiving millions of requests per second, and that one key can overload a second, and that one key can overload a single Redis node or shard, even though single Redis node or shard, even though that shard, or even though that cache is that shard, or even though that cache is technically working as expected, right? technically working as expected, right? And so, this, of course, isn't just a And so, this, of course, isn't just a problem with caching. You've heard us problem with caching. You've heard us talk about this if you've watched our talk about this if you've watched our other videos in other contexts, too. other videos in other contexts, too. Databases can have hot rows or Databases can have hot rows or partitions as well. But it's a common partitions as well. But it's a common follow-up question in interviews once follow-up question in interviews once you introduce caching, especially if you you introduce caching, especially if you introduced caching in order to scale introduced caching in order to scale your reads, right? So, caching does your reads, right? So, caching does increase your overall read throughput. increase your overall read throughput. You're now hitting memory as opposed to You're now hitting memory as opposed to disk, but it doesn't completely solve disk, but it doesn't completely solve the problem if there's one piece of data the problem if there's one piece of data that is so overwhelmingly popular, like that is so overwhelmingly popular, like with Taylor Swift here. with Taylor Swift here. And so, there's a couple things that you And so, there's a couple things that you can do. The first and and the most can do. The first and and the most common solution is to just replicate common solution is to just replicate these hot keys. And so, if everybody is these hot keys. And so, if everybody is trying to know about Taylor Swift, well, trying to know about Taylor Swift, well, you can put Taylor Swift on each of the you can put Taylor Swift on each of the different shards, or each of the different shards, or each of the different instances of the cache in your different instances of the cache in your cache cluster as you've scaled up. Uh if cache cluster as you've scaled up. Uh if what I'm talking here about sharding and what I'm talking here about sharding and clustering is clustering is not familiar to you, I'm going to link a not familiar to you, I'm going to link a video uh on sharding below, but this is video uh on sharding below, but this is basically the concept of a single cache basically the concept of a single cache isn't enough, so we needed to add isn't enough, so we needed to add additional instances of caches and shard additional instances of caches and shard our data across them, meaning split our our data across them, meaning split our data across them. Okay. So, now back to data across them. Okay. So, now back to the issue at hand. Replicating hot keys, the issue at hand. Replicating hot keys, right? Means that we'll take Taylor right? Means that we'll take Taylor Swift and we'll put her on each of these Swift and we'll put her on each of these different caches. different caches. And then now the application server can And then now the application server can just load balance evenly amongst all of just load balance evenly amongst all of them. So, instead of all the traffic them. So, instead of all the traffic going to this cache or this node, it going to this cache or this node, it could hit any of the three here, right? could hit any of the three here, right? Another fairly popular thing to do is Another fairly popular thing to do is that you can add a local fallback cache. that you can add a local fallback cache. Basically, use that in-process caching Basically, use that in-process caching idea that we talked about earlier in idea that we talked about earlier in order to add a cache Oh. order to add a cache Oh. In order to add a cache In order to add a cache here. here. here. Right? Right? Right? And so, this way, we keep extremely hot And so, this way, we keep extremely hot values like Taylor Swift in the app's values like Taylor Swift in the app's memory, so that repeated requests don't memory, so that repeated requests don't ever even need to go hit Redis. And we ever even need to go hit Redis. And we just store them locally here. just store them locally here. Um hot keys, let's see, to wrap up here, Um hot keys, let's see, to wrap up here, they were really good reminder that they were really good reminder that caching helps us scale reads, uh but it caching helps us scale reads, uh but it doesn't make your system magically doesn't make your system magically infinite, and interviewers love to probe infinite, and interviewers love to probe you on that. So, be ready. you on that. So, be ready. &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; [snorts] &gt;&gt; All right, let's wrap up with what is &gt;&gt; All right, let's wrap up with what is probably the most interesting to all of probably the most interesting to all of you who are preparing for your system you who are preparing for your system design interviews. And that's how should design interviews. And that's how should you discuss caching in your actual you discuss caching in your actual system design interview. Well, there's system design interview. Well, there's two things that we'll talk about. A, two things that we'll talk about. A, when should you bring it up? And then B, when should you bring it up? And then B, once you bring it up, how you should once you bring it up, how you should talk about it, or how you should talk about it, or how you should introduce it. Right? introduce it. Right? And so, the first and most important And so, the first and most important thing is don't just add a cache for the thing is don't just add a cache for the sake of adding a cache. I see this all sake of adding a cache. I see this all the time. I see candidates who just the time. I see candidates who just assume we always need a cache, they assume we always need a cache, they throw it down without any proper throw it down without any proper justification, and sometimes they're justification, and sometimes they're wrong, and even if they're right, the wrong, and even if they're right, the lack of justification was a red flag. lack of justification was a red flag. And so, you want to bring up caching And so, you want to bring up caching typically when one of four things is typically when one of four things is true. The first is that you have a true. The first is that you have a really read-heavy workload that's really read-heavy workload that's straining your database. And so, you straining your database. And so, you could say something like, "We're serving could say something like, "We're serving 100 million daily active users, each of 100 million daily active users, each of which are making uh 20 requests per day. which are making uh 20 requests per day. That's 2 billion reads hitting the That's 2 billion reads hitting the database." Somebody check my math. I I database." Somebody check my math. I I did that a little too quick in my head. did that a little too quick in my head. In any case, you know, it's more than In any case, you know, it's more than our database can handle, so let's put a our database can handle, so let's put a cache in front of it to take the read cache in front of it to take the read load off of our database. Cool. load off of our database. Cool. For expensive queries, you could say For expensive queries, you could say something like in the case of newsfeed, something like in the case of newsfeed, right? Computing a user's personalized right? Computing a user's personalized nude feed newsfeed is going to require nude feed newsfeed is going to require joining a bunch of posts, followers, joining a bunch of posts, followers, likes, all of these things across likes, all of these things across multiple tables, uh and that's going to multiple tables, uh and that's going to be really expensive to compute. And so, be really expensive to compute. And so, what we can do is just cache that what we can do is just cache that newsfeed, uh store it with a TTL of 60 newsfeed, uh store it with a TTL of 60 seconds or so, and then serve it really seconds or so, and then serve it really quickly from something like Redis. quickly from something like Redis. And then high database CPU, this isn't And then high database CPU, this isn't going to come up in an interview because going to come up in an interview because you don't have these metrics, uh but you don't have these metrics, uh but certainly in real life, if your database certainly in real life, if your database is starting to peg out on its CPU, then is starting to peg out on its CPU, then it would make sense to add a cache in it would make sense to add a cache in front of it. And then latency front of it. And then latency requirements, this certainly will come requirements, this certainly will come up in an interview. So, in your up in an interview. So, in your non-functional requirements, maybe you non-functional requirements, maybe you specified that you needed a specified that you needed a 100-millisecond response time on some 100-millisecond response time on some API endpoint. Well, then you could argue API endpoint. Well, then you could argue or justify that that database query is or justify that that database query is going to take too long, especially if it going to take too long, especially if it has expensive queries, and so, you'll has expensive queries, and so, you'll have to cache it instead, right? have to cache it instead, right? The pattern's pretty simple here. The pattern's pretty simple here. Identify the bottleneck, be able to Identify the bottleneck, be able to quantify it with some rough numbers, and quantify it with some rough numbers, and then explain how caching solves it. then explain how caching solves it. Now, once you've done that, you'll want Now, once you've done that, you'll want to actually introduce caching, and this to actually introduce caching, and this is where you talk about all the things is where you talk about all the things that we discussed in this video. And so, that we discussed in this video. And so, the first one being, identify the the first one being, identify the bottleneck. That's what we just we just bottleneck. That's what we just we just mentioned a moment ago. And then decide mentioned a moment ago. And then decide what to cache. Not everything should be what to cache. Not everything should be cached, of course. Focus on the data cached, of course. Focus on the data that is causing the issue, that needs to that is causing the issue, that needs to be read frequently, maybe it doesn't be read frequently, maybe it doesn't change often, it's expensive to fetch or change often, it's expensive to fetch or compute. These are the things that you compute. These are the things that you want to focus on caching. Think about want to focus on caching. Think about what the cache keys are going to be. Be what the cache keys are going to be. Be explicit about this. Especially in explicit about this. Especially in junior or more mid-level interviews, I junior or more mid-level interviews, I often see candidates say, "I'm going to often see candidates say, "I'm going to add a cache here, and that's going to add a cache here, and that's going to solve everything." And my follow-up is solve everything." And my follow-up is always, "Well, what are you caching? always, "Well, what are you caching? What is your cache key? Uh what values What is your cache key? Uh what values are in your cache?" Right? So, make sure are in your cache?" Right? So, make sure you're proactive about that. you're proactive about that. The second thing is to choose that cache The second thing is to choose that cache architecture. All right, so this is architecture. All right, so this is everything that we were talking about everything that we were talking about with cache-asides, write-throughs, with cache-asides, write-throughs, write-behinds, etc. So, you can say, write-behinds, etc. So, you can say, "I'll use cache-aside on read. We check "I'll use cache-aside on read. We check Redis first. If it's there, we return Redis first. If it's there, we return it. If not, we'll query the database, it. If not, we'll query the database, store the results in Redis, and then store the results in Redis, and then return it back to the user." Right? return it back to the user." Right? And then you'll want to mention the And then you'll want to mention the eviction policy. And so, you'll say, eviction policy. And so, you'll say, "We'll introduce a cache. We'll either "We'll introduce a cache. We'll either use LRU, uh LFU, or maybe you'll mention use LRU, uh LFU, or maybe you'll mention that you'll have a TTL for preventing that you'll have a TTL for preventing stale data." But provide some stale data." But provide some justification there as it is relevant to justification there as it is relevant to your system. your system. And then lastly, address any potential And then lastly, address any potential downsides. And so, don't necessarily downsides. And so, don't necessarily just go through the three that I just go through the three that I mentioned, but think about your system. mentioned, but think about your system. Which of these are relevant? Do I have a Which of these are relevant? Do I have a TTL on a popular key that could cause a TTL on a popular key that could cause a cache stampede or a thundering herd? Do cache stampede or a thundering herd? Do I have issues with cache consistency, I have issues with cache consistency, where I'm going to potentially be where I'm going to potentially be serving data that is no longer fresh, serving data that is no longer fresh, but instead stale? Uh and is that a but instead stale? Uh and is that a problem for my system, or is it not? And problem for my system, or is it not? And do I have any issues maybe with hot keys do I have any issues maybe with hot keys or anything similar? or anything similar? If you discuss in this order these five If you discuss in this order these five things, then you'll be a caching pro, things, then you'll be a caching pro, your interviewer will be impressed, your interviewer will be impressed, um and you can continue to talk about um and you can continue to talk about the rest of the design. the rest of the design. I think the last thing that I'll mention I think the last thing that I'll mention is that caching usually comes up during is that caching usually comes up during your deep dives when you're discussing your deep dives when you're discussing scale. scale. scale. And so, when you get to your And so, when you get to your non-functional requirements, and you're non-functional requirements, and you're either talking about scale or latency, either talking about scale or latency, that's often times the most appropriate that's often times the most appropriate time to bring up caching and try to time to bring up caching and try to figure out where it fits into your figure out where it fits into your system. And it's super common in these system. And it's super common in these interviews, so it's a really good thing interviews, so it's a really good thing to have down pat. to have down pat. All right, there you have it, folks. All right, there you have it, folks. Hopefully, If found this useful. Hopefully, If found this useful. Um any questions, anything you think I Um any questions, anything you think I got wrong, go ahead and drop a comment. got wrong, go ahead and drop a comment. I respond to as many of those as I can. I respond to as many of those as I can. You'll have the Excalidraw that we used You'll have the Excalidraw that we used here in the description. So, go ahead here in the description. So, go ahead and check that out. My LinkedIn is and check that out. My LinkedIn is there. Connect with me. there. Connect with me. I respond to as many messages as I respond to as many messages as possible. I'd love to hear success possible. I'd love to hear success stories, especially if you're finding stories, especially if you're finding what we're doing useful. And most what we're doing useful. And most importantly, good luck with the upcoming importantly, good luck with the upcoming interviews. You guys are going to nail interviews. You guys are going to nail it. You're putting the work in. it. You're putting the work in.
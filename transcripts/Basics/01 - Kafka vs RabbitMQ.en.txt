Kafka and RabbitMQ aren't Kafka and RabbitMQ aren't interchangeable. They solve different interchangeable. They solve different problems, and picking the wrong one can problems, and picking the wrong one can mean significant rework later. But both mean significant rework later. But both exist to solve the same fundamental exist to solve the same fundamental challenge, though. When one service challenge, though. When one service needs to talk to another, the simplest needs to talk to another, the simplest option is a direct call. Your order option is a direct call. Your order service makes an HTTP request to your service makes an HTTP request to your inventory service, waits for a response, inventory service, waits for a response, and moves on. and moves on. This works great until that inventory This works great until that inventory service is slow, or down, or getting service is slow, or down, or getting hammered with traffic. Suddenly, your hammered with traffic. Suddenly, your order service is just stuck waiting, order service is just stuck waiting, timing out, or dropping requests on the timing out, or dropping requests on the floor because it has nowhere to put floor because it has nowhere to put them. Message queues like Kafka and them. Message queues like Kafka and RabbitMQ solve this because they add a RabbitMQ solve this because they add a buffer between your services. buffer between your services. Instead of calling the inventory service Instead of calling the inventory service directly, your order service drops a directly, your order service drops a message into a queue, and then just message into a queue, and then just returns immediately. The inventory returns immediately. The inventory service can then pick up that message service can then pick up that message whenever it's ready and process it. whenever it's ready and process it. If the inventory service is slow, If the inventory service is slow, messages accumulate in the queue. messages accumulate in the queue. If you get a massive spike in orders If you get a massive spike in orders during a flash sale, for example, the during a flash sale, for example, the queue absorbs all of that traffic, and queue absorbs all of that traffic, and then lets your downstream services then lets your downstream services process it at whatever pace they can process it at whatever pace they can handle. So, Kafka and RabbitMQ both give handle. So, Kafka and RabbitMQ both give you this decoupling. Producers, you this decoupling. Producers, consumers, with a broker or a message consumers, with a broker or a message queue in the middle. But how they queue in the middle. But how they actually work under the hood is actually work under the hood is fundamentally different, and that's what fundamentally different, and that's what needs to end up driving your needs to end up driving your decision-making when you're deciding decision-making when you're deciding which of the two to use in your project. which of the two to use in your project. Starting with RabbitMQ, Starting with RabbitMQ, RabbitMQ is a traditional message RabbitMQ is a traditional message broker, and it follows a mental model broker, and it follows a mental model that most developers find pretty that most developers find pretty intuitive if you've ever used a intuitive if you've ever used a queue-like data structure. queue-like data structure. A producer sends a message to a broker. A producer sends a message to a broker. The broker looks at some routing rules The broker looks at some routing rules you've configured to figure out which you've configured to figure out which queue that message belongs to, and then queue that message belongs to, and then a consumer pulls the messages off of a consumer pulls the messages off of that queue. that queue. Once the consumer acknowledges Once the consumer acknowledges successful processing, RabbitMQ deletes successful processing, RabbitMQ deletes the message. the message. The broker handles a lot for you. It The broker handles a lot for you. It routes messages to the right queues, routes messages to the right queues, tracks which messages have been tracks which messages have been delivered, and then it retries any delivered, and then it retries any failed deliveries. failed deliveries. When a message fails repeatedly, When a message fails repeatedly, RabbitMQ moves it to a dead letter queue RabbitMQ moves it to a dead letter queue automatically so that you can debug it automatically so that you can debug it later. As the engineer, you configure later. As the engineer, you configure these rules, and then the broker these rules, and then the broker enforces them. enforces them. Kafka doesn't have all of this built in. Kafka doesn't have all of this built in. If you need dead letter handling in If you need dead letter handling in Kafka, you end up building it yourself. Kafka, you end up building it yourself. Now, from the perspective of the Now, from the perspective of the consumer, this is simple. You connect or consumer, this is simple. You connect or subscribe to a queue, messages show up, subscribe to a queue, messages show up, you process them, and you send back an you process them, and you send back an acknowledgement. All of the complexity acknowledgement. All of the complexity of figuring out where messages came of figuring out where messages came from, where they need to go, that's the from, where they need to go, that's the broker's problem, not the consumers. broker's problem, not the consumers. This model maps really well to This model maps really well to task-oriented workloads. Sending emails, task-oriented workloads. Sending emails, processing payments, resizing messages. processing payments, resizing messages. You've got a unit of work that needs to You've got a unit of work that needs to happen. You drop it into a queue, happen. You drop it into a queue, something picks it up and does it. As we something picks it up and does it. As we alluded to, Kafka works a little alluded to, Kafka works a little differently. Instead of being a message differently. Instead of being a message broker that routes messages to queues broker that routes messages to queues and deletes them when they're consumed, and deletes them when they're consumed, Kafka is essentially a distributed Kafka is essentially a distributed append-only log. append-only log. When a producer sends a message, Kafka When a producer sends a message, Kafka appends it to a topic. appends it to a topic. That message doesn't disappear when That message doesn't disappear when someone reads it. It just sits there in someone reads it. It just sits there in the log. And depending on how you've the log. And depending on how you've configured your attention, it may stay configured your attention, it may stay there for hours, days, weeks, or even there for hours, days, weeks, or even indefinitely. indefinitely. indefinitely. Consumers then read from this log, but Consumers then read from this log, but they're responsible for tracking their they're responsible for tracking their own position. own position. Kafka calls this position an offset. Kafka calls this position an offset. So, when your consumer reads a message So, when your consumer reads a message number 500, your consumer then remembers number 500, your consumer then remembers that it's at position 500. If your that it's at position 500. If your consumer crashes or restarts, it looks consumer crashes or restarts, it looks up where it left off, and it picks up up where it left off, and it picks up from there. from there. And if you need to reread a message from And if you need to reread a message from an hour ago, you just rewind at that an hour ago, you just rewind at that offset. Totally possible. offset. Totally possible. That changes the model. That changes the model. RabbitMQ is a smart broker with simple RabbitMQ is a smart broker with simple consumers. It handles routing, tracks consumers. It handles routing, tracks deliveries, manages retries. deliveries, manages retries. Kafka is a simple broker with smart Kafka is a simple broker with smart consumers. It stores messages in order. consumers. It stores messages in order. Consumers decide what to read and when. Consumers decide what to read and when. Kafka does track consumer group offsets, Kafka does track consumer group offsets, but the consumer drives the logic still. but the consumer drives the logic still. The benefit is that messages become The benefit is that messages become durable and replayable in Kafka. durable and replayable in Kafka. So, multiple consumer groups can read So, multiple consumer groups can read the same stream independently. Your the same stream independently. Your analytics team can process the same analytics team can process the same events as your real-time notification events as your real-time notification system. A new service that comes online system. A new service that comes online 6 months from now can read the entire 6 months from now can read the entire history from day one and do whatever history from day one and do whatever backfilling it needs to do. So, let's backfilling it needs to do. So, let's emphasize that distinction again. emphasize that distinction again. RabbitMQ is a message broker. Messages RabbitMQ is a message broker. Messages just flow through it. just flow through it. Kafka is a log. Messages live in it. Kafka is a log. Messages live in it. In RabbitMQ, once a message is consumed, In RabbitMQ, once a message is consumed, it's gone. In Kafka, messages persist it's gone. In Kafka, messages persist and any number of consumers can read and any number of consumers can read them at any time from any point in them at any time from any point in history. history. history. This one difference drives almost every This one difference drives almost every other trade-off. other trade-off. Because they solve different problems, Because they solve different problems, many teams end up actually using both. many teams end up actually using both. Kafka as their durable event stream that Kafka as their durable event stream that multiple systems read from, RabbitMQ as multiple systems read from, RabbitMQ as the task queue that processes the work the task queue that processes the work that those events trigger. Now that you that those events trigger. Now that you understand the basic models, here are understand the basic models, here are the specific technical differences that the specific technical differences that should drive your decision. should drive your decision. Starting with ordering guarantees. Starting with ordering guarantees. Both systems preserve order, but Both systems preserve order, but differently. RabbitMQ queues are differently. RabbitMQ queues are strictly ordered, meaning messages come strictly ordered, meaning messages come out in the order that they went in. So, out in the order that they went in. So, if you had just a single consumer, then if you had just a single consumer, then you are guaranteed perfect ordering. But you are guaranteed perfect ordering. But with multiple consumers pulling from the with multiple consumers pulling from the queue at the same time, they each queue at the same time, they each process in parallel. So, you could trade process in parallel. So, you could trade off that ordering for throughput. off that ordering for throughput. Kafka, on the other hand, splits each Kafka, on the other hand, splits each topic into partitions. topic into partitions. So, order is guaranteed only within a So, order is guaranteed only within a partition, but not across partitions. partition, but not across partitions. You control which partition a message You control which partition a message goes to by its partition key. goes to by its partition key. All orders for consumer 12345 go to the All orders for consumer 12345 go to the same partition and get processed in same partition and get processed in order. Customer 67890's orders may go to order. Customer 67890's orders may go to a different partition. a different partition. So, each consumer's orders stay in So, each consumer's orders stay in sequence, but there is no global sequence, but there is no global ordering. ordering. ordering. The trade-off is that RabbitMQ gives you The trade-off is that RabbitMQ gives you global ordering with a single consumer. global ordering with a single consumer. Kafka gives you per entity ordering with Kafka gives you per entity ordering with parallelism. Pick based on which one you parallelism. Pick based on which one you need. This decision then has a need. This decision then has a consequence on throughput and latency. consequence on throughput and latency. RabbitMQ handles about 4,000 to 10,000 RabbitMQ handles about 4,000 to 10,000 messages per second. Of course, this is messages per second. Of course, this is dependent on message size and hardware. dependent on message size and hardware. But the latency sits at around 1 to 5 But the latency sits at around 1 to 5 milliseconds for low-volume workloads. milliseconds for low-volume workloads. That's because the broker pushes That's because the broker pushes messages to consumers automatically, but messages to consumers automatically, but the broker's also doing a lot of work. the broker's also doing a lot of work. Tracking delivery state, managing Tracking delivery state, managing acknowledgements, handling routing acknowledgements, handling routing decisions, all of that per message decisions, all of that per message overhead ends up adding up, and as it overhead ends up adding up, and as it increases, throughput decreases. increases, throughput decreases. Kafka handles over a million messages Kafka handles over a million messages per second, roughly 100 times more. per second, roughly 100 times more. Latency is higher at baseline, typically Latency is higher at baseline, typically 5 to 50 milliseconds, because consumers 5 to 50 milliseconds, because consumers pull messages in batches, rather than pull messages in batches, rather than receiving them immediately. receiving them immediately. The broker does far less work per The broker does far less work per message. It appends to a sequential log, message. It appends to a sequential log, and then lets consumers track their own and then lets consumers track their own position. position. position. And so, under heavy load, that And so, under heavy load, that simplicity really pays off. All that it simplicity really pays off. All that it needs to do is append to a log, and thus needs to do is append to a log, and thus latency can stay consistent even as the latency can stay consistent even as the volume grows. What about delivery volume grows. What about delivery guarantees, which is a really, really guarantees, which is a really, really important topic when it comes to message important topic when it comes to message queues? queues? queues? What happens when a consumer fails to What happens when a consumer fails to process a message? process a message? You have two options. You could lose You have two options. You could lose that message, or you could deliver it that message, or you could deliver it again to another consumer and risk again to another consumer and risk duplicating the work. duplicating the work. At most once delivery means that you At most once delivery means that you might lose messages. The broker sends might lose messages. The broker sends once and it doesn't retry. once and it doesn't retry. This is really fast, but if something This is really fast, but if something goes wrong, the message is gone forever. goes wrong, the message is gone forever. At least once means that you might get At least once means that you might get duplicates. The broker will just retry duplicates. The broker will just retry until it gets an acknowledgement from a until it gets an acknowledgement from a consumer. This way you get no data loss, consumer. This way you get no data loss, but your consumers could end up seeing but your consumers could end up seeing that message twice and thus processing that message twice and thus processing it twice. it twice. Both RabbitMQ and Kafka support at least Both RabbitMQ and Kafka support at least once, which is what most applications once, which is what most applications need and is the industry standard. Kafka need and is the industry standard. Kafka does also support exactly once, which is does also support exactly once, which is the holy grail, where each message is the holy grail, where each message is passed and processed exactly one time. passed and processed exactly one time. But it's much more limited than it But it's much more limited than it sounds, So, be cautious if anybody sounds, So, be cautious if anybody recommends this. recommends this. It only works when both input and output It only works when both input and output are Kafka topics within the exact same are Kafka topics within the exact same Kafka cluster under Kafka transactions. Kafka cluster under Kafka transactions. So, the moment you need to write to a So, the moment you need to write to a database, call on an external API, or database, call on an external API, or cross cluster boundaries, you're back to cross cluster boundaries, you're back to at least once. In practice, most at least once. In practice, most applications still need item potent applications still need item potent consumers. So, don't end up picking consumers. So, don't end up picking Kafka just because you heard it supports Kafka just because you heard it supports exactly once. The reality is it's a lot exactly once. The reality is it's a lot more nuanced than that, and for your use more nuanced than that, and for your use case, it probably doesn't. RabbitMQ is case, it probably doesn't. RabbitMQ is much simpler to run. It's a single much simpler to run. It's a single binary. It's straightforward clustering. binary. It's straightforward clustering. It has built-in management UI. So, if It has built-in management UI. So, if you're a small team running just a few you're a small team running just a few queues, it's really approachable. queues, it's really approachable. Kafka is, of course, harder. Kafka is, of course, harder. Historically, you needed Zookeeper as a Historically, you needed Zookeeper as a separate coordination service. Newer separate coordination service. Newer versions don't use that, they use Raft, versions don't use that, they use Raft, uh which eliminates the need for uh which eliminates the need for Zookeeper, but you still need to manage Zookeeper, but you still need to manage partition rebalancing, broker failures, partition rebalancing, broker failures, topic configurations, and consumer group topic configurations, and consumer group coordination. So, there's a lot more to coordination. So, there's a lot more to learn, and a lot more that can go wrong. learn, and a lot more that can go wrong. Managed services change this calculus, Managed services change this calculus, of course. You have Confluent Cloud, of course. You have Confluent Cloud, Amazon MSK, and Azure Event Hubs, which Amazon MSK, and Azure Event Hubs, which handle most of that Kafka complexity for handle most of that Kafka complexity for you. So, if you're choosing Kafka, you. So, if you're choosing Kafka, strongly consider a managed service strongly consider a managed service unless you have dedicated infrastructure unless you have dedicated infrastructure expertise already within the team. So, expertise already within the team. So, to wrap up here, when should you use to wrap up here, when should you use each? You'll want RabbitMQ if you need each? You'll want RabbitMQ if you need task queues and background jobs, like task queues and background jobs, like sending emails, processing payments, sending emails, processing payments, resizing images. The work goes in, it resizing images. The work goes in, it gets done, and the message disappears. gets done, and the message disappears. If you want smart routing, like the If you want smart routing, like the broker decides which worker gets which broker decides which worker gets which message based on content. message based on content. If you want low latency at moderate If you want low latency at moderate scale, so sub 5 ms delivery when you're scale, so sub 5 ms delivery when you're not pushing millions of messages. And not pushing millions of messages. And you want simple operations. It's easier you want simple operations. It's easier to set up, and it's easier to reason to set up, and it's easier to reason about. about. about. Instagram actually uses RabbitMQ to Instagram actually uses RabbitMQ to process photo uploads. When you post a process photo uploads. When you post a picture, resizing and filtering happens picture, resizing and filtering happens via background workers, which are pulled via background workers, which are pulled off of RabbitMQ queues. Reddit, as I off of RabbitMQ queues. Reddit, as I understand it, also uses it to build understand it, also uses it to build comment threads and calculate karma comment threads and calculate karma scores. So, these are classic task queue scores. So, these are classic task queue patterns. On the other hand, you'll want patterns. On the other hand, you'll want Kafka if you need multiple systems Kafka if you need multiple systems reading the same events, analytics, reading the same events, analytics, fraud detection, billing, and audit fraud detection, billing, and audit logging, all consuming one stream logging, all consuming one stream independently. independently. independently. If you need replay capabilities, If you need replay capabilities, reprocessing historical data to debug reprocessing historical data to debug issues or rebuild state. issues or rebuild state. If you have massive scale, millions of If you have massive scale, millions of events per second with consistent events per second with consistent latency requirements. latency requirements. And if you need a durable event history, And if you need a durable event history, so a permanent record of everything that so a permanent record of everything that ever happened. ever happened. Netflix processes petabytes of data Netflix processes petabytes of data daily through Kafka for recommendations daily through Kafka for recommendations and billing. Uber uses it for real-time and billing. Uber uses it for real-time pricing and fraud detection across pricing and fraud detection across millions of rides. And then LinkedIn millions of rides. And then LinkedIn invented Kafka and still uses it to invented Kafka and still uses it to power their feed and messaging. So, power their feed and messaging. So, these systems need every event stored, these systems need every event stored, and need to be replayable, and are and need to be replayable, and are consumed by dozens of different consumed by dozens of different services. So, if you have a well-defined services. So, if you have a well-defined constrained use case that fits the constrained use case that fits the RabbitMQ model, like a task queue or RabbitMQ model, like a task queue or background jobs, go with RabbitMQ for background jobs, go with RabbitMQ for its simplicity. For everything else, you its simplicity. For everything else, you may need to lean towards Kafka and may need to lean towards Kafka and consider whether or not you want to host consider whether or not you want to host your own or just use a managed service. your own or just use a managed service. Or, of course, you can use both. Kafka Or, of course, you can use both. Kafka is the event backbone, RabbitMQ for the is the event backbone, RabbitMQ for the background jobs that those events background jobs that those events trigger.
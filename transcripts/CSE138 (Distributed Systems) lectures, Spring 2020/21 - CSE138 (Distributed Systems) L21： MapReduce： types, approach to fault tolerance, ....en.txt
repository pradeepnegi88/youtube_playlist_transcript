all right ready to get started sorry all right ready to get started sorry about the delay about the delay uh so today um we're gonna finish up our uh so today um we're gonna finish up our discussion of mapreduce discussion of mapreduce and uh we so we began talking about and uh we so we began talking about mapreduce last time mapreduce last time uh we worked through an example this uh we worked through an example this inverted index example that's kind of a inverted index example that's kind of a canonical canonical canonical example of what you do with mapreduce example of what you do with mapreduce and today we'll talk about some more and today we'll talk about some more examples examples examples and we'll also we'll talk about the and we'll also we'll talk about the mapreduce approach to fault tolerance mapreduce approach to fault tolerance and we'll we'll talk about some and we'll we'll talk about some optimizations we can do on the optimizations we can do on the the basic uh approach that we talked the basic uh approach that we talked about last time about last time uh so um to recap can somebody uh so um to recap can somebody uh remind me from last time what are the uh remind me from last time what are the yeah we got map shape map phase shuffle yeah we got map shape map phase shuffle so let's refresh our memory on what goes on let's refresh our memory on what goes on here let's talk about the map phase here let's talk about the map phase first um first um so this phase is when the programmer so this phase is when the programmer supplied map function supplied map function runs on all the input data can somebody runs on all the input data can somebody tell me tell me what the type of that map function is what the type of that map function is that is that is what's the type of its input and what's what's the type of its input and what's the type of its output the type of its output so these are going to be the types that so these are going to be the types that the the input and the output have the the input and the output have conceptually conceptually conceptually they won't necessarily be the actual they won't necessarily be the actual write feel free to look at the paper if you feel free to look at the paper if you need to refresh your memory on this so need to refresh your memory on this so we're we're we're we're trying to figure out what the type we're trying to figure out what the type of the map function is of the map function is what is its input type and what is its what is its input type and what is its hmm okay so hi hmm okay so hi i didn't think this would be a hard i didn't think this would be a hard question so um so the input question so um so the input to the map function is going to be a to the map function is going to be a single key value pair single key value pair so the input data for the entire so the input data for the entire mapreduce job mapreduce job is going to be a potentially huge set of is going to be a potentially huge set of key value pairs key value pairs and the map function is the code that and the map function is the code that you want to have run on each one of them you want to have run on each one of them some input key value pair the output some input key value pair the output from the map function from the map function is going to be a set of intermediate key is going to be a set of intermediate key right so where do these intermediate key right so where do these intermediate key value pairs that are produced by value pairs that are produced by the map function stored where did they the map function stored where did they end up after end up after right so they are going to be sent to right so they are going to be sent to the reduced workers the reduced workers sooner or later right but initially they sooner or later right but initially they get stored locally get stored locally so they get stored in local storage on so they get stored in local storage on the map workers on the machines where the map workers on the machines where the map function the map function was run um so let's actually look at was run um so let's actually look at some examples of map functions i'll come some examples of map functions i'll come back to this back to this um so last time we talked about creating um so last time we talked about creating an inverted index right an inverted index right so the input to the map function so the input to the map function recall that so this is i'll put this at recall that so this is i'll put this at the top this is the type of the map the top this is the type of the map function function function um so for an inverted index the input um so for an inverted index the input would have been a pair of let's say a would have been a pair of let's say a document identifier document identifier let's say doc 1 and let's say doc 1 and a list of words in it so it would be a list of words in it so it would be something like this something like this so this would have been one input to the so this would have been one input to the map function in the inverted index map function in the inverted index example example example and the output of the map function was and the output of the map function was right it's going to be something that right it's going to be something that would would would look like this so we would be have a look like this so we would be have a mapping from words mapping from words okay so that's what we would do if we okay so that's what we would do if we wanted to do the inverted index wanted to do the inverted index mapreduce is more than just a mapreduce is more than just a distributed fault tolerant framework for distributed fault tolerant framework for computing computing computing inverted indices it's a distributed inverted indices it's a distributed fault tolerant framework for doing lots fault tolerant framework for doing lots of things of things so what if we wanted to do something so what if we wanted to do something else what if we wanted to do a word else what if we wanted to do a word count count count instead what would we want the map instead what would we want the map function to spit out then so let's say function to spit out then so let's say so let's say the input is still of that so let's say the input is still of that shape what would we want the map shape what would we want the map function to produce if we were doing a function to produce if we were doing a so let's say the whole sentence is the so let's say the whole sentence is the quick brown fox jumps over the lazy dog quick brown fox jumps over the lazy dog so there's there's two occurs in so there's there's two occurs in occurrences of the occurrences of the in the sentence right so in the sentence right so one way to do this would be to say well one way to do this would be to say well the the the has to be mapped to the number two but has to be mapped to the number two but another thing you could do is just another thing you could do is just emit a whole bunch of pairs that look emit a whole bunch of pairs that look like this like this the one the one and then later the one and then later the one because all of the occurrences of the because all of the occurrences of the are going to be added together later so are going to be added together later so it's okay to defer that adding until it's okay to defer that adding until after the after the um after this phase it's okay to defer um after this phase it's okay to defer the the the the adding until the reduced phase however adding until the reduced phase however if we felt like adding it here as an if we felt like adding it here as an optimization optimization optimization we could so this this would be a we could so this this would be a completely legal and valid completely legal and valid output of the map function but we could output of the map function but we could also also also have something that did some combination have something that did some combination now are we done now are we done no right because we still have to no right because we still have to combine with word counts that happen combine with word counts that happen on other map workers um but we could do on other map workers um but we could do this this this we could combine as much as possible we could combine as much as possible locally locally locally and we'll come back to talk about that and we'll come back to talk about that in a minute in a minute okay so that's inverted index and word okay so that's inverted index and word count um count um what about uh what about something else what about uh what about something else what about distributed grep what about distributed grep um so let's say that we want to search um so let's say that we want to search for a string um for a string um let's say that we want to search for the let's say that we want to search for the string dog string dog maybe we want all documents that have maybe we want all documents that have the string dog in them the string dog in them what should what should the map function what should what should the map function produce then again assuming that produce then again assuming that the input is uh the input is uh is one of these pairs of document is one of these pairs of document identifier so what would we want our our map so what would we want our our map function to spit out if we were trying function to spit out if we were trying to do this this distributed graph to do this this distributed graph so i grabbed this spelled with a p so i grabbed this spelled with a p um so so in this case um so so in this case uh what we're trying to do is we're uh what we're trying to do is we're trying we're trying to figure out trying we're trying to figure out what documents have a given word in them what documents have a given word in them and and and and maybe we even want the context in and maybe we even want the context in which that word appears which that word appears right so instead of just saying right so instead of just saying oh well no dog occurs in document one oh well no dog occurs in document one we we might want this piece of text that we we might want this piece of text that says lazy dog says lazy dog then maybe the output we would want then maybe the output we would want would be um would be um or it could be the whole document if we or it could be the whole document if we want so it depends what we want right it want so it depends what we want right it depends whether we want depends whether we want uh different results uh uh different results uh for different occurrences of this for different occurrences of this pattern dog or pattern dog or if we want one result for every document if we want one result for every document that has any occurrence as a pattern that has any occurrence as a pattern right right right so if dog occurs at several places in so if dog occurs at several places in the document the document do you want that document to just appear do you want that document to just appear in the results once or do you want in the results once or do you want uh individual occurrences of dog in the uh individual occurrences of dog in the document with their document with the document with their document with the context around them context around them so if the input had been like i love my so if the input had been like i love my dog spot who is the best dog ever dog spot who is the best dog ever something like this so this would be the something like this so this would be the set of intermediate key value pairs that set of intermediate key value pairs that would be produced as the output would be produced as the output okay so that's a bunch of examples of okay so that's a bunch of examples of what happens after the map phase the what happens after the map phase the shuffle phase shuffle phase there yeah so that's when the data yeah so that's when the data from every map worker the result of from every map worker the result of running this map function this set of running this map function this set of intermediate key value pairs intermediate key value pairs that's when that data goes on to the that's when that data goes on to the reduce worker reduce worker so i wrote the appropriate reduced so i wrote the appropriate reduced workers here so how is it decided workers here so how is it decided which intermediate key value pairs are which intermediate key value pairs are going to go on to which going to go on to which yeah so it's going to get decided by yeah so it's going to get decided by what's called the partitioning function what's called the partitioning function which does something like which does something like hash of the key mod r where r hash of the key mod r where r is is the number of the reduced workers is is the number of the reduced workers so interestingly in the paper um they don't interestingly in the paper um they don't actually say actually say that the default partitioning function that the default partitioning function is hash of key is hash of key mod r they say that there is a default mod r they say that there is a default partitioning function partitioning function that's used to distribute data onto the that's used to distribute data onto the appropriate reduced workers appropriate reduced workers and they say that it uses hashing and and they say that it uses hashing and then they say for example then they say for example hash of key mod r but they don't hash of key mod r but they don't actually come out and say that that's actually come out and say that that's actually it actually it so maybe google has some secret so maybe google has some secret partitioning by hash approach that partitioning by hash approach that they're not telling us about they're not telling us about but it's probably something a lot like but it's probably something a lot like hash of key mod hash of key mod r um by the way somebody just asked in r um by the way somebody just asked in chat um chat um what's grep um yeah so what's grep um yeah so um grep is i let me actually look up um grep is i let me actually look up what grupp stands for i don't even know what grupp stands for i don't even know so so so grep is probably best known as a unix grep is probably best known as a unix command line utility command line utility um for searching for it um for searching for it searching for uh searching for uh lines of text that match a regular lines of text that match a regular expression expression expression um so according to google grep stands um so according to google grep stands for global regular expression print for global regular expression print i did not know that until just now um so i did not know that until just now um so greps uh greps uh grips for searching for text that grips for searching for text that matches a regular expression matches a regular expression in this case um we were searching like in this case um we were searching like in this example here in this example here we were searching uh for we were searching uh for documents and uh places in documents documents and uh places in documents that matched this regular expression dog that matched this regular expression dog and we were saying that there was some and we were saying that there was some context around the matched expression context around the matched expression that we wanted to keep that we wanted to keep so depending on what you wanted to do so depending on what you wanted to do you could just ask for you could just ask for you could ask for what are all the you could ask for what are all the documents that contain the string dog documents that contain the string dog or you could ask for what's the context or you could ask for what's the context in the documents that contain the string in the documents that contain the string dogs is obviously something that dogs is obviously something that back to this so we talked about this a back to this so we talked about this a little bit last time but i little bit last time but i want to run over it again so at the end want to run over it again so at the end of the last lecture we talked about why of the last lecture we talked about why why they did this instead of using why they did this instead of using consistent hashing consistent hashing because as we've discussed a number of because as we've discussed a number of times now times now doing hash of key mod r doesn't work doing hash of key mod r doesn't work great as a way to divide up data great as a way to divide up data um if the if the number r ever changes um if the if the number r ever changes so r here is the number so r here is the number of reduced workers that they are more of reduced workers that they are more generally it would just be the number of generally it would just be the number of nodes that you want to partition data nodes that you want to partition data onto onto onto um so this isn't great if r ever changes um so this isn't great if r ever changes because it means that data needs to get because it means that data needs to get moved around moved around um so but why is it okay to do that in um so but why is it okay to do that in we talked about this a little bit last we talked about this a little bit last time but let's let's uh let's just recap time but let's let's uh let's just recap it why is it okay to do this it why is it okay to do this mapreduce so let's think about the context here so so let's think about the context here so the reason why we didn't want to do this the reason why we didn't want to do this in the setting of say in the setting of say dynamo is that if a node added dynamo is that if a node added or what if a node if node joined the or what if a node if node joined the ring or if a node left the ring ring or if a node left the ring either because it failed or maybe you either because it failed or maybe you took it away on purpose because you took it away on purpose because you needed those resources for another needed those resources for another reason reason reason um then um then the place where an existing key hashes the place where an existing key hashes to is going to change to is going to change because hash of key mod 4 is different because hash of key mod 4 is different from hash of key mod 5 let's say from hash of key mod 5 let's say which means that a whole lot of data which means that a whole lot of data would move uh would move uh and unnecessarily so right so we talked and unnecessarily so right so we talked about consistent hashing as a way to about consistent hashing as a way to ensure the minimum of data movement ensure the minimum of data movement when uh nodes left or joined when uh nodes left or joined the the set of of nodes onto which the the set of of nodes onto which you're partitioning data you're partitioning data um so with mapreduce i think the reason um so with mapreduce i think the reason why this is okay why this is okay is because r doesn't change right so is because r doesn't change right so this is this is an offline system not an this is this is an offline system not an online system online system so with consistent hashing we do it so with consistent hashing we do it because we always want to be responding because we always want to be responding to client requests to client requests right whether or not a node dies right whether or not a node dies or whether or not we're adding new nodes or whether or not we're adding new nodes it's not like we can stop it's not like we can stop responding to client requests just responding to client requests just because we need to add a new node to the because we need to add a new node to the system system system so in that situation it's worth so in that situation it's worth taking the time to make adding or losing taking the time to make adding or losing nodes as non-disruptive as possible nodes as non-disruptive as possible but with mapreduce the reduced workers but with mapreduce the reduced workers are not serving client requests right are not serving client requests right is somebody going to come along in the is somebody going to come along in the middle of the mapreduce computation and middle of the mapreduce computation and say say say oh here's another piece of new input oh here's another piece of new input data that you're going to handle data that you're going to handle no there's an assumption that at the no there's an assumption that at the time you begin the computation you time you begin the computation you already have already have all of the input data that you need all of the input data that you need that's why this is batch processing like that's why this is batch processing like we talked about last time we talked about last time um so the mapreduce framework can make a um so the mapreduce framework can make a smart decision smart decision about um about how many about um about how many map workers well it might ask you to map workers well it might ask you to specify like how many map specify like how many map workers and how many reduced workers you workers and how many reduced workers you need uh but it doesn't need uh but it doesn't you you the programmer okay i guess the you you the programmer okay i guess the map reduced framework decides how many map reduced framework decides how many map workers are needed map workers are needed you the programmer are expected to make you the programmer are expected to make an informed decision about how many an informed decision about how many reduced workers you need reduced workers you need and if you chose too few and your job is and if you chose too few and your job is taking longer than you like taking longer than you like too bad better luck next time too bad better luck next time so but the assumption is that since you so but the assumption is that since you have all the input data to begin with have all the input data to begin with you're not going to be adding more map you're not going to be adding more map or reduced workers to the system in the or reduced workers to the system in the middle of a map reduced job middle of a map reduced job in the way that you might have to add in the way that you might have to add more nodes to dynamo more nodes to dynamo to handle uh an increased to handle uh an increased load of requests from clients or just to load of requests from clients or just to store more data um store more data um so so adding more nodes beyond the so so adding more nodes beyond the originally planned number originally planned number is during a job is not really a thing in is during a job is not really a thing in mapreduce mapreduce mapreduce and so that's why this this this number and so that's why this this this number uh uh uh doesn't change we don't have to worry doesn't change we don't have to worry about this number changing during the about this number changing during the job job job data data data so by the way so by the way though what about crashing right what though what about crashing right what about when mapreduce workers crash about when mapreduce workers crash how does the mapreduce framework handle how does the mapreduce framework handle that does anybody remember from the that does anybody remember from the paper yeah so somebody just wrote in chat yeah so somebody just wrote in chat assign its jobs to another machine okay assign its jobs to another machine okay but who does that who but who does that who who is responsible for knowing who is responsible for knowing what work the failed machine was doing what work the failed machine was doing and then assigning its jobs to somebody and then assigning its jobs to somebody else the master else the master right so mapreduce involves a machine right so mapreduce involves a machine called the master and this is actually a called the master and this is actually a very important piece that we left out of very important piece that we left out of the picture that we drew last time the picture that we drew last time so if you consider the picture that we so if you consider the picture that we drew last time where we had some map drew last time where we had some map workers on which some computation took place and on which some computation took place and and we had our shuffle and we had our shuffle so this isn't actually all the machines so this isn't actually all the machines that are involved in that are involved in mapreduce uh there's also going to be a mapreduce uh there's also going to be a master up here job it is to know up here job it is to know who all these other machines are and who all these other machines are and assign work out to them assign work out to them and have some idea of who is alive and and have some idea of who is alive and who is dead who is dead um so the master is actually uh talking um so the master is actually uh talking so the master is responsible for so the master is responsible for and it pings them periodically right to and it pings them periodically right to see if they're still alive see if they're still alive if it doesn't hear back within some if it doesn't hear back within some given amount of time it's going to given amount of time it's going to assume that they've failed assume that they've failed um and the master knows what tasks are um and the master knows what tasks are assigned where assigned where so if a worker fails the master will so if a worker fails the master will reset the the task that that worker was reset the the task that that worker was working on back to unfinished working on back to unfinished and some other worker will have to step and some other worker will have to step in to replace it and be assigned that in to replace it and be assigned that task task task okay so let's let's talk about this okay so let's let's talk about this concept of failure for a second concept of failure for a second so if that happens so if that happens if a map worker fails do you have to if a map worker fails do you have to yes you actually do yes you actually do because why well because it stores its because why well because it stores its output output output remember we say it stores its output remember we say it stores its output those intermediate key value pairs those intermediate key value pairs on its local disk that's that's an on its local disk that's that's an important aspect of the mapreduce important aspect of the mapreduce framework right framework right that it that it just stores those things that it that it just stores those things locally which is cheaper to do than than locally which is cheaper to do than than sending them over a network sending them over a network but because it's just storing those but because it's just storing those things locally things locally if it dies that disk is going to be if it dies that disk is going to be inaccessible if it crashes inaccessible if it crashes so all of the work that it did and so all of the work that it did and stored locally has to be redone stored locally has to be redone so this is a trade-off that mapreduce so this is a trade-off that mapreduce makes the design of mapreduce makes this makes the design of mapreduce makes this trade-off trade-off trade-off they could have said that this map they could have said that this map worker has to store all its intermediate worker has to store all its intermediate data on data on gfs on the distributed file system gfs on the distributed file system um and then if it were stored there then um and then if it were stored there then it would be accessible even if the map it would be accessible even if the map worker died worker died right but that would take more time than right but that would take more time than storing it locally so they considered storing it locally so they considered the pros and cons and they decided the pros and cons and they decided that the right thing to do was to store that the right thing to do was to store data locally data locally and occasionally have to pay the cost of and occasionally have to pay the cost of recomputing that data recomputing that data rather than store it on the distributed rather than store it on the distributed file system and not have to pay that file system and not have to pay that recomputation cost recomputation cost but instead having to pay this upfront but instead having to pay this upfront additional cost of having having to put additional cost of having having to put everything everything everything uh on the distributed file system uh on the distributed file system so distributed systems are full of so distributed systems are full of trade-offs like this right trade-offs like this right and so this is an interesting uh this is and so this is an interesting uh this is an interesting aspect of of map reduce an interesting aspect of of map reduce one of the one of the hallmarks of one of the one of the hallmarks of mapreduce i think mapreduce i think uh is that um it makes the choice uh is that um it makes the choice of um of redoing work of um of redoing work if a map worker fails all its work must if a map worker fails all its work must be redone be redone and and this isn't this is this is not and and this isn't this is this is not necessarily necessarily necessarily the wrong choice right like that sounds the wrong choice right like that sounds like a bad thing written out like that like a bad thing written out like that but the alternative would have been uh but the alternative would have been uh to store to store its work on the distributed file system its work on the distributed file system which would be expensive to do which would be expensive to do and maybe not necessary if if crashes and maybe not necessary if if crashes are rare are rare so in mapreduce they often make the so in mapreduce they often make the choice of redoing work like this that's choice of redoing work like this that's their approach of fault tolerance their approach of fault tolerance approach to fault tolerance remember how approach to fault tolerance remember how we said earlier in the course we said earlier in the course that fault tolerance often involves that fault tolerance often involves making copies of things making copies of things right we said uh replication right we said uh replication of of data right is one manifestation of of of data right is one manifestation of that so creating that so creating replicas is one manifestation of that replicas is one manifestation of that resending messages so that you get resending messages so that you get reliable delivery is another reliable delivery is another manifestation of that making copies for manifestation of that making copies for fault tolerance fault tolerance and another is replication of and another is replication of computation or redoing computation and computation or redoing computation and that's the choice that mapreduce often that's the choice that mapreduce often makes makes makes okay now reduce workers recall that they okay now reduce workers recall that they actually do store their output actually do store their output or whatever it is that whatever or whatever it is that whatever distributed file system is being used distributed file system is being used so for reduced worker dies um so for reduced worker dies um its tasks don't have to be re-executed its tasks don't have to be re-executed that that that output is going to be readily available output is going to be readily available uh uh uh this is kind of an interesting point this is kind of an interesting point though i'll come back to this later though i'll come back to this later okay so we're we've talked about okay so we're we've talked about onto the reduced phase so the data has onto the reduced phase so the data has gotten on to the reduced workers now gotten on to the reduced workers now the reduce function can run we talked the reduce function can run we talked about the type about the type of the map function conceptually of the map function conceptually can somebody tell me what the what the can somebody tell me what the what the conceptually so the reduced workers are they're each so the reduced workers are they're each going to have their own partition of the going to have their own partition of the data right the data that got hashed onto data right the data that got hashed onto them them them so every every one of these intermediate so every every one of these intermediate key value pairs key value pairs that had the same key is going to end up that had the same key is going to end up on the same reduce worker on the same reduce worker right and it's the job of the reduce right and it's the job of the reduce so we could say that the reduce function so we could say that the reduce function takes we could say this we could say takes we could say this we could say that it takes a set that it takes a set with the constraint that all the keys with the constraint that all the keys uh but instead the way that they express uh but instead the way that they express this in the paper is this in the paper is they say that the reduce function takes they say that the reduce function takes one key and a set of values that go with one key and a set of values that go with it it it a a pair of a key a a pair of a key and it produces a set of output values and it produces a set of output values so let's see some examples of that um so so let's see some examples of that um so for the inverted index that we talked for the inverted index that we talked about about about which i dropped on the floor uh so which i dropped on the floor uh so here's inverted index here's inverted index so what's going to be so this was the so what's going to be so this was the type of the map function for inverted type of the map function for inverted index or this was an example index or this was an example of inputs and outputs for the map of inputs and outputs for the map function for inverted index function for inverted index um what would it be for um for the um what would it be for um for the reduce function reduce function um so for the reduce function so this um so for the reduce function so this was this was the was this was the this was for the map function this was for the map function you probably have this in your notes you probably have this in your notes okay so let's talk about this uh maybe okay so let's talk about this uh maybe people are a little bit confused so people are a little bit confused so so these are going to be the so these are going to be the intermediate key value pairs um intermediate key value pairs um recall that what we're trying to recall that what we're trying to construct um construct um is something that will let us look up is something that will let us look up okay what are all the documents that okay what are all the documents that that contain a given word that contain a given word right um so right um so so the input to the reduce function um so the input to the reduce function um is going to be uh uh is going to be uh uh so conceptually right uh it's gonna be so conceptually right uh it's gonna be uh so so this is this is what it would uh so so this is this is what it would look like in reality right it might be look like in reality right it might be like the like the occurred in document one and the occurred in document one and the also occurred in document three uh and also occurred in document three uh and so on so on um and then that would get brought um and then that would get brought together together together but we just said that um but we just said that um uh that we should think of the type of uh that we should think of the type of the reduced function conceptually as the reduced function conceptually as already being this already being this right we should think of it being as a right we should think of it being as a mapping from a single key mapping from a single key uh to a set of values so this uh to a set of values so this conceptually speaking is already the conceptually speaking is already the input of the reduced function input of the reduced function conceptually the reduce function really conceptually the reduce function really doesn't have to do much at all doesn't have to do much at all it just writes its output out to the it just writes its output out to the distributed file system basically distributed file system basically so in practice um you'd pass it an so in practice um you'd pass it an iterator iterator iterator and it would have to iterate through the and it would have to iterate through the values and do something with them values and do something with them right um but the thing it does with them right um but the thing it does with them could be as simple as just writing them could be as simple as just writing them out to gfs out to gfs unchanged okay so what if we were doing unchanged okay so what if we were doing word count word count um what would the input to the reduce um what would the input to the reduce function function function so recall that reduce maps a particular so recall that reduce maps a particular key to a set of values key to a set of values or the the conceptual input type of or the the conceptual input type of reduce reduce reduce um is a a particular key and a set of um is a a particular key and a set of values so so what would it look like for values so so what would it look like for right yeah it would it might be right yeah it would it might be one one one one one one one one one one uh because the maps map function emitted uh because the maps map function emitted um um um the and one every time it saw the word the and one every time it saw the word the the the right um or if it had done this right um or if it had done this optimization maybe you'd have some other optimization maybe you'd have some other numbers in there so you might have like numbers in there so you might have like the the the one two one three three one two one three three etc so what does the reduce function do etc so what does the reduce function do with this with this it just adds them all together so it it just adds them all together so it would output something like would output something like the five and that would be a count the five and that would be a count of every occurrence of the in every of every occurrence of the in every document that is pretty a document that is pretty a pretty boring example i should have done pretty boring example i should have done um but that would be what the output of um but that would be what the output of the the reduce function would look like the the reduce function would look like for for for um for word count so here um for word count so here uh but hopefully you begin to get the uh but hopefully you begin to get the idea what's the input to the reduced function what's the input to the reduced function document one document one so that would be the input to the reduce so that would be the input to the reduce function recall again that reduce function recall again that reduce function maps a particular key to a set function maps a particular key to a set of values of values and then the reduce function would just and then the reduce function would just output its input output its input this is actually pretty common for the this is actually pretty common for the reduced function to not do anything reduced function to not do anything interesting interesting interesting so i find this kind of fascinating so we so i find this kind of fascinating so we said you know in mapreduce said you know in mapreduce um uh um uh we have this communication pattern right we have this communication pattern right uh this all to all communication pattern uh this all to all communication pattern uh where work gets mapped onto the uh where work gets mapped onto the but a whole lot of magic i think happens but a whole lot of magic i think happens in that mapping onto the reduced workers in that mapping onto the reduced workers right right right because since the intermediate key value because since the intermediate key value pairs that are produced by the map pairs that are produced by the map workers workers workers get partitioned on the reduced workers get partitioned on the reduced workers by key by key um that's kind of where all the magic um that's kind of where all the magic happens and if happens and if if the if the job of the map like if the if the if the job of the map like if the if the work that you're trying to do if the work that you're trying to do largely consists of uh splitting these largely consists of uh splitting these things up by key things up by key then by the time you get done shuffling then by the time you get done shuffling them on to the correct reduced workers them on to the correct reduced workers uh you've already done that so it's uh you've already done that so it's actually pretty common for the reduce actually pretty common for the reduce function itself to not have to do much function itself to not have to do much interesting all of the magic happens interesting all of the magic happens in this shuffle phase where all of these in this shuffle phase where all of these key value pairs get collated by key key value pairs get collated by key questions about this questions about this somebody asked me to look at this again somebody asked me to look at this again so so so yeah for the for the word count example yeah for the for the word count example um um um so we're saying so our input is these uh so we're saying so our input is these uh these pairs of documents in words right these pairs of documents in words right on the map function the map function is on the map function the map function is going to produce going to produce either just this list of pairs where either just this list of pairs where it's every word that was in the input it's every word that was in the input and the number one and the number one or if it's doing a clever optimization or if it's doing a clever optimization it might do some it might do some combination of of the values combination of of the values so if a word occurs more than once then so if a word occurs more than once then then the count over here would be the then the count over here would be the number number number of times that it was that it counted of times that it was that it counted locally um locally um so then conceptually the input to the so then conceptually the input to the reduce function so a given reduced reduce function so a given reduced worker is going to deal worker is going to deal uh with all of um all of these uh with all of um all of these intermediate key value pairs that um intermediate key value pairs that um that had a particular key let's say it's that had a particular key let's say it's the word the or the word dog or whatever the word the or the word dog or whatever um so conceptually the input to the um so conceptually the input to the reduce function is going to be reduce function is going to be that key and then everything that was that key and then everything that was paired up with that key whether it's paired up with that key whether it's just number ones if you didn't do the just number ones if you didn't do the optimization or maybe it's some other optimization or maybe it's some other numbers if you did numbers if you did um so um so um so um so so yeah so so there there would have so yeah so so there there would have been many documents been many documents actually and so this might have been the actually and so this might have been the result from this one result from this one particular map worker that was dealing particular map worker that was dealing with document one a different map worker with document one a different map worker would have dealt with document two would have dealt with document two maybe in a different one would have maybe in a different one would have dealt with document three um dealt with document three um but all of those uh any occurrences of but all of those uh any occurrences of the same key the word the the same key the word the or the word dog or whatever would get or the word dog or whatever would get sent to the same map worker sent to the same map worker and so um that that happens and so um that that happens as part of the magic of the of the as part of the magic of the of the shuffle phase so shuffle phase so um which i don't really mean to describe um which i don't really mean to describe as magic it's really just hashing as magic it's really just hashing um but we called it a communication um but we called it a communication pattern pattern pattern but it's actually uh it important to the but it's actually uh it important to the computation as well computation as well uh because it causes those um uh uh because it causes those um uh those those records that had the same those those records that had the same key to get collated together in the same key to get collated together in the same place so that now we can easily add place so that now we can easily add um so somebody just asked on on twitch um so somebody just asked on on twitch who's responsible for transforming who's responsible for transforming the output of the map function to the the output of the map function to the input of the reduce function okay input of the reduce function okay so yeah so the output of the map so yeah so the output of the map function you you might ask function you you might ask like uh if this is what the output of like uh if this is what the output of the the the of the of the map function looks like of the of the map function looks like which is these intermediate key value which is these intermediate key value pairs pairs pairs but the input to the reduce function is but the input to the reduce function is supposed to look like this supposed to look like this um who's responsible for turning this um who's responsible for turning this into this so into this so um let me let me say again this is the um let me let me say again this is the conceptual type of the input to the conceptual type of the input to the reduce function reduce function in practice um what you would get is in practice um what you would get is a key and then like some sort of a key and then like some sort of iterator that would iterate over iterator that would iterate over over these values um so over these values um so it's so all the data is there on the it's so all the data is there on the reduced worker because it gets sent reduced worker because it gets sent there there there by the shuffle phase um and so by the shuffle phase um and so um in in principle um in in principle this is the the type of the thing that this is the the type of the thing that you have um you have um whether there's actually something uh whether there's actually something uh in like not even like whether or not in like not even like whether or not there's anything there's anything in memory letter let alone serialized in memory letter let alone serialized out to disk that actually looks like out to disk that actually looks like this is is another question this is is another question um there probably isn't uh but um there probably isn't uh but conceptually speaking this is this is conceptually speaking this is this is the type of the input of the of the the type of the input of the of the reduce so i'm glad we've been talking about so i'm glad we've been talking about word count here because this this kind word count here because this this kind of gives us an of gives us an in to talk about this um this particular in to talk about this um this particular optimization optimization optimization that's interesting so this word count that's interesting so this word count example is really screaming out for for example is really screaming out for for this optimization this optimization right where different keys um right where different keys um or where where two keys that are the or where where two keys that are the same uh same uh get combined right so get combined right so let's say that i'm a map worker let's say that i'm a map worker and i have to count occurrences of the and i have to count occurrences of the word dog word dog so we'll say that maybe the document so we'll say that maybe the document that i'm working it with that i'm working it with is like my dog is like my dog spot is the best dog is the best dog so if i use the naive version of the so if i use the naive version of the word count map function that appears in word count map function that appears in the paper the paper then it would emit these then it would emit these and then during the shuffle phase all of and then during the shuffle phase all of those would end up getting sent to the those would end up getting sent to the same reduced worker because they have same reduced worker because they have the same key the same key and then added together on the reduced and then added together on the reduced worker worker worker the trouble with doing it that way is the trouble with doing it that way is that it wastes bandwidth that it wastes bandwidth right so they could have just been added right so they could have just been added together together together on the map workers side and then the map on the map workers side and then the map worker could have just been sent worker could have just been sent or could have just sent this over to the or could have just sent this over to the reduced worker reduced worker and then that would be added together and then that would be added together with every other map workers count for with every other map workers count for the word dog the word dog so the reduced worker would still have so the reduced worker would still have to do addition right to do addition right but they would be adding together these but they would be adding together these uh um these these subtotals uh um these these subtotals right from all of the map workers right from all of the map workers instead of just adding up a bunch of instead of just adding up a bunch of ones ones ones so doing this on the map worker so doing this on the map worker um doing doing as much combination um doing doing as much combination of the intermediate key value pairs of the intermediate key value pairs locally on the map worker locally on the map worker and a combined function is actually just and a combined function is actually just the same as a reduce function the same as a reduce function except that it runs on the map worker except that it runs on the map worker advance why is it safe for us to take some of why is it safe for us to take some of the work that the reduced worker the work that the reduced worker normally would have done and do it early normally would have done and do it early on the map worker why on the map worker why in the case of this word count in the case of this word count yeah so somebody just said in chat yeah so somebody just said in chat because it has no effect on the rest of because it has no effect on the rest of the the the the distributed result okay sure okay the distributed result okay sure okay why does it have no effect um so why does it have no effect um so i would say the reason why this is okay i would say the reason why this is okay to do to do um is because addition is associative um is because addition is associative right that is if you add a plus b right that is if you add a plus b and then you add c to that then that's and then you add c to that then that's going to be the same as going to be the same as adding a to the result of adding b plus adding a to the result of adding b plus c it's the associative property of it's the associative property of addition addition addition if the reduce operation that you're if the reduce operation that you're doing is associative doing is associative then it's okay to do as much as you can then it's okay to do as much as you can early in other words locally on the map early in other words locally on the map workers workers workers and the reduced workers will still have and the reduced workers will still have work to do right work to do right they'll still have work to do but not as they'll still have work to do but not as much data will have to be sent during much data will have to be sent during the shuffle phase the shuffle phase so instead of sending um the pair so instead of sending um the pair dog one a thousand times instead you can dog one a thousand times instead you can just send dog just send dog and 1000 to the relevant reduced worker and 1000 to the relevant reduced worker and the reduced worker will then add and the reduced worker will then add that number 1000 together with the that number 1000 together with the totals that it got totals that it got from the other map workers for dog to from the other map workers for dog to get the final result get the final result but it's just fine to to do this but it's just fine to to do this edition early uh because it doesn't make edition early uh because it doesn't make a difference a difference in the in in the output in the in in the output now it might not necessarily be the case now it might not necessarily be the case that every that every um that every reduced function um that every reduced function uh is associative this way and so if uh is associative this way and so if that's true that's true then then you can't do this optimization then then you can't do this optimization but for something like word count you but for something like word count you all right let's uh talk about the quiz all right let's uh talk about the quiz question from last time and then do a question from last time and then do a yeah so here was here was last time's yeah so here was here was last time's quiz uh quiz uh so we asked uh what happens during the so we asked uh what happens during the shuffle phase of map of a map reduce shuffle phase of map of a map reduce computation so computation so the answer we were looking for here was the answer we were looking for here was this was the place where this was the place where the intermediate key value pairs that the intermediate key value pairs that were produced locally on the map workers were produced locally on the map workers get sent to the appropriate reduce get sent to the appropriate reduce worker via some sort of partitioning worker via some sort of partitioning function function function such as hash of key mod r where r is the such as hash of key mod r where r is the number of reduced workers number of reduced workers that's what happens during the shuffle that's what happens during the shuffle phase that's when data gets sent to the phase that's when data gets sent to the appropriate reduced worker appropriate reduced worker yes somebody just asked a question um yes somebody just asked a question um in chat which was can the reduce in chat which was can the reduce function output a set instead of a function output a set instead of a single value single value uh this was just asked on twitch um so uh this was just asked on twitch um so we were talking a second ago about about we were talking a second ago about about the um the types of map and reduce the um the types of map and reduce in the map reduce framework um so yes we in the map reduce framework um so yes we do do do think of the the the output of the think of the the the output of the reduce function as reduce function as as being a set this is if you're as being a set this is if you're familiar with uh familiar with uh the notions of map and reduced from the notions of map and reduced from functional programming uh functional programming uh then then this might sound a little then then this might sound a little strange right uh strange right uh so i actually want to after a minute so i actually want to after a minute when we get a couple more quiz responses when we get a couple more quiz responses um i want to talk about the difference um i want to talk about the difference between between between uh these types in mapreduce and uh these types in mapreduce and what we normally think of when we're what we normally think of when we're so i'll give it like another minute or so i'll give it like another minute or two here two here somebody else asked in in slack or in somebody else asked in in slack or in twitch chat uh twitch chat uh if there were no combined function then if there were no combined function then uh would uh would the would the map worker send this pair the would the map worker send this pair of dog and the number one three times to of dog and the number one three times to the reduced worker the reduced worker yeah it would uh so that's yeah it would uh so that's that's one of the things that a combined that's one of the things that a combined function could do would be to do that function could do would be to do that all right it's a pretty good response all right it's a pretty good response rate uh rate uh yeah so the question that was just asked yeah so the question that was just asked was kind of anticipating the very last was kind of anticipating the very last thing that i want to talk about thing that i want to talk about in mapreduce these in mapreduce these are the the types of the map and reduce are the the types of the map and reduce function function function at least conceptually uh so at least conceptually uh so the question that was asked was uh does the question that was asked was uh does reduce reduce produce one value or a set reduce reduce produce one value or a set of output values well in mapreduce of output values well in mapreduce it produces a set which might look a it produces a set which might look a little bit strange uh little bit strange uh if you're uh if what you're used to is if you're uh if what you're used to is uh like the traditional uh uh like the traditional uh definition of reduced from functional definition of reduced from functional programming so let's talk about the programming so let's talk about the traditional definitions of map and traditional definitions of map and reduce uh just briefly i'm a i'm a reduce uh just briefly i'm a i'm a programming languages person so when programming languages person so when talking about mapreduce i would be talking about mapreduce i would be remiss if i didn't spend a little bit of remiss if i didn't spend a little bit of time talking about this time talking about this okay so so what is what's the type of a okay so so what is what's the type of a map function map function uh traditionally uh traditionally so a map function is something that so a map function is something that takes a function takes a function and a list and it applies that function and a list and it applies that function to everything in the list to everything in the list so uh if we were so uh if we were writing this down in haskell we would writing this down in haskell we would say say say and we're not uh even without writing and we're not uh even without writing down the definition of map we can kind down the definition of map we can kind of see what's going on here right if we of see what's going on here right if we have a if we have a list of have a if we have a list of of a's you know whatever they are of a's you know whatever they are numbers whatever numbers whatever um and then we have a function that um and then we have a function that takes things of that type and turns them takes things of that type and turns them into another type into another type then we can take everything in that list then we can take everything in that list apply the function to it and apply the function to it and get a list of the output type um get a list of the output type um so in haskell it would look like this so in haskell it would look like this you would write the implementation would you would write the implementation would look like this look like this one possibility uh is that the list uh one possibility uh is that the list uh here is empty here is empty right um so if this list is empty right um so if this list is empty right the output list is also going to right the output list is also going to be empty so in that case we don't care be empty so in that case we don't care what the what the function is and so you what the what the function is and so you would you typically write in haskell you would you typically write in haskell you would write an underscore to show that would write an underscore to show that you don't care about that argument you don't care about that argument so in the case where the so in the case where the the input list is empty this is an empty the input list is empty this is an empty list list list in haskell then the output list is also in haskell then the output list is also empty that's that case that's the boring empty that's that case that's the boring case or the case or the or the base case um and in the other or the base case um and in the other case um you have some function f um you have some function f which has a head and a tail so it has a which has a head and a tail so it has a first element which we'll call first element which we'll call x and then the rest of its element uh x and then the rest of its element uh which we call x's which we call x's and then what you do is you create a new and then what you do is you create a new list list list that just applies that function f to that just applies that function f to x and that's the first thing in the list x and that's the first thing in the list and then you you stick that on to and then you you stick that on to on the rest of the list again passing in on the rest of the list again passing in the function the function so this will be a recursive call this so this will be a recursive call this will go and eventually will go and eventually uh you'll get to the base case uh and uh you'll get to the base case uh and then you'll be done then you'll be done so for example if you were to call map so for example if you were to call map um of oh let's say um of oh let's say um increment um increment and the and the the input list was one and the and the the input list was one two three two three um then the output would be two three um then the output would be two three four so that's an example um what about so that's an example um what about reduce reduce reduce reduce uh reduce uh is going to take a function that's going is going to take a function that's going to take to take it's going to take a value of type b it's going to take a value of type b it's going to take a list of values of it's going to take a list of values of type a type a and it's going to take and it's going to and it's going to take and it's going to return a value of type b so notice that return a value of type b so notice that it returns a single value not a list it returns a single value not a list um so what does this do well um so what does this do well if you have a list of a's and if you if you have a list of a's and if you have a function have a function that takes an a and a b and produces a b that takes an a and a b and produces a b how are you going to run that function how are you going to run that function well you need a b well you need a b thankfully you have one right here um thankfully you have one right here um so this is your b um so that b is going so this is your b um so that b is going to be to be uh is going to be useful this is what we uh is going to be useful this is what we call the call the um sometimes you call you might call um sometimes you call you might call this like the base this like the base value so the implementation would look like so the implementation would look like this [Music] [Music] [Music] if you're reducing and this input list a if you're reducing and this input list a is empty is empty just like we saw with map if the input just like we saw with map if the input list is empty list is empty uh then it's easy right um so uh then it's easy right um so if this input list is empty you can't if this input list is empty you can't run this this function right because you run this this function right because you don't have anything of type a because don't have anything of type a because the list is empty the list is empty um you were supposed to produce a b oh um you were supposed to produce a b oh look you have a b look you have a b so you can just give back that b that so you can just give back that b that you got so you got so we don't care what this function is um we don't care what this function is um we have our base value or our identity we have our base value or our identity value or sometimes called our zero value or sometimes called our zero value so often often traditionally value so often often traditionally called z and the list here is empty called z and the list here is empty and then this is z so that's the base and then this is z so that's the base case here's the recursive case so reduce here's the recursive case so reduce takes takes takes a function that's this function here a function that's this function here from a to b from a to b uh from a to b to b um uh from a to b to b um we have our zero value z and we have our we have our zero value z and we have our list which is not empty list which is not empty has a first element x and then the rest has a first element x and then the rest what do we do well we're trying to what do we do well we're trying to create this b create this b right so what we can do is we can take right so what we can do is we can take our function our function and then we need another argument which and then we need another argument which is of type b is of type b how do we get a thing of type b with a how do we get a thing of type b with a so that's what the implementation of so that's what the implementation of reduce looks like none of this is reduce looks like none of this is necessarily relevant to the course uh i necessarily relevant to the course uh i just felt as though just felt as though i would be remiss if i didn't talk about i would be remiss if i didn't talk about this when this when as a programming languages person when as a programming languages person when i'm teaching mapreduce i should talk i'm teaching mapreduce i should talk about about about what mapreduce means to me as a pl what mapreduce means to me as a pl person person person um so so an example of this um so so an example of this might be well we talked about about word might be well we talked about about word count right count right um so um word count was an example of um so um word count was an example of adding together a bunch of numbers so adding together a bunch of numbers so you might do that with reduce you might do that with reduce right so so if your reduce was like right so so if your reduce was like let's see if i can put together an let's see if i can put together an example quickly here example quickly here so um so you might say reduce so um so you might say reduce um and then the function you might pass um and then the function you might pass in here might be like in here might be like add and this is like let's say like a add and this is like let's say like a binary add function that takes two binary add function that takes two numbers numbers numbers um the base value that you might pass in um the base value that you might pass in and then the list of numbers that you and then the list of numbers that you would pass in let's say it's like would pass in let's say it's like one one two one one one two one something like what we had in our word something like what we had in our word count um so what's going to happen count um so what's going to happen uh reduce is going to go through and add uh reduce is going to go through and add those things up those things up which was the same type as this which which was the same type as this which was what it was supposed to be was what it was supposed to be all right there's your brief all right there's your brief introduction to haskell for the day introduction to haskell for the day um that's all i have to talk about um um that's all i have to talk about um i think we're through with our i think we're through with our discussion of mapreduce we don't have discussion of mapreduce we don't have class class class on friday because friday is our on friday because friday is our hack day uh i have office hours after hack day uh i have office hours after this so come talk to me if you have this so come talk to me if you have questions questions questions and monday is a holiday so i will see and monday is a holiday so i will see you again you again
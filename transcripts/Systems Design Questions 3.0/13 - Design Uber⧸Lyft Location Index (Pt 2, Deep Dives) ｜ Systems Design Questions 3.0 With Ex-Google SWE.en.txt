Hey everyone and welcome back to the Hey everyone and welcome back to the channel. Uh I've got to do a pretty channel. Uh I've got to do a pretty quick one today uh because I'm going to quick one today uh because I'm going to run into work in a second, but uh you run into work in a second, but uh you may notice that my mustache is missing, may notice that my mustache is missing, which is tragic because as we all know, which is tragic because as we all know, it looked extremely sick and cool and uh it looked extremely sick and cool and uh I've been forced to shave it by the I've been forced to shave it by the forces that be. Um but yeah, just going forces that be. Um but yeah, just going to have to go ahead and uh move on with to have to go ahead and uh move on with life and stay strong. So let's get into life and stay strong. So let's get into this video. Today we'll be building out this video. Today we'll be building out Uber's location index. As a recap from Uber's location index. As a recap from the last video, Uber uses their location the last video, Uber uses their location index to match riders with nearby index to match riders with nearby drivers. Drivers are constantly updating drivers. Drivers are constantly updating their location in the index so that when their location in the index so that when passengers create a ride request, Uber passengers create a ride request, Uber servers can very efficiently figure out servers can very efficiently figure out a set of drivers to notify. Let's a set of drivers to notify. Let's formalize the APIs that the Uber formalize the APIs that the Uber location service needs to satisfy. location service needs to satisfy. First, drivers should be able to update First, drivers should be able to update their location in the index. They can do their location in the index. They can do this by calling the ping endpoint. Next, this by calling the ping endpoint. Next, riders should be able to quickly compute riders should be able to quickly compute the set of nearest drivers within a the set of nearest drivers within a distance threshold. This distance distance threshold. This distance threshold is configurable. As we threshold is configurable. As we discussed in the last video, let's discussed in the last video, let's estimate that across our index, we'll estimate that across our index, we'll receive a million location updates per receive a million location updates per second from each driver. My sources second from each driver. My sources online tell me that Uber coordinates online tell me that Uber coordinates over 30 million trips daily. If we over 30 million trips daily. If we convert that to a per second metric and convert that to a per second metric and assume a uniform distribution, we'll assume a uniform distribution, we'll also be servicing 350 queries per second also be servicing 350 queries per second to find all the nearest drivers for our to find all the nearest drivers for our rider. The number of driver pings in rider. The number of driver pings in particular here is quite large. As we particular here is quite large. As we discussed in the last video, we need to discussed in the last video, we need to place each one in a geospatial index. place each one in a geospatial index. This allows us to efficiently compute This allows us to efficiently compute all drivers within a distance of a all drivers within a distance of a target point. I'm not sure whether there target point. I'm not sure whether there are any databases that can handle a are any databases that can handle a million index writes per second, million index writes per second, especially when you toss some reads in especially when you toss some reads in there, too. Ultimately, we need a way of there, too. Ultimately, we need a way of scaling the system out across many scaling the system out across many nodes. There are a bunch of different nodes. There are a bunch of different ways of sharding out a database, all ways of sharding out a database, all with their own trade-offs. In the with their own trade-offs. In the following approaches that we'll discuss, following approaches that we'll discuss, we'll roundroin driver pings to each we'll roundroin driver pings to each shard to maintain an even load on each shard to maintain an even load on each server. That sounds pretty promising on server. That sounds pretty promising on paper, not much unlike my ex-girlfriend. paper, not much unlike my ex-girlfriend. Let's see what happens in practice, Let's see what happens in practice, though. Let's imagine that when a rider though. Let's imagine that when a rider wants to find the closest drivers, they wants to find the closest drivers, they pick a random shard and look at who is pick a random shard and look at who is closest to them. As we can see in the closest to them. As we can see in the diagram, we may end up matching our diagram, we may end up matching our rider with a non-optimal driver. To rider with a non-optimal driver. To mitigate this, our rider could instead mitigate this, our rider could instead read from all shards and aggregate the read from all shards and aggregate the results. In this situation, our query is results. In this situation, our query is going to be bottlenecked by the latency going to be bottlenecked by the latency of the slowest shard. When thinking of the slowest shard. When thinking about how to shard out a database, we about how to shard out a database, we want to consider our querying patterns. want to consider our querying patterns. If possible, the best way to establish If possible, the best way to establish partitions is to do so in a way that we partitions is to do so in a way that we only have to interact with a single only have to interact with a single shard at a time. In our case, we only shard at a time. In our case, we only care about having to be able to find all care about having to be able to find all points within a certain distance of some points within a certain distance of some potential passenger. Implicitly, that potential passenger. Implicitly, that means that we want all drivers that are means that we want all drivers that are close proximity to one another to live close proximity to one another to live on the same partition. This is going to on the same partition. This is going to be our guiding approach. Let's introduce be our guiding approach. Let's introduce a new technology out of Uber, which is a new technology out of Uber, which is called H3. H3 maps out the entire globe called H3. H3 maps out the entire globe into a bunch of fixed size hexagons and into a bunch of fixed size hexagons and some pentagons. and in many ways some pentagons. and in many ways resembles a quad tree except each cell resembles a quad tree except each cell has seven children instead of four. H3 has seven children instead of four. H3 is really useful for performing is really useful for performing bucketing of geographic locations which bucketing of geographic locations which is exactly what we want to be doing is exactly what we want to be doing here. Each one of these hexagons has a here. Each one of these hexagons has a unique cell ID. In order to get the cell unique cell ID. In order to get the cell ID for a given point, H3 provides really ID for a given point, H3 provides really easy to use utilities. All you need to easy to use utilities. All you need to provide is a latitude and longitude and provide is a latitude and longitude and something known as a resolution, which something known as a resolution, which we'll explain now. As we saw in our last we'll explain now. As we saw in our last picture, a driver's location can picture, a driver's location can actually be contained in many different actually be contained in many different cells. This is because bigger cells, cells. This is because bigger cells, which are known as lower resolution, which are known as lower resolution, actually contain smaller cells at higher actually contain smaller cells at higher resolution. H3 has preset areas for all resolution. H3 has preset areas for all cells of a given resolution, which I've cells of a given resolution, which I've attached on screen. attached on screen. This is pretty great, right? We can just This is pretty great, right? We can just pick a resolution that we think is good pick a resolution that we think is good enough for us, get the H3 cell for a enough for us, get the H3 cell for a latitude and a longitude, and use that latitude and a longitude, and use that as our shard key. We don't want to pick as our shard key. We don't want to pick one that's too small since otherwise one that's too small since otherwise we're going to have a ton of partitions we're going to have a ton of partitions which is lots of extra overhead on our which is lots of extra overhead on our systems. Let's say that we choose systems. Let's say that we choose resolution 2 giving us 6,000 clean resolution 2 giving us 6,000 clean shards of 90,000 km. This seems really shards of 90,000 km. This seems really good at first but could break down in good at first but could break down in practice. Those zones are pretty big and practice. Those zones are pretty big and we know that in reality Uber has hot we know that in reality Uber has hot spots. For example, Uber is probably spots. For example, Uber is probably serving a lot more rides in New York, serving a lot more rides in New York, London, and Paris than they do in London, and Paris than they do in Topeka, Kansas. While we still want to Topeka, Kansas. While we still want to use the cell ID as our partition key, in use the cell ID as our partition key, in some places we may want to use a cell some places we may want to use a cell key at a higher resolution. For example, key at a higher resolution. For example, in the eastern United States, we may in the eastern United States, we may want to use resolution 6, which is 36 km want to use resolution 6, which is 36 km per shard. How can we accomplish this? per shard. How can we accomplish this? When a driver sends their latitude and When a driver sends their latitude and longitude to a server, the server needs longitude to a server, the server needs to figure out the resolution they should to figure out the resolution they should use for the H3 cell. In order to do use for the H3 cell. In order to do this, we can publish a mapping of all this, we can publish a mapping of all the H3 cells that form distinct the H3 cells that form distinct partitions. We can just keep a Postgress partitions. We can just keep a Postgress table known as H3 shards which lists all table known as H3 shards which lists all the cell ids. If a system administrator the cell ids. If a system administrator notices that one particular shard is notices that one particular shard is experiencing a lot of load, they can experiencing a lot of load, they can remove the H3 shard from this table and remove the H3 shard from this table and atomically add entries for all seven of atomically add entries for all seven of the children. This way any subsequent the children. This way any subsequent pings in the cell will be distributed pings in the cell will be distributed across the child cells which should live across the child cells which should live on different physical nodes. When a on different physical nodes. When a driver or a rider needs to figure out driver or a rider needs to figure out what H3 cell ID it should use, it can what H3 cell ID it should use, it can start by computing the largest H3 cell start by computing the largest H3 cell that it's a member of. If that's one of that it's a member of. If that's one of our partitions, then great. We can stop our partitions, then great. We can stop there and just use it. Otherwise, we there and just use it. Otherwise, we should compute the H3 cell at the next should compute the H3 cell at the next resolution until we find one in the H3 resolution until we find one in the H3 shard index. Then we can employ the shard index. Then we can employ the shard to figure out where to route the shard to figure out where to route the query to at a million pings per second. query to at a million pings per second. I'm going to be super conservative and I'm going to be super conservative and say we'll never need more than 100,000 say we'll never need more than 100,000 shards. Rather than having to load all shards. Rather than having to load all of the H3 partition keys from the of the H3 partition keys from the database all the time, we can cache this database all the time, we can cache this in a hash set on each server. Every in a hash set on each server. Every minute or so, the servers can pull that minute or so, the servers can pull that database in order to refresh the list in database in order to refresh the list in the background. As a result of this the background. As a result of this caching, one situation that could happen caching, one situation that could happen here is that servers may route a driver here is that servers may route a driver ping to the wrong shard. If a partition ping to the wrong shard. If a partition is split into many, there's a short is split into many, there's a short period of time where a location ping may period of time where a location ping may be sent to its old shard. At the end of be sent to its old shard. At the end of the day, this isn't a big deal. Recall the day, this isn't a big deal. Recall that for each driver ping, we that for each driver ping, we automatically expire it after a few automatically expire it after a few seconds. This means that any rows that seconds. This means that any rows that are created on the wrong shard will just are created on the wrong shard will just go away. And after a bit longer, our go away. And after a bit longer, our server will get an updated list of valid server will get an updated list of valid H3 cells that we can use as our sharding H3 cells that we can use as our sharding keys. When it comes to servicing keys. When it comes to servicing passenger requests for rides, we want passenger requests for rides, we want proximity queries to go to the right proximity queries to go to the right place. In that case, we can actually place. In that case, we can actually read from the database instead of our read from the database instead of our local cache. These aren't nearly as local cache. These aren't nearly as frequent though, so it's really not a frequent though, so it's really not a big deal. Let's bring together the big deal. Let's bring together the highle design. We'll start by just highle design. We'll start by just establishing the infrastructure here. establishing the infrastructure here. Our main location index is a postgress Our main location index is a postgress table using the postgis extension. This table using the postgis extension. This allows us to maintain a geospatial index allows us to maintain a geospatial index per shard which is exactly what we need per shard which is exactly what we need here. We can use situs to scale this out here. We can use situs to scale this out over a large cluster of Postgress nodes over a large cluster of Postgress nodes and use the H3 cell ID column as our and use the H3 cell ID column as our partition key. Hash range partitioning partition key. Hash range partitioning over this column should be good enough over this column should be good enough here. We'll also have a table to list here. We'll also have a table to list our current valid H3 buckets. our current valid H3 buckets. administrators can come along and change administrators can come along and change the list to either split a cell into its the list to either split a cell into its children or take a bunch of child cells children or take a bunch of child cells and replace them with their parent to and replace them with their parent to change how our tables are partitioned. change how our tables are partitioned. When it comes to handling pings from a When it comes to handling pings from a driver, the first thing that we can do driver, the first thing that we can do is get our H3 cell ID for the latitude is get our H3 cell ID for the latitude and longitude at resolution zero. If and longitude at resolution zero. If that's in the H3 shard table, that's that's in the H3 shard table, that's great. If not, we keep traversing down great. If not, we keep traversing down until we find a cell ID that is. Keep in until we find a cell ID that is. Keep in mind that ideally the entries in the mind that ideally the entries in the shard table are already cached on our shard table are already cached on our server to minimize the number of network server to minimize the number of network calls to make here. We then upload a row calls to make here. We then upload a row to our Postgress table with the to our Postgress table with the appropriate H3 cell ID, geohash, and appropriate H3 cell ID, geohash, and driver ID and Citus takes care of driver ID and Citus takes care of routing it to the proper shard for us. routing it to the proper shard for us. When finding nearby drivers for our ride When finding nearby drivers for our ride request, the first thing that we need to request, the first thing that we need to do is figure out the shard key for the do is figure out the shard key for the rider's latitude and longitude in the rider's latitude and longitude in the same way that we did for our driver. same way that we did for our driver. However, this step is a little bit more However, this step is a little bit more complex. If I'm a rider and I'm right on complex. If I'm a rider and I'm right on the edge of my H3 shard boundary, I the edge of my H3 shard boundary, I won't consider any drivers that are won't consider any drivers that are right across that edge. This means that right across that edge. This means that we'd potentially find a sub-optimal we'd potentially find a sub-optimal driver. Recall that we're searching for driver. Recall that we're searching for all drivers within a distance of x miles all drivers within a distance of x miles to a rider. We can basically outline the to a rider. We can basically outline the circle on the map. We can figure out a circle on the map. We can figure out a few latitude and longitude points on the few latitude and longitude points on the edge of that circle. For example, x edge of that circle. For example, x miles north, south, east, and west. This miles north, south, east, and west. This is really doable with basic math. From is really doable with basic math. From there, we can figure out the shards that there, we can figure out the shards that all of those points belong to. The all of those points belong to. The majority of the time, they should be the majority of the time, they should be the same as our writers location, but some same as our writers location, but some of the points may require checking other of the points may require checking other cells. Finally, we issue a query to the cells. Finally, we issue a query to the appropriate shards in our location appropriate shards in our location index, which utilize the local index, which utilize the local geospatial index per shard. Since we geospatial index per shard. Since we specify which H3 shards were searching specify which H3 shards were searching in our query, Citus will route it to in our query, Citus will route it to only the appropriate nodes as opposed to only the appropriate nodes as opposed to all of them. In this video, I mainly all of them. In this video, I mainly used H3 because it abstracts away from used H3 because it abstracts away from any difficulty with trying to bucket any difficulty with trying to bucket similar locations to one another. That similar locations to one another. That being said, some of you may recall from being said, some of you may recall from prior videos that geohash values that prior videos that geohash values that are in close proximity to one another are in close proximity to one another are actually lexographically similar. are actually lexographically similar. What this means is that we could also What this means is that we could also range partition our geospatial index by range partition our geospatial index by geohash range. While this works nicely geohash range. While this works nicely in theory, in practice it becomes a in theory, in practice it becomes a little bit tough to actually deal with. little bit tough to actually deal with. we'd have to be careful to manually we'd have to be careful to manually reconfigure our ranges for every single reconfigure our ranges for every single shard as opposed to our existing shard as opposed to our existing solution which is basically just split solution which is basically just split the big shard and evenly redistribute the big shard and evenly redistribute its children to other nodes in the its children to other nodes in the cluster. Thanks everyone for stopping cluster. Thanks everyone for stopping by. I hope you enjoyed the video. Uh I by. I hope you enjoyed the video. Uh I think this is a cool one to actually think this is a cool one to actually discuss geos sharding a little bit. Uh discuss geos sharding a little bit. Uh in the next one I think we'll talk about in the next one I think we'll talk about sharding out a search index since I sharding out a search index since I didn't really talk about that too much didn't really talk about that too much for the sake of the Yelp video. And uh for the sake of the Yelp video. And uh yeah, looking forward to it. See y'all yeah, looking forward to it. See y'all soon.
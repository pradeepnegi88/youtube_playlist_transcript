Hey everybody, welcome back to the Hey everybody, welcome back to the channel. Uh, for those of you who are channel. Uh, for those of you who are new here, I'm Evan. I'm a former new here, I'm Evan. I'm a former metastaf engineer and I'm the current metastaf engineer and I'm the current co-founder of hello.com. co-founder of hello.com. If you are preparing for an upcoming If you are preparing for an upcoming software engineering interview, then software engineering interview, then you'll want to come over to Hello you'll want to come over to Hello Interview. What you see here, we have Interview. What you see here, we have everything that you need to prepare for everything that you need to prepare for your upcoming interviews across all of your upcoming interviews across all of the major interview types. Today's the major interview types. Today's video, we're going to be focusing on video, we're going to be focusing on low-level design. And specifically, low-level design. And specifically, we're going to be focused on what is we're going to be focused on what is oftentimes the most tricky part of oftentimes the most tricky part of low-level design interviews, which is low-level design interviews, which is concurrency. And so I'm going to break concurrency. And so I'm going to break down the three categories of concurrency down the three categories of concurrency issues that you'll see in your issues that you'll see in your interviews from correctness, interviews from correctness, coordination, and scarcity. Uh show you coordination, and scarcity. Uh show you examples, the tools you need, the examples, the tools you need, the primitives in order to solve each of primitives in order to solve each of these. I'm going to remain pretty high these. I'm going to remain pretty high level here so we can get through this level here so we can get through this quickly, but know that as you see here quickly, but know that as you see here on the site, we have detailed breakdowns on the site, we have detailed breakdowns for each of them. So you can come over for each of them. So you can come over here and read that. We also have problem here and read that. We also have problem breakdowns where we go into common breakdowns where we go into common interview questions. I think four of the interview questions. I think four of the current seven soon to be eight uh have current seven soon to be eight uh have concurrent aspects to them. So we probe concurrent aspects to them. So we probe concurrency in each of them concurrency in each of them respectively. So you can read through respectively. So you can read through those and see how they end up being those and see how they end up being applied. Uh as always, if you like what applied. Uh as always, if you like what we're doing here, please don't forget to we're doing here, please don't forget to like and subscribe. Enough with intros. like and subscribe. Enough with intros. Let's get into it. Let's get into it. So I want you to start by imagining that So I want you to start by imagining that you're booking a ticket booking service you're booking a ticket booking service similar to Ticket Master, but it's in similar to Ticket Master, but it's in process. This is not a distributed process. This is not a distributed system. This is not system design. This system. This is not system design. This is low-level design which means that is low-level design which means that working we're working within a single working we're working within a single process. And so in this booking system process. And so in this booking system you can of course browse seats and book you can of course browse seats and book them. And so you start off by writing them. And so you start off by writing this book seat function which is part of this book seat function which is part of your booking service class where given a your booking service class where given a seat you check if that seat is available seat you check if that seat is available and if yes you book it. And so I have and if yes you book it. And so I have the implementation for you here in Java the implementation for you here in Java as well as in Python if that's better as well as in Python if that's better for you. Now this code may look fine to for you. Now this code may look fine to you and to be frank actually it is fine you and to be frank actually it is fine in singlethreaded languages like in singlethreaded languages like TypeScript or something then there is TypeScript or something then there is nothing wrong with this but if you're nothing wrong with this but if you're operating in a multi-threaded operating in a multi-threaded environment then what's going to happen environment then what's going to happen if two users try to book the same seat if two users try to book the same seat at exactly the same time this would mean at exactly the same time this would mean that two different threads are accessing that two different threads are accessing the share the same shared state the share the same shared state basically the seats map at roughly the basically the seats map at roughly the same time and so let's walk through what same time and so let's walk through what would happen here what you would have is would happen here what you would have is you would have thread A we can call her you would have thread A we can call her Alice in this case is going to check if Alice in this case is going to check if seat 7A is available. It is. Then thread seat 7A is available. It is. Then thread B is also going to check if seat 7A is B is also going to check if seat 7A is available. It also is because thread A available. It also is because thread A or Alice here still hasn't completed her or Alice here still hasn't completed her booking. And so the next thing that's booking. And so the next thing that's going to happen is that Alex is going to going to happen is that Alex is going to book and at the same time then Bob is book and at the same time then Bob is going to book. Bob's book is going to going to book. Bob's book is going to overwrite Alice's book. And now Bob is overwrite Alice's book. And now Bob is the person with the seat. Alice thinks the person with the seat. Alice thinks that she has a seat, but she shows up to that she has a seat, but she shows up to the concert and sees Bob there, and the concert and sees Bob there, and we're in a bunch of trouble. So, this we're in a bunch of trouble. So, this example illustrates what is the most example illustrates what is the most common type of concurrency bug that common type of concurrency bug that shows up in interviews. It's called shows up in interviews. It's called correctness issues, and it's easy to correctness issues, and it's easy to miss if you're not specifically looking miss if you're not specifically looking for it. But, there are plenty of other for it. But, there are plenty of other concurrency challenges that arise in concurrency challenges that arise in these multi-threaded systems, but they these multi-threaded systems, but they fall neatly into three main categories. fall neatly into three main categories. First, we have correctness, like we just First, we have correctness, like we just saw here. Second is coordination, and saw here. Second is coordination, and then third is scarcity. And once you then third is scarcity. And once you know these three categories, you know these three categories, you understand what they are, how they show understand what they are, how they show up, and how to solve them, then you're up, and how to solve them, then you're well equipped to handle just about any well equipped to handle just about any concurrency challenge that's going to be concurrency challenge that's going to be thrown your way in a low-level design or thrown your way in a low-level design or machine coding interview. machine coding interview. Let's go through each of these Let's go through each of these categories one by one, starting with categories one by one, starting with correctness. So, as we just saw, correctness. So, as we just saw, correctness issues happen when shared correctness issues happen when shared state gets corrupted because two threads state gets corrupted because two threads are trying to access it at the exact are trying to access it at the exact same time. Now, the pattern that you same time. Now, the pattern that you want to recognize here that you want to want to recognize here that you want to keep an eye out for in your interviews keep an eye out for in your interviews is oftentimes called check then act. is oftentimes called check then act. Check then act. Basically, you check a Check then act. Basically, you check a condition like right here and then you condition like right here and then you act based on that condition. But between act based on that condition. But between the check and the act, another thread the check and the act, another thread can come in and change things. So, can come in and change things. So, obviously in our ticket booking example, obviously in our ticket booking example, we checked if the seat was available and we checked if the seat was available and then we booked it. And that's two then we booked it. And that's two separate steps. So in that gap between separate steps. So in that gap between the check and the action, another thread the check and the action, another thread can sneak in there and book that seat can sneak in there and book that seat out from under us. And this gap is often out from under us. And this gap is often times where the bug lives. So this is times where the bug lives. So this is the trouble area right here. And this the trouble area right here. And this check then act pattern is probably the check then act pattern is probably the most popular concurrency issue that ends most popular concurrency issue that ends up showing up in low-level design up showing up in low-level design interviews. If you have a parking lot interviews. If you have a parking lot system with multiple entrances, then you system with multiple entrances, then you check if the spot is empty and then check if the spot is empty and then assign a car to it. Check then act. rate assign a car to it. Check then act. rate limiter, you check if the user is under limiter, you check if the user is under their rate limit and then you allow the their rate limit and then you allow the request, check, then act. An inventory request, check, then act. An inventory system, you check if the sock is system, you check if the sock is available and then process the order. available and then process the order. It's all the same shape and the goal is It's all the same shape and the goal is to be able to recognize that. Now, maybe to be able to recognize that. Now, maybe importantly, most importantly, the fix, importantly, most importantly, the fix, at least at a high level, is that the at least at a high level, is that the check and the action need to happen check and the action need to happen together as one atomic operation. This together as one atomic operation. This prevents any thread from being allowed prevents any thread from being allowed to come in between here and interrupt to come in between here and interrupt things. The standard tool for this is things. The standard tool for this is called a lock. And each programming called a lock. And each programming language has its own lock primitives. language has its own lock primitives. You have mutxes in things like Rust and You have mutxes in things like Rust and Go. You have a synchronized block in Go. You have a synchronized block in Java, which we'll see here in just a Java, which we'll see here in just a moment. A lock in Python. Mutex or moment. A lock in Python. Mutex or semaphors in C++. All of these allow semaphors in C++. All of these allow just one thread to execute a section of just one thread to execute a section of code at a time while other threads wait code at a time while other threads wait on that lock to be released. So you on that lock to be released. So you acquire the lock first, do your check, acquire the lock first, do your check, do your action, and then release the do your action, and then release the lock. So let's see what that would look lock. So let's see what that would look like. In the Java case, it's going to like. In the Java case, it's going to look like this. Java uses what's called look like this. Java uses what's called a synchronized block. And so everything a synchronized block. And so everything within the synchronized block is while within the synchronized block is while the lock is held. Basically, if you have the lock is held. Basically, if you have the synchronized block, this is when we the synchronized block, this is when we take the lock, we operate, and then we take the lock, we operate, and then we release the lock at the end of that release the lock at the end of that synchronized block. In the case of synchronized block. In the case of Python, you do with lock and that lock Python, you do with lock and that lock is typically an instance variable. And is typically an instance variable. And so similar idea here, right? You first so similar idea here, right? You first grab the lock and then everything that grab the lock and then everything that is within that block has that lock held is within that block has that lock held and thus if the lock is held no two and thus if the lock is held no two threads can intervene with each other. threads can intervene with each other. If we come back over to our diagram it If we come back over to our diagram it would now look like this. You'd have would now look like this. You'd have thread one or Alice check if things are thread one or Alice check if things are available. Yes. Uh we would then book it available. Yes. Uh we would then book it from Alice. The status equals booked. from Alice. The status equals booked. And that whole time Bob can't come in And that whole time Bob can't come in because Alice first grabbed the lock. because Alice first grabbed the lock. And so Alice is holding the lock. She's And so Alice is holding the lock. She's able to do both the check and the act able to do both the check and the act act. Bob is just waiting. And it's only act. Bob is just waiting. And it's only once that lock is released and she has once that lock is released and she has set the status of that seat to booked set the status of that seat to booked that Bob then can check if it's that Bob then can check if it's available. And now he's going to available. And now he's going to correctly be updated to understand that correctly be updated to understand that the answer is no. The second most common the answer is no. The second most common correctness pattern that shows up is correctness pattern that shows up is what's called read modify write. And so what's called read modify write. And so this is when you read a value, you this is when you read a value, you compute something from it, and then you compute something from it, and then you write it back. So the classic example is write it back. So the classic example is just incrementing a counter. You have a just incrementing a counter. You have a counter here where it's currently at counter here where it's currently at one. We do count plus+. And now this one. We do count plus+. And now this might look to you like one operation. might look to you like one operation. It's one line of code, but it's actually It's one line of code, but it's actually three. We first read the value to see three. We first read the value to see what it currently is. We add one and what it currently is. We add one and then we write back that new value. And then we write back that new value. And so just like the example above, if two so just like the example above, if two threads were doing this at the exact threads were doing this at the exact same time, both might read for example same time, both might read for example five. Both might read for example five. five. Both might read for example five. They might might both compute six. They They might might both compute six. They might both then write six and we're left might both then write six and we're left with six as the final count when it with six as the final count when it should have been seven. We lost an should have been seven. We lost an increment. This shows up obviously in increment. This shows up obviously in hit counters, bank account balances, hit counters, bank account balances, inventory quantities, metric inventory quantities, metric aggregation. Basically, anytime you're aggregation. Basically, anytime you're updating a value based on its current updating a value based on its current state, you're going to run into this state, you're going to run into this read modify write pattern. Now, the fix read modify write pattern. Now, the fix follows the exact same idea. We need to follows the exact same idea. We need to make this whole operation, this read, make this whole operation, this read, modify, and write atomic. And so for modify, and write atomic. And so for simple counters, you can actually use simple counters, you can actually use what's called an atomic variable which what's called an atomic variable which handles this at the hardware level, handles this at the hardware level, which is pretty cool. And so modern which is pretty cool. And so modern CPUs, they have special instructions CPUs, they have special instructions like compare and swap that do a read like compare and swap that do a read modify write in a single CPU cycle. modify write in a single CPU cycle. Basically, the hardware guarantees that Basically, the hardware guarantees that no other threads can see that value mid no other threads can see that value mid update. So let's look at the update. So let's look at the implementation of the solution. Just implementation of the solution. Just like just like with locks, each major like just like with locks, each major language provides its own primitive for language provides its own primitive for an atomic integer. And so in Java it's an atomic integer. And so in Java it's called exactly that an atomic integer. called exactly that an atomic integer. You can instantiate your atomic integer You can instantiate your atomic integer to whatever you want. In this case zero to whatever you want. In this case zero maybe we say 10. And then anytime you maybe we say 10. And then anytime you want to increment it by one instead of want to increment it by one instead of doing count plus+ you have to do doing count plus+ you have to do count.increment and get this is going to count.increment and get this is going to be that hardwell hardware level atomic be that hardwell hardware level atomic operation. And so this would then return operation. And so this would then return back to you 11 in our case. And it's back to you 11 in our case. And it's going to be using that compare and swap going to be using that compare and swap at the actual CPU. Now, unfortunately, at the actual CPU. Now, unfortunately, Python doesn't actually support built-in Python doesn't actually support built-in atomics. So, the way to solve this in atomics. So, the way to solve this in Python, unfortunately, there is no Python, unfortunately, there is no atomic that you can use, you're going to atomic that you can use, you're going to have to just do what we did above, and have to just do what we did above, and you're going to need to use a lock. So, you're going to need to use a lock. So, I know it's a little bit annoying I know it's a little bit annoying depending on the language that you're depending on the language that you're implementing your low-level design implementing your low-level design interview in. Uh you may have an atomic interview in. Uh you may have an atomic primitive, you may not. How do you know primitive, you may not. How do you know when to use each? When do you use an when to use each? When do you use an atomic variable and when do you use a atomic variable and when do you use a law? Well, you're going to want to use law? Well, you're going to want to use an atomic variable when you're updating an atomic variable when you're updating just a single variable or counter. So just a single variable or counter. So things like flags or simple statistics. things like flags or simple statistics. But the moment that you have to update But the moment that you have to update two things, the moment that you have to two things, the moment that you have to update two separate variables and they update two separate variables and they need to stay consistent with each other, need to stay consistent with each other, something like transferring money something like transferring money between accounts, well then atomics between accounts, well then atomics can't help you here anymore. You need to can't help you here anymore. You need to rely on the lock. So to summarize, we rely on the lock. So to summarize, we had two main patterns that show up in had two main patterns that show up in correctness. We have check then act and correctness. We have check then act and then we have read modify write. Both then we have read modify write. Both have the same root cause, a gap between have the same root cause, a gap between operations where another thread can operations where another thread can interfere. And the fix is the same for interfere. And the fix is the same for both. Make the operation atomic, usually both. Make the operation atomic, usually with a lock, but sometimes you can use with a lock, but sometimes you can use an atomic variable depending on the an atomic variable depending on the language and depending on whether or not language and depending on whether or not The second category of concurrency The second category of concurrency problems is what's called coordination. problems is what's called coordination. And so let's say that you are building And so let's say that you are building zoom in here. Let's say that you're zoom in here. Let's say that you're building a website and users sign up and building a website and users sign up and you need to send them a welcome email. you need to send them a welcome email. But sending an email takes about 500 But sending an email takes about 500 milliseconds, half a second. and you milliseconds, half a second. and you don't want the signup request to be don't want the signup request to be blocked for that full half second. And blocked for that full half second. And so you basically want to hand off that so you basically want to hand off that expensive work. You want to hand off the expensive work. You want to hand off the sending of the email to a background sending of the email to a background thread and then just return the signup thread and then just return the signup request to the user immediately. So you request to the user immediately. So you have these API threads that need to pass have these API threads that need to pass tasks to email sender or worker threads. tasks to email sender or worker threads. And the natural solution is to just put And the natural solution is to just put a queue between the two of them. So the a queue between the two of them. So the producer pushes tasks into the queue and producer pushes tasks into the queue and then the consumers or the email senders then the consumers or the email senders can just pull things off of the queue can just pull things off of the queue and send those emails as they see fit. and send those emails as they see fit. This seems simple but in doing so you've This seems simple but in doing so you've actually introduced two problems. The actually introduced two problems. The first is how does this worker know when first is how does this worker know when new work has arrived? Let me write that. new work has arrived? Let me write that. How does consumer consumer know that How does consumer consumer know that work arrived and how does it know so work arrived and how does it know so efficiently? We'll talk about this in efficiently? We'll talk about this in just a moment. And then the second is just a moment. And then the second is what happens if work is arriving too what happens if work is arriving too fast for consumer to keep up. So you fast for consumer to keep up. So you could imagine that we have tons of send could imagine that we have tons of send emails here. We only have a couple emails here. We only have a couple worker threads and this queue just keeps worker threads and this queue just keeps getting larger and larger and larger. So getting larger and larger and larger. So those are the two problems that we need those are the two problems that we need to solve. And let's go ahead and start to solve. And let's go ahead and start with the first one. So basically your with the first one. So basically your worker thread needs to be able to wait worker thread needs to be able to wait for tasks. And now the naive approach is for tasks. And now the naive approach is that your thread is just going to spin that your thread is just going to spin in a loop always checking if there is in a loop always checking if there is work. And so you can see here we'll just work. And so you can see here we'll just say while true if the que is empty then say while true if the que is empty then try to pull off the queue and process try to pull off the queue and process the task. And this is just going to keep the task. And this is just going to keep processing while true basically processing while true basically non-stop. You can see again our job in non-stop. You can see again our job in our Python implementation. Well, there's our Python implementation. Well, there's a clear issue with this and that's that a clear issue with this and that's that we're just burning CPU doing nothing. we're just burning CPU doing nothing. this thread is fully occupied by just this thread is fully occupied by just doing that looping and looping and doing that looping and looping and looping always checking the queue. Are looping always checking the queue. Are you ready? Are you ready? Are you ready? you ready? Are you ready? Are you ready? Are you ready? And this is wasting Are you ready? And this is wasting valuable CPU resources that we could be valuable CPU resources that we could be using elsewhere. So the first solution using elsewhere. So the first solution that you might have thought of is well, that you might have thought of is well, we could sleep and pull. Basically, we could sleep and pull. Basically, let's introduce a sleep here so that let's introduce a sleep here so that we're not just but maybe we're waiting we're not just but maybe we're waiting just a little bit. And so that would just a little bit. And so that would look something like this. Let me put it look something like this. Let me put it in here for Java. And in this case, in here for Java. And in this case, we're going to try to pull. If we didn't we're going to try to pull. If we didn't get it, then we're going to sleep for get it, then we're going to sleep for just 100 milliseconds. And then we'll just 100 milliseconds. And then we'll try again. This will make sure that try again. This will make sure that we're wasting less CPU. And I'll paste we're wasting less CPU. And I'll paste in here the same thing for Python, so in here the same thing for Python, so you can see what that would look like. you can see what that would look like. Now, this might be better. You're Now, this might be better. You're wasting fewer cycles, but you're still wasting fewer cycles, but you're still wasting cycles, but you've also wasting cycles, but you've also introduced latency. And so now if a new introduced latency. And so now if a new job comes in to send the email that job comes in to send the email that should take 500 milliseconds, it could should take 500 milliseconds, it could take 600 milliseconds because it could take 600 milliseconds because it could land and we have to wait this 100 land and we have to wait this 100 milliseconds before we're even able to milliseconds before we're even able to pull it off of the queue. What you pull it off of the queue. What you actually want is for the workers to actually want is for the workers to sleep when there's nothing to do and sleep when there's nothing to do and then wake up instantly the moment that then wake up instantly the moment that that work arrives. The solution is to that work arrives. The solution is to use what's called a blocking queue. And use what's called a blocking queue. And just like with locks and just like with just like with locks and just like with atomic variables, most major languages atomic variables, most major languages have a blocking q built in. The way that have a blocking q built in. The way that it works is that the worker calls a it works is that the worker calls a function called take. And if they call function called take. And if they call take on the empty queue, then that take on the empty queue, then that thread actually just goes to sleep. And thread actually just goes to sleep. And then when a producer calls put to put then when a producer calls put to put something into the queue, it something into the queue, it automatically wakes up all one of the automatically wakes up all one of the sleeping workers saying there's sleeping workers saying there's something for you to process now. So let something for you to process now. So let me paste this in so you can see what the me paste this in so you can see what the solution looks like starting with Java. solution looks like starting with Java. And so in Java, we're going to And so in Java, we're going to instantiate, let's ignore that for now. instantiate, let's ignore that for now. We're going to instantiate a blocking Q. We're going to instantiate a blocking Q. And so when a new user comes in, we're And so when a new user comes in, we're going to put onto that Q and return going to put onto that Q and return success. Cool. That thread is done. Now success. Cool. That thread is done. Now on the other end for our consumer, while on the other end for our consumer, while it's still in a while loop, if it calls it's still in a while loop, if it calls Q.take and this is empty, it's just Q.take and this is empty, it's just going to go to sleep at this moment. going to go to sleep at this moment. It's going to say everybody else can use It's going to say everybody else can use the CPU. I don't need it now. And then the CPU. I don't need it now. And then it's not until that next put comes in it's not until that next put comes in that this is going to automatically wake that this is going to automatically wake up, process the task, and then repeat. up, process the task, and then repeat. If it's empty, sleep, and so on. And I If it's empty, sleep, and so on. And I can show you what that implementation can show you what that implementation looks like in Python as well. Same idea. looks like in Python as well. Same idea. Python's Q is actually a blocking Q by Python's Q is actually a blocking Q by default. Let me remove that for now. default. Let me remove that for now. It's actually a blocking Q by default. It's actually a blocking Q by default. And so the same thing, you have a put, And so the same thing, you have a put, but instead of take, you have a get in but instead of take, you have a get in Python. So slightly different API, but Python. So slightly different API, but the same general concept. So now let's the same general concept. So now let's come back to what that second issue was come back to what that second issue was within coordination. Basically, what within coordination. Basically, what happens if a worker is arriving? If work happens if a worker is arriving? If work is arriving too fast for the consumers is arriving too fast for the consumers to be able to keep up, you could say to be able to keep up, you could say maybe there's marketing emails that just maybe there's marketing emails that just went out. 50,000 users just clicked on went out. 50,000 users just clicked on the link all at once. This generated a the link all at once. This generated a bunch of background tasks, a bunch of bunch of background tasks, a bunch of emails to be sent out. The queue grew emails to be sent out. The queue grew and grew and grew. It's eventually and grew and grew. It's eventually unbounded and as a result, we run out of unbounded and as a result, we run out of memory and the entire process crashes. memory and the entire process crashes. That's what we're trying to avoid here. That's what we're trying to avoid here. And so the solution is to use a bounded And so the solution is to use a bounded blocking queue. You basically set a blocking queue. You basically set a maximum size and when it's full, maximum size and when it's full, producers block until there's room. This producers block until there's room. This is what's called back pressure. It's is what's called back pressure. It's very important. So the system naturally very important. So the system naturally slows down when it can't keep up. And so slows down when it can't keep up. And so blocking cues will solve this problem blocking cues will solve this problem for us. And the implementation of a for us. And the implementation of a blocking Q is really straightforward. blocking Q is really straightforward. You actually saw me remove it cuz I was You actually saw me remove it cuz I was a little ahead of myself. But it's just a little ahead of myself. But it's just that when you instantiate these blocking that when you instantiate these blocking cues, you can instantiate them um with cues, you can instantiate them um with the maximum number of items that you the maximum number of items that you want in the queue before we start to want in the queue before we start to apply some back pressure. And that'll apply some back pressure. And that'll naturally make it a bounded block and Q. naturally make it a bounded block and Q. So you can see that in the case there. So you can see that in the case there. And then in Python, we already removed And then in Python, we already removed it, but it was that max size 1,00 it, but it was that max size 1,00 parameter. Now, using a blocking Q, parameter. Now, using a blocking Q, specifically a bounded blocking Q, specifically a bounded blocking Q, you're going to almost always want to you're going to almost always want to make sure that you bound it. I see no make sure that you bound it. I see no reason why you would ever not bound it. reason why you would ever not bound it. But this is going to show up in But this is going to show up in interviews whenever you need to process interviews whenever you need to process something asynchronously. So taskulers, something asynchronously. So taskulers, background job processor, message cues. background job processor, message cues. If work is flowing un in from one If work is flowing un in from one component to another, from one thread component to another, from one thread specifically to another, then that's specifically to another, then that's coordination. And the blocking queue is coordination. And the blocking queue is almost always your answer. There's more almost always your answer. There's more to it, of course. How do you size the to it, of course. How do you size the buffer appropriately? What happens buffer appropriately? What happens during shutdown? Uh what if blocking during shutdown? Uh what if blocking isn't acceptable on your request path? isn't acceptable on your request path? The main thing to remember for your The main thing to remember for your interview is that if you need interview is that if you need coordination, then you're going to want coordination, then you're going to want The final category of concurrency The final category of concurrency problems is called scarcity. And this is problems is called scarcity. And this is when you need to limit concurrent access when you need to limit concurrent access to finite resources. So for example, I to finite resources. So for example, I want you to say that you're calling an want you to say that you're calling an external API that only allows 10 external API that only allows 10 concurrent requests. That's its rate concurrent requests. That's its rate limit. And you have 50 threads that limit. And you have 50 threads that might all be trying to call it might all be trying to call it simultaneously. You need some way to say simultaneously. You need some way to say only 10 of you can be here at any given only 10 of you can be here at any given time. time. time. The tool for this is what's called a The tool for this is what's called a semaphore. You probably remember semaphore. You probably remember semaphors. I'm sure you learned them in semaphors. I'm sure you learned them in school. And you can think of semaphors school. And you can think of semaphors conceptually as just a bucket of conceptually as just a bucket of permits. Before you do some operation, permits. Before you do some operation, you grab a permit out of the bucket. And you grab a permit out of the bucket. And once you finish that operation, you put once you finish that operation, you put the permit back. If you arrive and there the permit back. If you arrive and there are no permits in the bucket, then you are no permits in the bucket, then you need to wait until somebody comes and re need to wait until somebody comes and re replenishes the permits. So let's look replenishes the permits. So let's look at the implementations and again Java at the implementations and again Java and Python. In Java, you have a built-in and Python. In Java, you have a built-in semaphore just like the others. This is semaphore just like the others. This is present in most the languages. And in present in most the languages. And in this case, let's say that you're this case, let's say that you're downloading some expensive file and you downloading some expensive file and you want to make sure that you're only want to make sure that you're only downloading five files at the same time. downloading five files at the same time. Maybe you don't want to run out of Maybe you don't want to run out of memory or you don't want to run out of memory or you don't want to run out of I/IO. And so you first acquire that I/IO. And so you first acquire that permit. You basically grab the permit permit. You basically grab the permit out of the bucket. You do that expensive out of the bucket. You do that expensive download and then you release the download and then you release the permit. And this is going to guarantee permit. And this is going to guarantee that only five threads can be doing this that only five threads can be doing this download at the same time. Exact same download at the same time. Exact same concept in Python. You have the concept in Python. You have the semaphore, you acquire, and you release semaphore, you acquire, and you release respectively. Now, here's one respectively. Now, here's one interesting thing to call out. What interesting thing to call out. What happens if this do download threw an happens if this do download threw an exception? If this were to throw an exception? If this were to throw an exception, then we would break out of exception, then we would break out of this download function. The exception this download function. The exception would bubble up and we would never would bubble up and we would never release or return the semaphore, right? release or return the semaphore, right? We would never return that permit. If We would never return that permit. If that happened five times, then your that happened five times, then your bucket of permits, to continue with this bucket of permits, to continue with this analogy, would be completely empty. None analogy, would be completely empty. None of your semaphors would have released of your semaphors would have released because they all threw and they'd be because they all threw and they'd be holding on to that semaphore forever. holding on to that semaphore forever. This would bring everything to a halting This would bring everything to a halting uh to a screeching halt, excuse me, and uh to a screeching halt, excuse me, and there would be no more downloads. This there would be no more downloads. This is a pretty common bug that you want to is a pretty common bug that you want to look out for in your interviews whenever look out for in your interviews whenever you're dealing with scarcity is can I you're dealing with scarcity is can I guarantee that this semaphore is going guarantee that this semaphore is going to be put back, that this permit is to be put back, that this permit is going to be put back so that others can going to be put back so that others can use it. And the solution, fortunately, use it. And the solution, fortunately, is really straightforward. You just want is really straightforward. You just want to wrap this in a try catch with a to wrap this in a try catch with a finally. So, you're going to try the do finally. So, you're going to try the do download. If it fails or anything goes download. If it fails or anything goes wrong at all, then the permit is going wrong at all, then the permit is going to be released. So, we can do that exact to be released. So, we can do that exact same thing. Of course, in Python, you same thing. Of course, in Python, you all know probably what this looks like, all know probably what this looks like, but for completeness, it'll look just but for completeness, it'll look just like that. Now, sometimes it's the case like that. Now, sometimes it's the case that you're not just counting, you're that you're not just counting, you're not just counting the number of not just counting the number of requests, but you're actually managing requests, but you're actually managing actual objects that have state, right? actual objects that have state, right? Stare with me. This is this is pretty Stare with me. This is this is pretty important. So, the example here would be important. So, the example here would be like database connections. They're the like database connections. They're the classic example. Each connection holds classic example. Each connection holds an open TCP socket. It consumes memory. an open TCP socket. It consumes memory. it tracks transition state and so you it tracks transition state and so you can't create a new one for every single can't create a new one for every single request or every single query you want request or every single query you want to hit the database with. Instead, you to hit the database with. Instead, you create a connection pool. You create create a connection pool. You create maybe 10 open connections and then all maybe 10 open connections and then all your threads can reuse those 10 open your threads can reuse those 10 open connections so we're not having to open connections so we're not having to open and close connections all the time. And and close connections all the time. And this is a different flavor of a scarcity this is a different flavor of a scarcity problem. In this case, we're just problem. In this case, we're just counting. In the case that I'm counting. In the case that I'm describing now, you have a fixed set of describing now, you have a fixed set of resources. And the solution is the same resources. And the solution is the same that we saw in coordination. You can use that we saw in coordination. You can use a blocking queue. You fill it up with a blocking queue. You fill it up with your objects threads take one out, use your objects threads take one out, use it, and then put it back. So it's the it, and then put it back. So it's the same underlying primitive, a blocking same underlying primitive, a blocking queue, but we're not using it in quite queue, but we're not using it in quite the same way. We're basically using it the same way. We're basically using it as like a bag of objects or a pool of as like a bag of objects or a pool of objects that can be reused. Let me show objects that can be reused. Let me show you exactly what that would look like. you exactly what that would look like. Um, so let me just write this. when Um, so let me just write this. when managing actual managing actual So here's the Java implementation. So here's the Java implementation. Again, we have our blocking Q. We should Again, we have our blocking Q. We should always instantiate it with a limit, always instantiate it with a limit, right? Um, and then you can load up your right? Um, and then you can load up your connections. So maybe we have 10 connections. So maybe we have 10 connections. So we put 10 connections connections. So we put 10 connections into our pool. Those are now in the into our pool. Those are now in the queue. And every time you want to run a queue. And every time you want to run a query, the first thing that you're going query, the first thing that you're going to do is take from the pool. Of course, to do is take from the pool. Of course, if none already, then we're just blocked if none already, then we're just blocked and we're waiting, as you remember from and we're waiting, as you remember from coordination. We're going to run our coordination. We're going to run our query. And then finally, we're going to query. And then finally, we're going to put it back into the pool. So, as put it back into the pool. So, as opposed to releasing locks or putting opposed to releasing locks or putting the semaphore back, we're going to just the semaphore back, we're going to just put the connection back into the pool. put the connection back into the pool. Basically, calling put again, and the Basically, calling put again, and the next thread that is waiting can pick up next thread that is waiting can pick up at that point. I'll show you, of course, at that point. I'll show you, of course, what it looks like in Python, too. what it looks like in Python, too. exact same idea there. The pattern of exact same idea there. The pattern of these two scarcity problems is the same these two scarcity problems is the same either way. You acquire something either way. You acquire something scarce, you use it, you then release it, scarce, you use it, you then release it, and you make sure that release happens and you make sure that release happens even when things fail by using that even when things fail by using that finally block. finally block. All right, so let's zoom back out here All right, so let's zoom back out here as we wrap things up. If you find as we wrap things up. If you find yourself in a low-level design interview yourself in a low-level design interview and concurrency comes up, here's your and concurrency comes up, here's your mental checklist. The first thing you mental checklist. The first thing you want to ask yourself is that is there want to ask yourself is that is there shared state that multiple threads can shared state that multiple threads can access? If the answer is yes, then access? If the answer is yes, then you're talking about a correctness you're talking about a correctness issue. And for correctness issues, we issue. And for correctness issues, we talked about both read modify write as talked about both read modify write as well as check then act. And in either well as check then act. And in either case, you want to use either a lock or case, you want to use either a lock or atomic variables. The next thing you atomic variables. The next thing you want to consider is is work flowing from want to consider is is work flowing from one thread to another. If so, then one thread to another. If so, then that's a coordination problem. And so that's a coordination problem. And so you think about what happens when you think about what happens when there's no work and what happens when there's no work and what happens when there's too much work. And in both there's too much work. And in both cases, you probably want to use a cases, you probably want to use a blocking queue or bounded blocking blocking queue or bounded blocking queue. And then third, we talked about queue. And then third, we talked about if there was a fixed limit of something, if there was a fixed limit of something, then it's scarcity. And so, make sure then it's scarcity. And so, make sure that resources always get released, of that resources always get released, of course, in that finally block. Uh, and course, in that finally block. Uh, and then you probably want to reach for then you probably want to reach for either a semaphore or a blocking queue either a semaphore or a blocking queue to be your pool as we discussed. Now, to be your pool as we discussed. Now, most uh most interview problems, they most uh most interview problems, they map cleanly to one of these three map cleanly to one of these three categories. And so, once you're able to categories. And so, once you're able to recognize these three, then you can see recognize these three, then you can see them in the interview and you know them in the interview and you know exactly which solutions to apply. And exactly which solutions to apply. And things end up being pretty simple. So things end up being pretty simple. So hopefully this was valuable. As I had hopefully this was valuable. As I had mentioned, we have full written mentioned, we have full written breakdowns on each of these correctness, breakdowns on each of these correctness, coordination, and scarcity with lots coordination, and scarcity with lots more examples and each of the core more examples and each of the core programming languages, what you should programming languages, what you should look out for in interview, the corner look out for in interview, the corner cases, far more detail than I've gone cases, far more detail than I've gone into in this video. That's going to be into in this video. That's going to be linked in the description that you can linked in the description that you can check out. Unfortunately, that's only check out. Unfortunately, that's only for premium users. Um, and then as for premium users. Um, and then as always, if you like the content, like always, if you like the content, like and subscribe. Uh, we'll be back with and subscribe. Uh, we'll be back with